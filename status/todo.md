# Cust TODO

The autonomous agent should complete a meaningful roadmap work package per cron run: usually one full C/interpreter/tooling feature or 2-4 tightly related backlog items. Prefer finishing concrete unchecked items with code/tests over generic improvement or status-only churn.

## Next recommended tasks

1. [x] Single-statement control-flow bodies and `else if` chains for `if`/`else`/`while`/`do`/`for`, including C dangling-`else` binding and compiler-oracle coverage
2. [x] C-style block comments `/* ... */` as lexer whitespace, including multi-line/inline comments, exact unterminated-comment diagnostics, and compiler-oracle coverage
3. [x] `void` helper functions and `return;` support; acceptance: lexer/parser/interpreter handle `void name(...) { ... }`, empty returns in void functions, side-effect-only void calls, int/void return-shape diagnostics, scalar-use diagnostics for void calls, and C compiler-oracle coverage
4. [x] Top-level global variables for scalar `int`/`char`, arrays, and supported pointer globals; acceptance: globals initialize before `main()`, persist in an outer scope, are visible/mutable from helper functions, reject duplicates in the global scope, and include valid/invalid interpreter fixtures plus C compiler-oracle coverage.
5. [x] Add `sizeof` support as the next expression-level C-subset feature; lexer/parser/interpreter handle `sizeof(int)`, `sizeof(char)`, pointer type sizes, scalar variables, arrays with declared element sizes, pointer expressions, string literals, and indexed expressions with defined Cust sizes (`int=8`, `char=1`, pointer `=8`), exact `sizeof(void)` diagnostics, interpreter fixtures, and stable native C compiler-oracle coverage for char/string/char-array sizes.
6. [x] Support uninitialized scalar and pointer declarations; acceptance: `int x;` / `char c;` default to `0`, supported pointer declarations such as `int *p;` default to null, array zero-initialization continues to work, exact missing-`=` diagnostics still fire for malformed declarations, and stable global zero-initialization has C compiler-oracle coverage.
- [x] Continue parser recovery/error-message expansion only for newly discovered malformed programs not already covered by the exact-error suite; acceptance: add focused exact-output parser tests before implementation for remaining delimiter/list boundary errors, unsupported near-future C forms, or other malformed programs that still fall through to generic messages. Switch missing-case-colon coverage, duplicate switch case/default label coverage, conditional-operator missing-colon, do-while missing-semicolon, increment/decrement non-lvalue coverage, pointer-bitwise operand diagnostics, bitwise-compound pointer diagnostics, invalid shift-count diagnostics, int/void return-shape diagnostics, scalar-use diagnostics for void calls, duplicate global diagnostics, missing-assignment diagnostics, unterminated block-comment lexer diagnostics, parenthesized pointer declarator diagnostics, multidimensional array declaration/parameter/field diagnostics, function-pointer declaration/typedef/local/parameter unsupported diagnostics, variadic function parameter diagnostics, unsupported `goto`/label diagnostics, comma-in-enum/switch integer-constant-expression diagnostics, pointer-cast const-discard diagnostics, one-level pointer-cast expression coverage, anonymous aggregate parameter/return diagnostics, enum/aggregate forward-declaration diagnostics, K&R/old-style function parameter-list diagnostics, and anonymous aggregate pointer-to-pointer/pointer-array declaration diagnostics are now included, so future malformed control/expression/declaration work should target only new precise failures.
8. [x] Add `LICENSE` file after confirming the project-owner's intended license; acceptance: repository root includes the chosen license text and README license section points to it.
9. [x] Function prototypes/declarations at top level; acceptance: parser accepts semicolon-terminated `int helper(int value);` / `void mutate(int *slot);`, ignores compatible duplicate declarations for runtime, rejects incompatible declarations/definitions with an exact diagnostic, and includes valid/invalid interpreter fixtures plus native C compiler-oracle coverage.
10. [x] `char` function return types and prototypes; acceptance: parser accepts `char name(...)` definitions/declarations, calls return scalar integer values, empty returns from `char` functions report an exact scalar return-shape diagnostic, `sizeof(char_return_call())` uses Cust's char size, and interpreter plus native C compiler-oracle fixtures cover the supported subset.
11. [x] Design and implement a deliberately scoped pointer-arithmetic milestone for array-backed pointers only; acceptance: write/update a short plan before code, support `p + n`, `p - n`, `p += n`, `p -= n`, `p++`/`p--`, and pointer difference only where both operands refer to the same array/string storage, preserve null/scalar-pointer unsupported diagnostics, include negative/out-of-bounds/read-only write diagnostics, and add interpreter/invalid/compiler-oracle fixtures.
12. [x] Design and implement a preprocessor-free struct first milestone; acceptance: document value/storage model, supported declaration syntax (`struct Point { int x; int y; };`), member access (`p.x`), assignment/copy limitations, function parameter rules, parser diagnostics for unsupported forms, and concrete valid/invalid/compiler-oracle fixtures.
13. [x] Extend struct support with same-type struct copy assignment and field lvalue expressions; acceptance: `a = b;` copies same-type struct fields, mismatched struct assignments report a targeted diagnostic, `p.x += 1`, `++p.x`, and `return p.x = 3;` work as scalar field lvalue expressions, and valid/invalid/compiler-oracle fixtures cover the supported subset.
14. [x] Design and implement struct by-value function parameters; acceptance: parser accepts `int f(struct Point p)` parameters after a prior struct declaration, calls copy same-type struct arguments by value, callee field writes do not mutate the caller's struct, mismatched/non-struct arguments report targeted diagnostics, prototypes check struct-parameter signatures, and valid/invalid/compiler-oracle fixtures cover the supported subset.
15. [x] Design and implement struct return types; acceptance: parser accepts `struct Point make_point(...)` definitions/prototypes after a prior struct declaration, callers receive a by-value copy, returned local structs remain valid after the callee exits, scalar/void return-shape diagnostics remain intact, mismatched return struct types report targeted diagnostics, and valid/invalid/compiler-oracle fixtures cover the supported subset.
16. [x] Design and implement a scoped pointers-to-structs milestone; acceptance: update `docs/plans/struct-model.md`/`docs/plans/pointer-model.md`, support `struct Point *p = &point;`, `p->x` reads/writes, `(*p).x` field access, null/out-of-scope diagnostics, and valid/invalid/compiler-oracle fixtures without adding nested/aggregate fields.
17. [x] Design and implement a scoped `typedef` alias milestone; acceptance: document supported alias forms (`typedef int Count;`, `typedef char Byte;`, and `typedef struct Point Point;` after a prior struct declaration), add lexer/parser/interpreter support without changing runtime storage, preserve existing exact diagnostics, and add valid/invalid/compiler-oracle fixtures.
18. [x] Extend `typedef` aliases to block scopes; acceptance: parser maintains lexical typedef scopes, inner block aliases may shadow outer aliases, aliases expire when leaving a block, `sizeof(alias)` observes the innermost alias, and valid/invalid/compiler-oracle fixtures cover shadowing and expiry.
19. [x] Implement a carefully scoped first-pass `const` qualifier milestone; acceptance: lexer/parser accept leading `const int` / `const char` scalar declarations, zero-initialized arrays, and scalar function parameters; runtime rejects direct assignment, compound assignment, increment/decrement, parameter mutation, and pointer writes to const scalars, marks const arrays read-only, and includes valid/invalid/compiler-oracle fixtures.
20. [x] Consider additional C-subset features only with clear acceptance fixtures; good candidates after first-pass `const` include pointer typedef aliases, enum typedefs, `sizeof(const type)`, const-qualified pointers (`const int *p` vs `int * const p`), or aggregate/nested struct fields. Assignment expressions, arithmetic compound assignment, bitwise/shift compound assignment, increment/decrement, bitwise/shift operators, the conditional operator, the comma operator, switch statements, do-while loops, block comments, void helper functions, global variables, `sizeof`, uninitialized scalar/pointer declarations, enum constant declarations, function prototypes, `char` function returns, scoped array-backed pointer arithmetic, first-pass structs, struct field lvalues/copy assignment, struct by-value parameters, struct return types, struct pointers/`->`, top-level/block-scoped typedef aliases, and leading scalar/array/parameter const qualifiers are now covered, so future expression/control work should avoid regressing exact delimiter diagnostics such as missing `]` inside index expressions.
21. [x] Implement pointer typedef aliases; acceptance: `typedef int *IntPtr;`, `typedef char *CharPtr;`, and `typedef struct Point *PointPtr;` resolve as parser-only pointer aliases usable in local/global declarations, parameters, prototypes, pointer calls, struct-pointer field access, and `sizeof(pointer_alias)`, while direct and alias-based pointer-to-pointer typedefs report exact diagnostics and compiler-oracle coverage verifies supported runtime behavior.
22. [x] Add enum typedef aliases as the next scoped typedef/data-type feature; acceptance: document that Cust keeps enum aliases as integer-only parser aliases, support `typedef enum State State;` after prior named enum declarations with block-scoped enum-tag lookup, preserve existing enum constants/read-only diagnostics, add valid/invalid fixtures and a compiler-oracle fixture.
23. [x] Add `sizeof(const type)` parsing for supported scalar/pointer type forms; acceptance: `sizeof(const int)` equals `sizeof(int)`, `sizeof(const char)` equals `sizeof(char)`, supported pointer-qualified spelling has deterministic pointer size if selected, `sizeof(const void)` remains rejected, and exact diagnostics preserve existing `sizeof` expression/type boundaries.
24. [x] Design and implement const-qualified pointer semantics beyond size/type contexts; acceptance: define Cust's supported subset for `const int *p` versus `int * const p`, implement parser/runtime enforcement for selected pointer declarations/parameters, preserve existing const scalar/array behavior, and add valid/invalid/compiler-oracle fixtures.
25. [x] Add stricter pointer const-conversion diagnostics; acceptance: Cust rejects assignments/argument passing/declarations that discard `points_to_const` metadata (for example assigning `const int *` to `int *`), documents the supported conservative subset, and adds exact valid/invalid/compiler-oracle coverage without regressing existing pointer alias behavior.
26. [x] Add const-qualified struct variables/parameters and struct-pointer const enforcement; acceptance: `const struct Point p;` and `const Point p;` are read-only struct bindings, `int f(const struct Point p)` gets a read-only by-value copy, `const struct Point *p` rejects `p->field` / `(*p).field` writes including compound/increment forms, `struct Point * const p` rejects pointer-slot reassignment while preserving mutable target writes, const-to-mutable struct pointer conversions are rejected, and valid/invalid/compiler-oracle fixtures cover the supported subset.
27. [x] Add const-qualified scalar struct fields inside `struct` definitions with concrete fixtures; acceptance: `const int`/`const char` fields are readable, mutable sibling fields remain writable, direct/pointer writes to const fields report `cannot assign to const struct field '<field>'`, whole-struct copy assignment into such structs is rejected, and interpreter plus compiler-oracle fixtures cover the supported subset.
28. [x] Add static storage-class parsing for top-level declarations; acceptance: `static int total`, `static int *p`, `static struct Point value`, `static int helper(...);`, and `static void helper(...) { ... }` parse and behave like ordinary single-file globals/functions, and interpreter plus compiler-oracle fixtures cover the supported subset.
29. [x] Add persistent local static storage for supported local declarations; acceptance: `static int counter = seed();`, `static int values[3];`, `static struct Point point;`, and supported static pointer locals initialize once, persist across calls, retain lexical/block visibility, and remain safe targets for Cust pointers after the declaring function returns, with valid/invalid/compiler-oracle fixtures.
30. [x] Add C-style empty `void` parameter lists for functions and prototypes; acceptance: `int main(void)`, `int helper(void);`, and `void side_effect(void)` parse as zero-argument signatures, calls still enforce zero arguments, malformed named `void` parameters report a targeted diagnostic, and valid/invalid/compiler-oracle fixtures cover the supported subset.
31. [x] Add scalar brace initializers for one-dimensional arrays; acceptance: `int values[3] = {1, 2};`, `char bytes[4] = {1, 0, 2};`, `static int seen[3] = {4, 5};`, and `const int table[2] = {8, 9};` work in global/local/static declaration contexts, omitted elements are zero-filled, excess initializers report a targeted diagnostic, and valid/invalid/compiler-oracle fixtures cover the supported subset.
32. [x] Add scalar brace initializers for supported structs; acceptance: `struct Point p = {1, 2};`, `const struct Config c = {7, 8};`, and `static struct Point saved = {3, 4};` initialize scalar fields in declaration order, omitted fields are zero-filled, const fields can be initialized but remain read-only afterward, excess initializers report a targeted diagnostic, and valid/invalid/compiler-oracle fixtures cover the supported subset.
33. [x] Add nested struct fields as the next scoped aggregate data-type feature; acceptance: `struct Rect { struct Point origin; int width; };` stores nested struct fields with deterministic Cust layout, `rect.origin.x` reads/writes through field paths, nested struct fields can be copied and passed by value as same-type struct values, `sizeof(rect.origin)` uses recursive Cust sizes, unknown nested fields report the innermost struct type diagnostic, and valid/invalid/compiler-oracle fixtures cover the supported subset.
34. [x] Add nested aggregate brace initializers as the next scoped C-subset data-type feature; acceptance: `struct Rect r = {{1, 2}, 3};` recursively initializes nested struct fields, zero-fills omitted nested entries, preserves static/const declaration behavior, rejects excess nested entries with nested struct diagnostics, and includes valid/invalid/compiler-oracle fixtures.
35. [x] Add one-dimensional scalar array fields inside structs; acceptance: `struct Packet { int values[3]; char tag[2]; };` supports array-field brace initializers, zero-fill, element reads/writes/compound/increment lvalues, `sizeof(array_field)`, same-type copy/by-value parameter isolation, invalid excess-initializer diagnostics, and compiler-oracle coverage.
36. [x] Design and implement pointer fields inside structs as the next scoped aggregate/pointer milestone; acceptance: one-level scalar/struct pointer fields such as `int *next_value;` and `struct Node *next;` use interpreter-owned pointer metadata, support pointer-field initializers/reassignment and `node.next->value` / `*node.external`, preserve const-pointee conversion checks, reject unsupported pointer-to-pointer/array-pointer field forms with targeted diagnostics, and add valid/invalid/compiler-oracle fixtures without native ABI layout assumptions.
37. [x] Design and implement arrays of supported struct types as the next aggregate milestone; acceptance: `struct Point points[2];` stores zero-initialized struct elements, supports `points[i].x` field-path reads/writes/lvalues, brace initializers for struct arrays, copy/by-value semantics that deep-clone struct elements and embedded arrays while pointer fields copy pointer values, targeted invalid bounds/initializer diagnostics, and compiler-oracle coverage without native struct-size assumptions.
38. [x] Add support for address-of struct-array elements and struct fields as a scoped pointer/aggregate follow-up; acceptance: `struct Point *p = &points[0];`, `&point.x`, nested field paths such as `&packet.anchor.y`, and `&points[i].x` produce interpreter-owned pointers with null/out-of-scope/const diagnostics, and valid/invalid/compiler-oracle fixtures cover supported forms without enabling pointer-to-pointer or unsafe host addresses.
39. [x] Add designated initializers for supported arrays and structs as the next aggregate feature; acceptance: document deterministic supported syntax, support scalar array designators (`[index] = expr`), struct field designators (`.field = value`) in any order, nested brace-list designators for supported aggregate fields, targeted invalid bounds/unknown-field diagnostics, and valid/invalid/compiler-oracle fixtures without native ABI padding assumptions.
40. [x] Add path designators for supported nested struct fields and scalar array fields inside struct initializers; acceptance: `.inner.x = 1` and `.values[1] = 2` initialize nested fields/elements in source order, repeated nested path designators merge sibling values instead of replacing them, invalid nested fields and array-field indices report targeted diagnostics, and valid/invalid/compiler-oracle fixtures cover the supported subset.
41. [x] Add a scoped first-pass `union` aggregate model with an implementation-ready design first; acceptance: document deterministic storage/layout semantics, support named scalar-field union declarations and variables, single first-field brace initializers, scalar member reads/writes over shared logical storage, deterministic `sizeof`, targeted excess-initializer diagnostics, and valid/invalid/compiler-oracle fixtures without native ABI padding assumptions.
42. [x] Expand scalar union support through aggregate paths; acceptance: nested union fields inside supported structs and one-dimensional union arrays preserve shared scalar-field semantics for brace initialization and field writes, by-value union copies/parameters remain isolated, deterministic nested `sizeof` works, and valid/compiler-oracle fixtures cover the supported subset.
43. [x] Add one-level union pointers and pointer fields inside unions as a scoped follow-up to the first-pass union model; acceptance: `union Number *p = &n`, `p->value`, `(*p).tag`, union-array element pointers, pointer parameters, self-referential union pointer fields, and scalar pointer fields inside unions work through Cust's interpreter-owned pointer metadata; scalar writes through union pointers synchronize sibling scalar fields; pointer-to-pointer union fields are rejected; and valid/invalid/compiler-oracle fixtures cover the supported subset.
44. [x] Add direct union-returning functions and prototypes; acceptance: `union Number make_number(int value);` and `union Number make_number(...) { ... }` parse as aggregate-returning signatures, returning a local union clones by value, side-effect-only union copy assignment from a return call works without scalar evaluation, by-value union parameters remain isolated, and valid/compiler-oracle fixtures cover the supported subset.
45. [x] Add aggregate initializer expressions from returned structs/unions; acceptance: `struct Point p = make_point(...);` and `union Number n = make_number(...);` initialize same-type aggregate variables directly from aggregate-returning calls, preserve by-value copies and `const struct` read-only bindings after initialization, reject mismatched aggregate result types, and include valid/invalid/compiler-oracle fixtures.
46. [x] Add aggregate-valued conditional and comma expression support; acceptance: `struct Point p = flag ? a : b;`, `target = cond ? left : right;`, and `(side_effect(), aggregate_value)` work for supported structs/unions in aggregate initializer, copy-assignment, parameter/return contexts, preserve branch short-circuiting and by-value copies, reject mismatched selected aggregate types, and include valid/invalid/compiler-oracle fixtures.
47. [x] Add direct aggregate type spellings in `sizeof(...)`; acceptance: `sizeof(struct Name)`, `sizeof(union Name)`, `sizeof(const union Name)`, `sizeof(struct Name *)`, and `sizeof(union Name *)` parse as type operands with deterministic Cust sizes, and valid/compiler-oracle fixtures cover the supported subset without relying on native struct padding.
48. [x] Add scoped aggregate-array element pointer arithmetic as a substantial pointer/aggregate follow-up; acceptance: `struct Point *p = &points[0]` and `union Number *n = &numbers[0]` support bounded `p + n`, `p - n`, `p += n`, `p -= n`, pointer-variable `++`/`--`, and same-array pointer difference over struct/union array element pointers; standalone struct/union pointers and one-past results remain rejected; valid/invalid/compiler-oracle fixtures cover the supported subset.
49. [x] Add C-style indexed field access for struct/union array pointers as a scoped follow-up to aggregate pointer arithmetic; acceptance: `struct Point *p = &points[0]; p[i].x`, `p[i].x += 1`, `++p[i].y`, `union Number *n = &numbers[0]; n[i].value` work over interpreter-owned aggregate-array element pointers, writes through `const struct Point *` views report `cannot assign through pointer to const`, and valid/invalid/compiler-oracle fixtures cover the supported subset.
50. [x] Add aggregate pointer indexed elements as by-value expressions; acceptance: `struct Point copy = p[i];`, `copy = p[i];`, `sum(p[i])`, `union Number picked = n[i];`, and `take(n[i])` deep-clone the selected struct/union array element from an aggregate pointer for declaration initializers, copy assignment, and by-value function arguments, preserving caller isolation and compiler-oracle coverage.
51. [x] Add designated initializers for aggregate arrays as a targeted aggregate initializer follow-up; acceptance: `struct Point points[3] = {[2] = {.x = 5}, [0] = {1, 2}};` and `union Number numbers[3] = {[1] = {.tag = 7}};` initialize selected aggregate-array elements, preserve omitted-element zero/default initialization and mixed positional continuation, reject out-of-bounds aggregate-array designators, and include valid/invalid/compiler-oracle fixtures.
52. [x] Add aggregate array decay to existing aggregate pointer parameters; acceptance: `struct Point points[2]; use(points);` and `union Number numbers[2]; use(numbers);` decay array variables to interpreter-owned pointers to element zero for `struct Point *` / `union Number *` parameters, preserve mutation through the callee, allow mutable-to-const pointer parameter flow, reject const aggregate-array decay into mutable pointer parameters, and include valid/invalid/compiler-oracle fixtures.
53. [x] Add direct pointer pointee type-mismatch diagnostics for scalar and aggregate pointer conversions; acceptance: declarations, assignments/assignment expressions, direct struct pointer-field assignment, pointer fields, and function arguments validate the runtime pointer target shape against declared `int *`, `char *`, `struct T *`, or `union T *` pointee metadata, preserve null compatibility and const-discard checks, and report targeted diagnostics such as `cannot convert pointer to char to pointer to int` / `cannot convert pointer to union 'Number' to pointer to struct 'Point'`.
54. [x] Add pointer-returning functions and prototypes as the next scoped pointer/function gap; acceptance: `int *`, `char *`, `const int *`, `struct T *`, `union T *`, and pointer typedef return types parse in definitions/prototypes, return interpreter-owned pointer metadata by value, preserve const and pointee-type conversion checks at the return boundary, reject pointer-to-pointer return types, and include valid/invalid/compiler-oracle fixtures.
55. [x] Add top-level aggregate typedef definitions; acceptance: `typedef struct Point { int x; int y; } Point;` and `typedef union Number { int value; char tag; } Number;` register the named aggregate and parser-only alias in one declaration, aliases feed existing aggregate declarations/arrays/pointers/functions/designated initializers, block-scoped aggregate typedef definitions are rejected until aggregate tag scopes are designed, and valid/invalid/compiler-oracle fixtures cover the supported subset.
56. [x] Add block-scoped aggregate typedef definitions; acceptance: `int main(void) { typedef struct Local { int value; } Local; Local v = {1}; }` and nested block-local union typedef definitions parse, aliases/tags are visible only until block exit, runtime keeps parsed type metadata available for executing block statements, out-of-scope aggregate typedef names report an invalid regression, repeated aggregate tag names remain rejected until unique internal type identities are designed, and valid/invalid/compiler-oracle fixtures cover the supported subset.
57. [x] Add C-style unsized array parameter spellings as pointer parameters; acceptance: `int values[]`, `char text[]`, `struct Point points[]`, and `union Number numbers[]` parse in definitions/prototypes, reuse existing array/string/aggregate-array decay and pointer binding, preserve callee mutation and const-discard diagnostics, and include valid/invalid/compiler-oracle fixtures.
58. [x] Design true aggregate tag shadowing with unique internal type identities before implementation; acceptance: block-local `typedef struct Point { ... } Point;` can shadow an outer `struct Point` without leaking aliases/tags or colliding in runtime metadata, diagnostics still display user-facing tag names, and valid/invalid/compiler-oracle fixtures cover nested shadowing and out-of-scope behavior without native ABI padding assumptions.
59. [x] Add anonymous aggregate typedef definitions as a scoped C-subset type-system follow-up; acceptance: `typedef struct { int x; int y; } Point;` and `typedef union { int value; char tag; } Number;` create unique internal aggregate type identities without source-level tags, install parser-only aliases, support alias-spelled declarations/arrays/pointers/functions/returns/`sizeof`, keep diagnostics user-facing through the alias display name, and include valid plus compiler-oracle fixtures.
60. [x] Refresh README/docs v0.1 limitations and roadmap to match the now-implemented structs/unions/typedefs/license surface; acceptance: remove stale limitations such as missing structs/license, keep interpreter/native-oracle boundaries clear, and run repository docs/license tests.
61. [x] Add anonymous enum typedef declarations as a scoped typedef/data-type follow-up; acceptance: `typedef enum { READY = 1, BUSY } Status;` creates a parser-only integer alias, exposes scoped enum constants at runtime, works in globals/locals/arrays/function parameters/returns/`sizeof(alias)`, supports block-local alias/constant shadowing, and includes valid/compiler-oracle fixtures without ABI-sensitive enum-size assertions.
62. [x] Add C-style unnamed parameter declarations in function prototypes; acceptance: semicolon-terminated prototypes such as `int add(int, int);`, `char first(char *);`, `void mutate(int [], struct Point *);`, and `union Number choose(union Number [], int);` parse as signatures without parameter names, remain compatible with named definitions, preserve exact missing-parameter-name diagnostics for function definitions, and include valid plus compiler-oracle fixtures.
63. [x] Add scalar cast expressions as a scoped expression feature; acceptance: `(int)expr`, `(char)expr`, and scalar typedef casts such as `(Count)expr` parse at unary precedence, preserve operand side effects, report targeted unsupported pointer/aggregate cast diagnostics, and include valid/invalid/compiler-oracle fixtures.
64. [x] Add aggregate pointer dereference values and copy assignment as the next scoped aggregate/pointer expression gap; acceptance: `struct Point copy = *p;`, `union Number picked = *n;`, by-value function arguments such as `sum(*p)`, aggregate returns such as `return *p;`, and aggregate pointer copy assignment `*p = replacement;` deep-copy supported struct/union targets through interpreter-owned pointers, preserve null/out-of-scope/const-pointee diagnostics, and include valid/invalid/compiler-oracle fixtures.
65. [x] Add aggregate assignment expressions returning aggregate values; acceptance: `struct Point copy = (target = source);` and `union Number picked = (*slot = replacement);` mutate same-type aggregate destinations, return by-value copies usable in aggregate declaration/assignment/argument/return contexts, preserve const/mismatch diagnostics, and include valid/invalid/compiler-oracle fixtures.
66. [x] Add type-aware `sizeof` for dereferenced pointer-valued expressions; acceptance: `sizeof(*(char_ptr + 1))`, `sizeof(*pointer_returning_call(...))`, `sizeof(*(&points[0].x))`, `sizeof(*(struct_ptr + 1))`, and `sizeof(*(cond ? left : right))` infer the pointed-to scalar/aggregate type without evaluating side-effectful operands, include valid/compiler-oracle fixtures, and avoid native ABI-sensitive aggregate-size assertions.
67. [x] Add top-level const-qualified function return types; acceptance: `const int f(void)`, `const char f(void)`, `const Count f(void)`, `const struct Point make(void)`, `const Point make(void)`, `const union Number make(void)`, and `const Number make(void)` parse as ordinary by-value return signatures, preserving existing pointer-return `const T *` semantics and prototype compatibility, with interpreter fixture coverage and a design note explaining why the native `-Werror` compiler-oracle path is intentionally skipped for ignored top-level return qualifiers.
68. [x] Add direct named-enum type spellings as a scoped C-subset feature; acceptance: `enum State global = READY;`, local `enum State value`, `const enum State value`, `enum State` parameters/prototypes/return types, and `sizeof(enum State)` parse and behave as Cust integer storage while enum constants remain scoped/read-only, with valid interpreter and compiler-oracle fixtures.
69. [x] Add direct struct pointer-field arithmetic and stricter type diagnostics as a scoped pointer-field follow-up; acceptance: pointer-valued fields such as `cursor.p` support array-backed pointer arithmetic expressions, `+=`/`-=`, and prefix/postfix `++`/`--`; direct pointer-field assignment validates concrete pointee type compatibility at the assignment boundary; invalid mismatches report targeted `cannot convert pointer to ...` diagnostics; and interpreter plus C compiler-oracle fixtures cover the supported subset.
70. [x] Close struct-pointer `->` pointer-field arithmetic parity with direct `.` pointer-field support; acceptance: pointer-valued fields reached through a struct pointer (`slot->p`) support array-backed pointer reads/dereference, `+=`/`-=`, prefix/postfix `++`/`--`, and reassignment from pointer arithmetic (`slot->p = slot->p - 2`) while preserving interpreter-owned pointer safety, const-conversion checks, and native C compiler-oracle coverage.
71. [x] Add braced scalar initializer expressions in scalar initializer contexts; acceptance: `int x = {expr};`, scalar array initializer entries/designators, scalar struct/union fields, and scalar path designators accept one contained expression with side effects preserved, trailing commas work, interpreter fixture coverage verifies Cust behavior, and compiler-oracle omission is documented because the repository's `-Werror` flags reject `-Wbraces` warnings for these forms.
72. [x] Add fixed-size scalar array parameter decay parity with unsized array parameters; acceptance: `int values[3]` and `char text[2]` parse as pointer parameters, still syntax-check bracket lengths, do not enforce the written bound at call time, allow larger arrays and string literals to decay through existing pointer binding, report pointer size for `sizeof(values)`, and include valid plus C compiler-oracle fixtures.
73. [x] Add C99-style aggregate compound literals as the next substantial C-subset feature; acceptance: `(struct Point){1, 2}`, `(Point){.x = 1, .y = 2}`, and `(union Number){.tag = 9}` parse as rvalue aggregate expressions, reuse existing aggregate initializer semantics, work in declaration initializers, assignment expressions, by-value arguments, aggregate returns, conditional/comma aggregate contexts, and scalar field reads such as `((Number){12}).tag`, with scoped design docs and C compiler-oracle coverage.
74. [x] Add C99-style scalar compound literals as the next expression feature; acceptance: `(int){expr}`, `(char){expr}`, and scalar typedef aliases such as `(Count){expr}` parse as rvalue scalar expressions, accept optional trailing commas while rejecting excess initializer entries, preserve initializer side effects in evaluated contexts, remain non-evaluating under `sizeof`, and include valid/invalid/compiler-oracle fixtures.
75. [x] Add C99-style scalar-array compound literals as the next compound-literal/pointer feature; acceptance: `(int[]){1, 2}`, `(char[]){'a', 0}`, and fixed-size `(int[3]){...}` parse as pointer-valued rvalue expressions with mutable interpreter-owned array storage, support positional and designated initializers with inferred unsized lengths or checked fixed lengths, initialize pointer variables and pointer/array parameters through existing decay/binding paths, reject excess fixed-size initializers, and include valid/invalid/compiler-oracle fixtures.
76. [x] Add C99-style aggregate-array compound literals as the next compound-literal/pointer feature; acceptance: `(struct Point[]){{1, 2}, {.x = 3}}`, `(union Number[]){{7}, [2] = {.value = 9}}`, and fixed-size `(struct Point[3]){...}` parse as pointer-valued rvalue expressions backed by mutable interpreter-owned aggregate-array storage in the current scope, support positional and designated aggregate element initializers with inferred or checked lengths, initialize aggregate pointer variables and flow directly into aggregate pointer/array parameters, reject excess fixed-size initializers, and include valid/invalid/compiler-oracle fixtures.
77. [x] Add addressable scalar and aggregate compound literals as a scoped compound-literal follow-up; acceptance: `&(int){7}`, `&(char){'a'}`, and `&(struct Point){.x = 1}` allocate hidden mutable storage in the current Cust scope, return safe interpreter-owned pointers, support dereference writes, pointer-parameter binding, aggregate `->` field access, concrete pointee type checks, valid/compiler-oracle fixtures, and design notes documenting lifetime/storage.
78. [x] Add C integer literal base spellings as a lexer/conformance feature; acceptance: `052`, `0x2a`, and `0X10` parse as integer constants usable in expressions/initializers, invalid octal digits such as `08` report a targeted lexer source/caret diagnostic, existing out-of-range integer diagnostics continue to apply to base-prefixed literals, and valid interpreter plus C compiler-oracle fixtures cover the supported subset.
79. [x] Add C integer literal suffix spellings as a lexer/conformance closure item; acceptance: decimal, octal, and hexadecimal constants accept `u`/`U`, `l`/`L`, `ll`/`LL`, and unsigned-long combinations such as `UL`, `lu`, `uL`, and `LLU`, preserving Cust's deterministic integer storage model, with valid interpreter and C compiler-oracle fixtures.
80. [x] Add standard C simple escape sequences as a lexer/conformance feature; acceptance: `\\a`, `\\b`, `\\f`, `\\r`, `\\v`, and `\\?` work in both character literals and string literals alongside the existing supported escapes, map to the same scalar byte values as native C, keep unsupported escape diagnostics for other sequences, and include valid interpreter plus C compiler-oracle fixtures.
80. [x] Add C numeric escape sequences as a lexer/conformance feature; acceptance: octal escapes such as `\\101` consume up to three octal digits in character/string literals, hex escapes such as `\\x2a` consume one or more hex digits, missing hex digits report exact source/caret diagnostics, and interpreter plus C compiler-oracle fixtures cover supported behavior.
81. [x] Add fixed-size `char` array initialization from string literals as the next conformance feature; acceptance: `char word[4] = "cat";`, `char exact[3] = "dog";`, shorter strings with zero-filled trailing elements, const/static char arrays, and too-long initializer diagnostics are covered by interpreter fixtures, with C compiler-oracle coverage for the warning-free supported subset.
82. [x] Add fixed-size `char` array string-literal initializer support inside supported aggregate fields; acceptance: `struct Label { char text[5]; }; struct Label l = {"cat"};`, `.text = "hi"`, nested path designators, and struct-array element initializers work with zero-fill and too-long diagnostics, plus valid/invalid/compiler-oracle fixtures.
83. [x] Add scalar array-field decay and array-element address-of parity for supported struct variables and struct-array elements; acceptance: `packet.values` and `packets[i].values` bind to existing `int *`/`char *` pointer and array parameter paths, `&packet.values[j]` and `&packets[i].values[j]` create interpreter-owned array-element pointers that alias embedded field storage, const/read-only pointer diagnostics remain intact, and valid plus C compiler-oracle fixtures cover the subset.
84. [x] Add the next substantial C-subset feature only with an implementation-ready design first; recommended candidates include arrow (`->`) array-field decay/indexing parity for struct pointers, richer parser diagnostics from newly discovered malformed programs, or carefully scoped standard-library-like builtin experiments only after a roadmap/design note. Acceptance: write valid/invalid/compiler-oracle fixtures first where applicable, preserve const and pointer safety, and avoid native ABI padding assumptions.
85. [x] Add arrow (`->`) scalar array-field decay/index/address-of parity for struct pointers as the follow-up to direct struct array-field decay; acceptance: `slot->values` and `slot->label` bind to `int *`/`char *` pointer or array parameters, `slot->values[i]` reads/writes embedded array elements, `&slot->values[i]` returns aliasing interpreter-owned array-element pointers, `const struct T *slot` preserves const-discard diagnostics for mutable pointer flows, and valid/invalid/compiler-oracle fixtures cover the subset.
86. [x] Continue substantial C-subset closure with implementation-ready design first; completed nested scalar array-field decay/address-of parity through nested struct fields with const root-struct and const struct-array pointer-conversion diagnostics. Acceptance covered by `one.inner.values`, `boxes[i].inner.values`, `ptr->inner.values`, matching `&...values[j]` forms, invalid const-discard fixtures, and C compiler-oracle coverage.
87. [x] Add address-of dereference pointer identity as a small but concrete pointer-conformance closure item; acceptance: `&*p`, `&*(values + 1)`, and `&*null_ptr` preserve the original pointer value without dereferencing the target, work in pointer declarations and pointer-parameter calls, and include valid plus C compiler-oracle fixtures for non-null native-compatible cases.
88. [x] Continue substantial C-subset closure with implementation-ready design first; completed aggregate-array fields inside supported structs. Acceptance covered by `struct Line { struct Point points[2]; };`, recursive brace/designated initialization, scalar field reads, scalar field assignment/compound assignment through `line.points[i].field`, by-value struct parameter deep-copy isolation, and C compiler-oracle coverage.
89. [x] Add embedded aggregate-array field pointer decay/address-of parity as a scoped aggregate pointer follow-up; acceptance covered by `line.points` aggregate-pointer decay into `struct Point *` parameters/variables, `&line.points[i]`, `line.points + n`, callee-side `points[i].field` mutation aliasing the containing struct, const-discard diagnostics for const containing structs, and C compiler-oracle coverage.
90. [x] Continue substantial C-subset closure with implementation-ready design first; completed struct-pointer embedded aggregate-array field decay/address-of parity. Acceptance covered by `slot->points`, `&slot->points[i]`, `slot->points + n`, `slot->points[i].field`, const struct-pointer conversion diagnostics, and C compiler-oracle coverage.
91. [x] Continue substantial C-subset closure with implementation-ready design first; completed char-array string-literal initializers in scalar-array compound literals plus nested embedded aggregate-array field pointer-decay fixture coverage. Acceptance covered by `(char[]){"cat"}`, fixed-size too-long diagnostics, `box->inner.points`, `&box->inner.points[i]`, pointer arithmetic, const-discard diagnostics, and C compiler-oracle coverage.
92. [x] Continue substantial C-subset closure with implementation-ready design first; completed `sizeof` array compound literals plus embedded union-array field pointer parity. Acceptance covered by `sizeof((char[]){...})`, fixed-size scalar and aggregate array compound literal sizes without initializer side effects, direct `bag.numbers` / `&bag.numbers[i]`, struct-pointer `bag->numbers` / `&bag->numbers[i]`, union-array field pointer arithmetic, const-discard diagnostics, and C compiler-oracle coverage.
93. [x] Add scalar compound literals as modifiable scalar lvalues; acceptance covered by `((int){1} = 5)`, `((int){3} += 4)`, `++(int){8}`, and `(int){9}++`, with parser postfix routing for cast/compound-literal expressions, initializer side effects preserved before mutation, and interpreter plus C compiler-oracle fixtures.
94. [x] Add aggregate compound literal scalar-field lvalues; acceptance covered by `((struct Point){1, 2}).x = 7`, `((struct Point){3, 4}).y += 5`, `++((struct Point){5, 6}).x`, `((struct Point){7, 8}).y++`, nested scalar fields, initializer side effects before mutation, const-field rejection, and interpreter plus C compiler-oracle fixtures.
95. [x] Add pointer-valued field reads on aggregate compound literals; acceptance covered by `((struct Cursor){values + 1}).p` flowing through pointer declarations, calls, dereference, pointer arithmetic, and pointer equality/truthiness contexts with pointee metadata preserved for type/const conversion checks and native compiler-oracle coverage.
96. [x] Add array-valued field decay on aggregate compound literals; acceptance covered by `((struct Line){{{1, 2}, {3, 4}}}).points` flowing into aggregate pointer declarations and parameters, aggregate pointer arithmetic plus `->` field reads, and const-discard diagnostics for const pointer fields and const scalar/aggregate array fields on aggregate compound literals with native compiler-oracle coverage.
97. [x] Continue substantial C-subset closure with implementation-ready design first; completed direct indexing and element address-of for scalar/aggregate array fields on aggregate compound literals. Acceptance covered by `((struct Packet){{1, 2, 3}}).values[1]`, `&((struct Packet){{4, 5, 6}}).values[2]`, `((struct Line){{{1, 2}, {3, 4}}}).points[1].y`, `&((struct Line){{{5, 6}, {7, 8}}}).points[0]`, const-discard diagnostics for const array-field elements, and C compiler-oracle coverage.
98. [x] Continue substantial C-subset closure with implementation-ready design first; completed direct lvalue write fixture coverage for scalar and aggregate array fields on aggregate compound literals plus parser support for `.` field access on aggregate-valued expression results. Acceptance covered by `((struct Packet){{1, 2, 3}}).values[0] = 7`, compound assignment/increment on scalar array-field elements, aggregate-array element scalar-field writes, `(((struct Line){{...}}).points[0] = replacement).x`, `(left = right).x`, `(cond ? left : replacement).y`, `(side_effect(), right).x`, and `make_point(5).y`, with interpreter plus C compiler-oracle fixtures.
99. [x] Continue substantial C-subset closure with implementation-ready design first; completed field access on union-valued expression results plus union-specific scalar-use diagnostics. Acceptance covered by `((union Number){4}).value`, `(left = right).value`, `(cond ? left : replacement).value`, `(side_effect(), replacement).value`, `make_number(5).value`, `((*(&left) = make_number(6))).value`, invalid `return make_number(1);` reporting `union function 'make_number' used as scalar expression`, and C compiler-oracle coverage.
100. [x] Continue substantial C-subset closure with implementation-ready design first; completed `sizeof` field queries on aggregate-valued expressions in non-evaluating context. Acceptance covered by `sizeof((left = right).x)`, `sizeof((cond ? left : right).tag)`, `sizeof((marker = marker + 10, right).x)`, `sizeof(make_point(7).tag)`, union assignment/conditional/call variants, explicit marker/state checks proving `sizeof` did not evaluate operand side effects, and C compiler-oracle coverage using ABI-independent `sizeof(field) == sizeof(type)` checks.
101. [x] Continue substantial C-subset closure with implementation-ready design first; completed `sizeof` full array-object sizing for scalar and embedded aggregate-array fields selected from aggregate-valued expression results. Acceptance covered by `sizeof(make_line(10).points)`, `sizeof((line = make_line(20)).numbers)`, `sizeof((1 ? line : make_line(30)).values)`, and `sizeof(make_box(40).line.points)` without evaluating calls, assignments, or unselected branches, plus ABI-independent C compiler-oracle ratio checks.
102. [x] Continue substantial C-subset closure with implementation-ready design first; completed const pointer-returning call conversion coverage. Acceptance covered by `const int *view(...)` flowing directly into a mutable `int *` declaration and reporting `cannot discard const qualifier from pointer target`; implementation now carries declared pointer-return pointee-const metadata through pointer conversion checks for calls.
103. [x] Add C reverse-subscript expression parity for the supported pointer/array subset; acceptance covered by `0[p]`, `2[values] = 9`, `1["hi"]`, `2[text]`, `1[(int[]){7, 8, 9}]`, and `1[points].y`, using the existing safe pointer arithmetic/dereference machinery with interpreter plus C compiler-oracle fixtures.
104. [x] Add address-of string-literal element parity; acceptance covered by direct `&"cast"[2]`, reverse `&1["dog"]`, grouped `&("hi")[1]`, relative indexing from the produced pointer, a read-only write diagnostic for `char *middle = &"cat"[1]; middle[0] = 'u';`, and C compiler-oracle coverage.
105. [x] Add adjacent string literal concatenation as a C lexical/expression conformance closure item; acceptance covered by `"he" "llo"` in char-array initializers, string pointer expressions with arithmetic, `sizeof("xy" "z")`, string-literal array-parameter calls, and char-array compound literal string initializers, with interpreter plus C compiler-oracle coverage.
106. [x] Continue substantial C-subset closure with implementation-ready design first; completed same-array pointer ordering comparisons for supported array-backed pointers. Acceptance covered by scalar-array pointers, string literal pointers, aggregate-array pointers, different-array diagnostics, preservation of the existing scalar-pointer ordering unsupported diagnostic, and C compiler-oracle coverage.
107. [x] Close embedded aggregate-array field pointer-ordering parity as a follow-up to same-array pointer ordering. Acceptance covered by direct and nested field paths such as `line.points < &line.points[2]`, `line.points + 1 <= &line.points[2]`, `box.line.points`, different embedded aggregate-array field diagnostics, and C compiler-oracle coverage.
108. [x] Continue substantial C-subset closure with implementation-ready design first; completed embedded aggregate-array field pointer equality parity. Acceptance covered by direct and nested field paths such as `line.points == &line.points[0]`, `(line.points + 2) == &line.points[2]`, `box.line.points == &box.line.points[0]`, and unequal embedded field element comparisons, with C compiler-oracle coverage.
109. [x] Continue substantial C-subset closure with implementation-ready design first; completed aggregate compound-literal pointer-field lvalue parity. Acceptance covered by pointer-valued fields selected from aggregate compound literals in assignment, pointer compound assignment, prefix decrement, and postfix increment pointer contexts, plus const pointer-field assignment rejection and C compiler-oracle coverage.
110. [x] Continue substantial C-subset closure with implementation-ready design first; completed parenthesized pointer declarator diagnostics for unsupported C forms such as `int (*row)[3];` and `int f(int (*row)[3])`, replacing generic missing-name errors with exact `parenthesized pointer declarations/parameters are not supported` diagnostics and focused invalid fixtures.
111. [x] Close aggregate compound-literal scalar-field addressability parity; acceptance covered by `&((struct Point){4, 8}).x`, nested field paths such as `&((struct Box){{2, 3}, 4}).inner.y`, union scalar fields such as `&((union Number){7}).value`, mutation through the resulting pointer, preserved hidden current-scope storage, and C compiler-oracle coverage.
112. [x] Close aggregate compound-literal aggregate-field addressability parity; acceptance covered by `&((struct Box){{5, 7}, 9}).inner` producing a safe `struct Point *` to hidden compound-literal storage, `->` field reads/writes, helper-function mutation through the pointer, and C compiler-oracle coverage.
113. [x] Close struct-pointer scalar field addressability parity; acceptance covered by `&point_ptr->x`, `&box_ptr->inner.y`, `struct Point *inner = &box_ptr->inner; &inner->x`, helper mutation through the resulting scalar pointers, aliasing back into original struct storage, and C compiler-oracle coverage.
114. [x] Continue substantial C-subset closure with implementation-ready design first; completed address-of scalar fields through pointers into embedded aggregate-array fields. Acceptance covered by `struct Point *p = line.points + 1; int *x = &p->x;`, nested containing paths such as `box.line.points + 2`, helper mutation through the resulting scalar pointers, aliasing back into original containing struct storage, and C compiler-oracle coverage.
115. [x] Continue substantial C-subset closure with implementation-ready design first; completed address-of aggregate-valued fields through pointers into embedded aggregate-array fields. Acceptance covered by `struct Segment *second = drawing.segments + 1; struct Point *start = &second->start;`, `start->x`/`start->y` reads and writes through the resulting struct pointer, helper mutation through the aggregate-field pointer, aliasing back into original containing struct storage, and C compiler-oracle coverage.
116. [x] Continue substantial C-subset closure with implementation-ready design first; completed pointer equality for scalar fields reached through pointers into embedded aggregate-array fields. Acceptance covered by independently computed `&mid->x` pointers targeting the same embedded aggregate-array element field, inequality against sibling fields/different elements, dereference mutation aliasing, and C compiler-oracle coverage.
117. [x] Continue substantial C-subset closure with implementation-ready design first; completed direct address-of for scalar field paths after embedded aggregate-array indexing expressions. Acceptance covered by `&line.points[1].x` and nested `&box.line.points[2].y`, helper-call mutation through the resulting scalar pointers, aliasing back into original embedded aggregate-array element fields, and C compiler-oracle coverage.
118. [x] Continue substantial C-subset closure with implementation-ready design first; completed direct aggregate-field address-of after struct-array and embedded aggregate-array indexing expressions. Acceptance covered by `&segments[1].start`, `&drawing.segments[1].start`, and nested `&box.drawing.segments[0].end`, helper-call mutation through the resulting struct pointers, aliasing back into original array element fields, and C compiler-oracle coverage.
119. [x] Add one-dimensional array type-name operands in `sizeof(...)` as a small C type-query conformance closure item; acceptance covered by scalar, const scalar, direct aggregate, and aggregate typedef array type operands such as `sizeof(int[3])`, `sizeof(const int[2])`, `sizeof(struct Pair[2])`, and `sizeof(Number[3])`, with deterministic Cust size results and C compiler-oracle relationship checks.
120. [x] Add signed/unsigned integer type spellings as a small C declaration/type-operand conformance closure item; acceptance covered by `signed`, `signed int`, `unsigned`, and `unsigned int` in globals, locals, static locals, `for` declaration initializers, function returns, parameters/prototypes, pointer declarations/parameters, typedef aliases, scalar casts, and `sizeof` type operands including `sizeof(const unsigned int[2])`, preserving Cust's deterministic integer storage model with interpreter plus C compiler-oracle coverage.
121. [x] Add signed/unsigned char type spellings as the next scalar declaration/type-operand conformance closure item; acceptance covered by `signed char` and `unsigned char` in globals, locals, static locals, `for` declaration initializers, function returns, prototypes, parameters, pointer declarations/parameters, typedef aliases, scalar casts, and `sizeof` scalar/pointer/array type operands, preserving Cust's deterministic `char` storage model with interpreter plus C compiler-oracle coverage.
122. [x] Continue substantial C-subset closure with implementation-ready design first; completed deterministic `long`/`short` type-spelling syntax parity. Acceptance covered by `long`, `long int`, `short`, `short int`, `signed long int`, and `unsigned short int` in globals, locals, static locals, `for` declarations, function returns/prototypes/parameters, pointer declarations/parameters, typedef aliases, scalar casts, and `sizeof` scalar/pointer/array type operands, preserving Cust's deterministic integer storage with interpreter plus C compiler-oracle coverage that avoids native ABI-sensitive size checks.
123. [x] Continue substantial C-subset closure with implementation-ready design first; completed deterministic `long long` type-spelling syntax parity. Acceptance covered by `long long`, `long long int`, `signed long long`, and `unsigned long long int` in globals, locals, static locals, `for` declarations, function returns/prototypes/parameters, pointer declarations/parameters, typedef aliases, scalar casts, and `sizeof` scalar/pointer/array type operands, preserving Cust's deterministic integer storage with interpreter plus C compiler-oracle coverage that avoids native ABI-sensitive size checks.
124. [x] Add C99 `_Bool` type spelling as a scalar syntax parity closure item; acceptance covered by `_Bool` globals, locals, static locals, `for` declarations, function returns/prototypes/parameters, pointer declarations/parameters, typedef aliases, scalar casts, and `sizeof` scalar/pointer/array type operands, with deterministic Cust size 1 and interpreter plus C compiler-oracle fixtures.
125. [x] Add local `auto`/`register` storage-class specifiers as a small C declaration syntax parity closure item; acceptance covered by block-scope scalar, pointer, aggregate, and `for` initializer declarations using `auto` or `register`, lowered to Cust's existing automatic local storage behavior, with interpreter plus C compiler-oracle fixtures that avoid native `register` address-taking restrictions.
126. [x] Continue substantial C-subset closure with implementation-ready design first; completed one-dimensional array typedef aliases. Acceptance covered by `typedef int Scores[3];`, `typedef char Word[4];`, `typedef Point Points[2];`, scalar/char/aggregate array alias declarations, const arrays, char-array string initializers, function parameter decay, `sizeof(alias)` full array-object queries, pointer-array typedef diagnostics, and C compiler-oracle coverage.
127. [x] Add top-level `extern` function storage-class syntax parity as a small C declaration conformance item; acceptance covered by `extern int helper(int);`, `extern void seed(int values[]);`, and `extern int helper(int value) { ... }` parsing through existing function/prototype paths, preserving signature compatibility checks, and interpreter plus C compiler-oracle fixtures without warning-prone initialized `extern` globals.
128. [x] Add uninitialized top-level `extern` global declarations as the object-declaration counterpart to extern function syntax; acceptance covered by scalar, char, scalar-array, struct, struct-array, union, enum, and one-level pointer declarations such as `extern int total;`, `extern struct Point origin;`, `extern union Number number;`, and `extern int *cursor;` being parser-only declarations whose later ordinary definitions provide runtime storage without duplicate-global diagnostics, with interpreter plus C compiler-oracle fixtures.
129. [x] Add C `volatile` type qualifier syntax parity as a small declaration/type-query conformance closure item; acceptance covered by `volatile int`, `const volatile char`, `volatile struct Point`, `volatile int *`, `int * volatile`-style post-star qualifier parsing via `volatile int * volatile`, volatile typedef aliases, function parameters, casts, and `sizeof(volatile ...)` type operands. Runtime behavior intentionally treats `volatile` as a no-op while preserving existing `const` enforcement, with interpreter plus C compiler-oracle coverage that avoids native ignored-return-qualifier and discarded-qualifier warnings.
130. [x] Add C99 `restrict` pointer qualifier syntax parity as a small declaration conformance closure item; acceptance covered by `int * restrict p`, `int * const restrict p`, `int * volatile restrict p`, function pointer parameters, and pointer fields inside structs, all treated as parser-level no-op syntax over Cust's existing interpreter-owned pointer model while preserving `const` metadata and C compiler-oracle coverage.
131. [x] Add top-level C function specifier syntax parity as a small function-declaration conformance item; acceptance covered by `static inline` function prototypes/definitions, `inline` around top-level storage-class specifiers, and `_Noreturn` function prototypes, all treated as parser-level no-op metadata over Cust's existing function table/signature model with C compiler-oracle coverage.
132. [x] Add const-qualified non-pointer typedef alias metadata as a const-correctness closure item; acceptance covered by `typedef const int ConstInt;`, `typedef const char ConstChar;`, `typedef const struct Point ConstPoint;`, and `typedef const int ConstScores[3];` preserving read-only scalar, aggregate, parameter, and array behavior after initialization, with assignment rejection and C compiler-oracle coverage.
133. [x] Add const-qualified one-level pointer typedef alias metadata as a pointer const-correctness closure item; acceptance covered by `typedef const int *ConstIntView;`, `typedef const struct Point *ConstPointView;`, `typedef int * const ConstIntSlot;`, and `typedef struct Point * const PointSlot;` preserving pointee-const and pointer-slot-const behavior separately for declarations, parameters, returns, and fields, with const-discard and const-slot assignment diagnostics plus C compiler-oracle coverage.
134. [x] Continue substantial C-subset closure with implementation-ready design first; completed postfix/interleaved const qualifier syntax parity. Acceptance covered by `int const`, `unsigned const int`, `long const int`, `int const *`, `struct Point const`, `typedef int const`, `typedef struct Point const`, const-qualified parameters, and interleaved-qualified function definition lookahead, preserving existing const metadata and C compiler-oracle coverage where native `-Werror=ignored-qualifiers` allows it.
135. [x] Add C11 `_Static_assert` syntax parity as a small conformance closure item; acceptance covered by top-level and block-scope true assertions, `sizeof`/array type condition expressions, false assertion diagnostic `static assertion failed: <message>`, interpreter invalid coverage, and C compiler-oracle coverage for the warning-free true subset.
136. [x] Add C11 `_Alignof(type-name)` as a small type-query conformance closure item; acceptance covered by scalar/qualified alias, one-level pointer, one-dimensional scalar/aggregate array, direct aggregate, and aggregate typedef type names, deterministic Cust alignment values, `_Alignof(void)` diagnostics, and ABI-independent C compiler-oracle relationships.
137. [x] Continue substantial C-subset closure with implementation-ready design first; completed C11 `_Alignas` declaration specifier syntax parity. Acceptance covered by `_Alignas(8) int global`, `_Alignas(int)` aggregate fields, block locals, `static _Alignas(8)` locals, and `for (_Alignas(8) int i = 0; ...)` parsing as no-op declaration metadata over Cust's deterministic storage model with interpreter plus C compiler-oracle fixtures.
138. [x] Add C11 `_Thread_local` storage-class syntax parity as a small declaration conformance item; acceptance covered by `_Thread_local int global`, `static _Thread_local int hidden`, and `static _Thread_local int local` parsing as parser-level no-op metadata over Cust's deterministic single-thread storage model, with interpreter plus C compiler-oracle fixtures.
139. [x] Add C11 `_Atomic` type qualifier/specifier syntax parity as a small declaration/type-query conformance item; acceptance covered by bare `_Atomic int value`, `_Atomic(type-name)` forms such as `_Atomic(int) values[2]`, typedef aliases, parameters, interpreter-only function return type parsing, pointers, `for` declarations, and `sizeof(_Atomic char)` parsing as no-op metadata over Cust's deterministic storage model with interpreter plus warning-free C compiler-oracle fixtures.
140. [x] Close the remaining arithmetic compound-assignment syntax gap; acceptance covered by `*=`, `/=`, and `%=` over scalar variables, indexed scalar array/pointer lvalues, and dereferenced pointer lvalues, with division-by-zero behavior using the existing diagnostic and C compiler-oracle coverage in `compound_assignments.c`.
141. [x] Continue substantial C-subset closure with implementation-ready design first; completed enum/switch constant-expression parity. Acceptance covered by enum constants as `switch case` labels, block-scoped enum constants in switches, enum initializer references such as `MODE_DONE = MODE_BUSY + 3`, duplicate identifier-valued case-label diagnostics, and C compiler-oracle coverage.
142. [x] Continue substantial C-subset closure with implementation-ready design first; completed broader parser-folded integer constant expressions for enum initializers and `switch case` labels. Acceptance covered by parenthesized constants, unary `+`/`-`/`~`/`!`, multiplicative `*`/`/`/`%`, shifts `<<`/`>>`, relational `<`/`<=`/`>`/`>=`, equality `==`/`!=`, bitwise `&`/`^`/`|`, logical `&&`/`||`, conditional `?:`, and type-query unary operators `sizeof(type-name)` / `_Alignof(type-name)` over literal and visible enum constants without evaluating runtime expressions, plus C compiler-oracle coverage.
143. [x] Add scalar casts in enum/switch integer constant expressions as a small constant-expression conformance closure item; acceptance covered by `(Count)(MODE_SIZE + (Small)6)` in enum initializer values and `(Count)(LOCAL_BASE + (Small)5)` in `switch case` labels, preserving Cust's existing deterministic scalar-cast value semantics and C compiler-oracle coverage.
144. [x] Add expression-form `sizeof(...)` operands in enum/switch integer constant expressions as a small constant-expression conformance closure item; acceptance covered by `sizeof(1 / 0) == sizeof(int)` in an enum initializer and matching switch case, proving the operand is parsed for size metadata but not runtime-evaluated, with interpreter plus ABI-independent C compiler-oracle coverage.
145. [x] Add C-style void cast expressions as a small expression-conformance closure item; acceptance covered by `(void)bump(5)`, `(void)*cursor`, `(void)(cursor + 1)`, `(void)touch(7)`, and `(void)(marker = marker + values[2])` evaluating side effects in discard context without scalar conversion, plus invalid scalar-use diagnostics and C compiler-oracle coverage.
146. [x] Add C99 array-parameter bracket qualifiers as a declaration syntax parity closure item; acceptance covered by `int values[static 3]`, `int values[restrict 3]`, `const int values[static const 3]`, and `struct Point points[static 2]` parsing as the existing pointer-parameter decay model, with bracket `const` preserving pointer-slot const metadata and rejecting parameter reassignment, plus valid/invalid/C compiler-oracle fixtures.
147. [x] Add block-scope `extern` object declarations as a declaration-syntax parity closure item; acceptance covered by `extern int total;`, `extern char marker;`, `extern int values[3];`, `extern struct Point origin;`, `extern union Number number;`, and `extern int *cursor;` inside a function block parsing as no-op declarations that do not shadow existing globals, plus initialized local extern rejection and C compiler-oracle coverage.
148. [x] Continue substantial C-subset closure with implementation-ready design first; completed C-compatible supported scalar type specifier permutations. Acceptance covered by `int unsigned`, `char signed`, `int const unsigned`, `int long signed`, `int short unsigned`, and `int long long unsigned` in globals, locals, typedef aliases, casts, `sizeof` type operands, function returns/prototypes/parameters, pointers, and `for` declarations, with interpreter plus C compiler-oracle fixtures and invalid combination validation.
149. [x] Continue substantial C-subset closure with implementation-ready design first; completed unsupported preprocessor-directive diagnostics for Cust's preprocessor-free subset. Acceptance covered by `#include <stdio.h>` reporting `preprocessor directives are not supported` with lexer source-line/caret context, replacing the generic `unexpected character '#'` fallback and documenting that no macro/include preprocessing runtime was added.
150. [x] Close unsupported C11 `_Generic` selection diagnostics as a parser-trust cleanup item; acceptance covered by `_Generic(1, int: 2, default: 3)` reporting `generic selections are not supported` at the `_Generic` keyword instead of a misleading downstream association-list parser error, with focused exact-output invalid coverage and implementation notes in `references/cust-generic-selection-diagnostics.md`.
151. [x] Close unsupported C function typedef alias diagnostics as a parser-trust cleanup item; acceptance covered by `typedef int Callback(int);` reporting `function typedef aliases are not supported` at the function declarator suffix instead of the generic missing-semicolon fallback, with focused exact-output invalid coverage and no function type alias/runtime support added.
152. [x] Add block-scope function prototype declarations as a C declaration-syntax parity item. Acceptance covered by local parser-only prototypes including `int add(int, int);`, `extern char pick(char, char);`, `extern struct Point make_point(int, int);`, `union Number make_number(int);`, and array-parameter forms inside `main`, while calls still resolve against existing top-level function definitions; valid interpreter and C compiler-oracle fixtures cover the supported subset.
153. [x] Add inferred-length scalar array declarations with initializers as a compact C declaration/initializer parity feature. Acceptance covered by `int values[] = {1, 2, [4] = 5, 6};`, `char word[] = "cat";`, and `const int table[] = {[1] = 3, [3] = 4};`, with inferred object lengths feeding existing zero-fill, `sizeof`, const-array, and array-parameter decay behavior; `int values[];` reports `expected '=' after inferred array declaration`; interpreter and compiler-oracle fixtures cover the supported subset.
154. [x] Add inferred-length aggregate array declarations as the next initializer/declaration parity feature. Acceptance covered by `struct Point points[] = {{1, 2}, {.y = 4}, [3] = {5, 6}};`, `const struct Point fixed[] = {{7, 8}, {.x = 9}};`, and `union Number numbers[] = {{3}, [2] = {.value = 5}};`, with inferred lengths feeding existing aggregate-array zero-fill, `sizeof`, pointer decay/arithmetic, const enforcement, and mutation aliasing; `struct Point points[];` reports `expected '=' after inferred aggregate array declaration`; interpreter and compiler-oracle fixtures cover the supported subset.
155. [x] Continue substantial C-subset closure with implementation-ready design first; completed one-level pointer cast expressions for the supported scalar/aggregate pointer subset. Acceptance covered by `(int *)0`, `(const int *)0`, pointer typedef casts such as `(IntPtr)(values + 1)`, const pointer typedef casts, pointer truthiness/equality, dereference reads, and `sizeof(*(char *)0)`/`sizeof(*(const int *)0)` metadata inference without evaluating null pointers; explicit casts preserve source const-pointee safety and reject const-discard flows, with interpreter plus C compiler-oracle fixtures.
156. [x] Continue substantial C-subset closure with implementation-ready design first; completed comma-separated scalar declaration lists. Acceptance covered by global, local, static-local, and `for` initializer scalar declarator lists such as `int a = 1, b, c = a + 2;`, `const int x = 7, y = 11;`, `char first = 'A', next = 'B';`, and `_Bool ok = expr, nope;`, preserving zero-initialization for omitted initializers and const metadata across every scalar declarator, with interpreter plus C compiler-oracle fixtures.
157. [x] Extend comma-separated declaration-list parity to pointer, array, and aggregate declarators in a scoped follow-up. Acceptance covered by `int *p = values, *q = values + 1;`, `const int *view = values + 1, *start = values;`, `int values[3] = {7, 8, 9}, zeros[2];`, `struct Point point = {10, 11}, copy = point;`, `struct Point points[2] = {{1, 2}, {3, 4}}, empty_points[1];`, and `union Number number = {12}, other;` with same-scope declaration-list lowering, const/pointee metadata preservation, assignment-precedence initializer parsing to preserve declaration separators, and interpreter plus C compiler-oracle fixtures.
158. [x] Continue C-subset closure by targeting newly discovered parser/runtime parity gaps with implementation-ready fixtures first; completed direct aggregate-array element copy assignment for supported struct/union arrays and aggregate pointer indexing. Acceptance covered by `points[0] = replacement`, `struct Point returned = (points[0] = replacement)`, and `cursor[0] = (struct Point){11, 12}`, preserving deep-copy isolation, same-type checks, const/read-only target diagnostics, interpreter coverage, and C compiler-oracle coverage.
159. [x] Continue C-subset closure by targeting newly discovered parser/runtime parity gaps with implementation-ready fixtures first; completed aggregate-array element copy assignment through embedded aggregate-array fields. Acceptance covered by `line.points[1] = replacement`, `struct Point returned = (line.points[1] = replacement)`, `slot->points[0] = (struct Point){11, 12}`, deep-copy isolation, same-type mismatch diagnostics, interpreter coverage, and C compiler-oracle coverage.
160. [x] Continue C-subset closure by targeting newly discovered parser/runtime parity gaps with implementation-ready fixtures first; completed aggregate pointer declaration-list conformance coverage. Acceptance covered by `struct Point *p = points, *q = points + 1;` and `union Number *n = numbers, *m = numbers + 2;` with `->`, indexed aggregate pointer reads/writes, pointer arithmetic, and function-argument flow verified by interpreter and C compiler-oracle fixtures.
161. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed comma-separated aggregate field declaration lists inside supported struct/union definitions. Acceptance covered by `int x, y;`, `char tag, code;`, `struct Point start, end;`, `int weights[2], offsets[2];`, `int *head, *tail;`, `const int *view, *limit;`, and `union Number { int value, other; };`, preserving duplicate-field diagnostics and C compiler-oracle coverage.
162. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed typedef-backed aggregate field declaration lists with alias const metadata. Acceptance covered by `IntPtr head, tail;`, `ConstIntView view, limit;`, `ConstIntSlot fixed;`, aggregate typedef array fields, aggregate pointer typedef fields, reassignment rejection for a const pointer-slot typedef field, and C compiler-oracle coverage.
163. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported aggregate forward-declaration diagnostics for incomplete `struct Point;` / `union Number;` forms, replacing misleading downstream `undefined struct type` variable-declaration errors with targeted `forward struct declarations are not supported` / `forward union declarations are not supported` parser errors at the semicolon.
164. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported aggregate bit-field diagnostics for `struct Flags { unsigned ready : 1; };` and `union Bits { int value : 3; };`, replacing misleading downstream `expected ';' after struct field declaration, found Colon` parser errors with targeted `bit-field aggregate fields are not supported` errors at the colon.
165. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed anonymous aggregate object declarations for supported structs/unions. Acceptance covered by global and local `struct { ... } name = {...};`, `union { ... } name = {...};`, anonymous aggregate arrays with brace/designated initializers, scalar array fields inside anonymous aggregates, field reads, and C compiler-oracle coverage.
166. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed qualified anonymous aggregate objects and same-declaration anonymous aggregate pointer declarator lists. Acceptance covered by `const struct { int x; int y; } origin = {2, 3};`, `struct { int x; int y; } point = {4, 5}, copy = point, *slot = &point;`, `union { int value; char tag; } number = {6}, *number_slot = &number;`, pointer `->` mutation, by-value copy, const anonymous aggregate field-assignment rejection, and C compiler-oracle coverage.
167. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed same-declaration anonymous aggregate array plus pointer declarator conformance coverage. Acceptance covered by `struct { int x; int y; } points[3] = {...}, *slot = points + 1;` and `union { int value; char tag; } numbers[2] = {...}, *picked = numbers;`, with pointer arithmetic, `->`, indexed aggregate pointer mutation, and native C compiler-oracle comparison.
168. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed comma-separated typedef alias declarator lists. Acceptance covered by `typedef int Count, *CountPtr, Counts[3];`, `typedef const int ConstCount, *ConstCountView, ConstCounts[2];`, `typedef struct Point Point, *PointPtr, Points[2];`, and `typedef struct { int value; int extra; } Anon, *AnonPtr, Anons[2];`, preserving per-declarator pointer stars, post-star const pointer-slot metadata, array alias suffixes, anonymous aggregate aliases, invalid const pointer-slot assignment diagnostics, and C compiler-oracle coverage.
169. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed inline enum definition object declarations. Acceptance covered by top-level and block-scope `enum { A = 1 } value = A;`, `const enum { LOCKED = 7 } global = LOCKED;`, `enum Mode { IDLE = 11 } mode = IDLE;`, and same-declaration scalar lists such as `enum { ONE = 1 } first = ONE, second = first + global_state;`, with runtime enum constants installed before same-statement initializers, const enum object assignment diagnostics, and C compiler-oracle coverage.
170. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed inline enum object declarations.
171. [x] Continue C-subset closure by auditing declaration contexts for inline enum object declarations. Acceptance covered by inline enum objects in `for` initializers, local `static enum` declarations, and local `auto enum` / `register enum` declarations, with parser routing for storage/context specifiers, static-local wrapping that preserves generated `EnumDecl` statements before `StaticLocal` variables, focused interpreter coverage, and C compiler-oracle coverage.
172. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed mixed supported-subset conformance coverage. Acceptance covered by `mixed_declaration_contexts.c`, which combines comma-separated typedef alias lists, inline enum object declarations in a `for` initializer and local declaration, pointer-first anonymous aggregate declaration lists, const anonymous aggregate arrays with pointer views, aggregate compound literal returns, and typedef-spelled aggregate return values in one warning-free native C compiler-oracle fixture. Focused interpreter coverage passed immediately, confirming the behavior was already supported and now locked in.
173. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported enum forward-declaration diagnostics. Acceptance covered by `enum Color;` reporting `forward enum declarations are not supported` at the semicolon instead of falling through to `undefined enum type 'Color'`, preserving Cust's no-incomplete-enum-type boundary with focused exact-output interpreter coverage.
174. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported anonymous aggregate parameter diagnostics. Acceptance covered by `int take(struct { int x; } point)` reporting `anonymous aggregate parameters are not supported` at the `struct` keyword instead of falling through to a generic `expected parameter type, found LBrace` parser error, preserving Cust's current named/typedef-backed aggregate parameter boundary without adding anonymous aggregate parameter runtime support.
175. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported anonymous aggregate return-type diagnostics. Acceptance covered by `struct { int x; } make(void)` reporting `anonymous aggregate return types are not supported` at the `struct` keyword instead of falling through to a generic aggregate-variable semicolon diagnostic, preserving Cust's current named/typedef-backed aggregate return-type boundary without adding anonymous aggregate return runtime support.
176. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported K&R/old-style function parameter-list diagnostics. Acceptance covered by `int add(x, y) int x; int y; { ... }` reporting `old-style function parameter lists are not supported` at the first identifier-list parameter, while `int identity(value) { ... }` preserves the existing missing-parameter-type diagnostic.
177. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed targeted invalid const anonymous aggregate array pointer-view coverage. Acceptance covered by `const struct { int x; int y; } points[2] = ..., *slot = points + 1; slot->x = 9;` rejecting writes through the same-declaration anonymous aggregate pointer view with `cannot assign through pointer to const`, proving const metadata is preserved for anonymous aggregate array pointer declarators. Focused RED first exposed an overly broad direct-root-array write fixture, which was corrected to the intended pointer-view path before GREEN.
178. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported anonymous aggregate pointer declarator diagnostics. Acceptance covered by `struct { int x; } **slot;` reporting `pointer-to-pointer declarations are not supported` at the second `*` and `struct { int x; } *slots[2];` reporting `pointer array declarations are not supported` at `[`, replacing generic missing-name/missing-assignment diagnostics and preserving Cust's one-level non-array pointer subset boundary.
179. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed anonymous aggregate object declarations in `for` initializer contexts. Acceptance covered by `for (struct { int x; } point = {1}; ...)` and `for (union { int value; char tag; } number = {5}; ...)`, routing `Token::Struct | Token::Union` through aggregate declaration parsing in `parse_for`, with focused interpreter RED/GREEN coverage and C compiler-oracle verification.
180. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed qualified anonymous aggregate object declarations in `for` initializer contexts. Acceptance covered by `for (const struct { int limit; int step; } cfg = {...}, *view = &cfg; ...)` and `for (volatile union { int value; char tag; } number = {...}; ...)`, routing qualifier-leading aggregate starts through aggregate declaration parsing in `parse_for`, preserving same-declaration pointer declarators and const pointer-view diagnostics, plus focused interpreter coverage and C compiler-oracle verification.
181. [x] Continue C-subset closure by auditing `_Alignas`/`_Thread_local`/storage-class declaration contexts for anonymous aggregate routing. Acceptance covered by `_Alignas(8) volatile union { ... } scratch`, `static _Alignas(8) const struct { ... } tuned, *view = &tuned`, top-level `_Thread_local const struct { ... } global_shape`, and local `static _Thread_local const struct { ... } local_shape`, preserving anonymous type identity, static-local wrapping, same-declaration pointer declarators, const metadata, and C compiler-oracle coverage.
182. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed unsupported anonymous aggregate parenthesized pointer declarator diagnostics. Acceptance covered by `struct { int x; } (*slot);` reporting `parenthesized pointer declarations are not supported` at the parenthesized declarator instead of the generic `expected struct variable name, found LParen`, preserving Cust's unsupported parenthesized pointer/function-pointer declarator boundary without adding runtime support.
183. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed anonymous aggregate type-name operands in `sizeof(...)` and `_Alignof(...)`. Acceptance covered by direct anonymous `struct`/`union` type queries, const-qualified anonymous aggregate type names, pointer and one-dimensional array suffixes, deterministic no-padding size/alignment behavior, and ABI-independent C compiler-oracle relationship coverage.
184. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed anonymous aggregate compound literals for expression-local C99 type names. Acceptance covered by anonymous `struct`/`union` compound literals with field access and anonymous aggregate-array compound literals with indexing/field access, using unique internal anonymous type identities without installing source-level tags or typedef aliases, plus interpreter and C compiler-oracle coverage.
185. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed anonymous aggregate pointer casts for expression-local C99 type names. Acceptance covered by `(struct { int x; } *)0`, `(const union { char tag; } *)0`, and `sizeof(*(struct { char tag; } *)0)`, creating unique internal anonymous aggregate type identities, preserving leading const as pointer-cast pointee metadata, and verifying warning-free C compiler-oracle behavior through null comparisons and non-evaluating `sizeof`.
186. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed anonymous aggregate fields inside supported aggregate definitions. Acceptance covered by `struct Box { struct { int x; int y; } point; union { int value; char tag; } number; struct { int value; } items[2]; };`, recursive brace initialization, direct field reads/writes, anonymous aggregate array fields, native C compiler-oracle coverage, and negative coverage proving separately spelled anonymous aggregate pointer types remain distinct.
187. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed const aggregate field nested-write enforcement. Acceptance covered by named `const struct Point point;` fields and anonymous `const struct { int x; int y; } point;` fields rejecting nested scalar writes such as `box.point.x = 3` / `box.point.y = 4` with `cannot assign to const struct field 'point'`, while preserving initializer/read behavior. The fix checks parent aggregate-field constness before recursive scalar-field assignment descent.
188. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed const aggregate-array parent-field diagnostics for direct fields and aggregate compound literals. Acceptance covered by `box.points[1].x = 9` and `((struct Box){{...}}).points[1].x = 9` rejecting writes through `const struct Point points[2];` with `cannot assign to const struct field 'points'`. The compound-literal fix preserves the existing pointer-to-const safety gate while tracing `AggregateFieldGet` pointer-expression metadata to recover the const parent field label without evaluating the compound literal.
189. [x] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; completed const embedded aggregate-array element copy-assignment enforcement through struct pointers. Acceptance covered by direct `line.points[1] = replacement` and struct-pointer `slot->points[0] = replacement` writes to `const struct Point points[2];`, both rejecting with `cannot assign to const struct field 'points'`. Focused RED proved the direct path was already safe while the struct-pointer path mutated through `AddressOfStructPtrArrayField`; implementation now traces `StructPtrArrayGet` / `AddressOfStructPtrArrayField` pointer metadata back to const aggregate fields.
190. [ ] Continue C-subset closure by finding the next newly discovered parser/runtime parity gap with implementation-ready fixtures first; good candidates include malformed-source fuzzing for another fresh exact diagnostic beyond the current parser-trust suite, additional mixed supported-subset conformance fixtures as new features land, auditing remaining pointer/const/storage-class/type-query combinations for anonymous aggregate fields and declaration lists, or adding targeted negative coverage for pointer-arithmetic diagnostics through embedded/anonymous aggregate field paths.

## Every-run checklist

- [ ] Pull latest `main` with `git checkout main && git pull --ff-only`
- [ ] Read all files in `status/`
- [ ] Ideate candidate roadmap-completion tasks from status/backlog/current code
- [ ] Think through impact, safety, dependencies, and testability; choose the best meaningful work package
- [ ] Record good overflow ideas in `status/todo.md` or `status/missing-features.md`
- [ ] Research docs if uncertain
- [ ] Write/update failing tests first for code behavior
- [ ] Create/implement the full selected C/interpreter/tooling work package and close concrete backlog items, not vague improvements
- [ ] Run local verification
- [ ] Run Docker verification
- [ ] Update `status/current-state.md`
- [ ] Update `status/todo.md`
- [ ] Update `status/stuck.md` if blocked
- [ ] Update `status/research.md` with useful links/findings
- [ ] Commit all verified changes, including status-only updates
- [ ] Push to `origin main` before ending the run
