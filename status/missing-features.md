# Cust Missing Features

Prioritized backlog for autonomous implementation.

Latest update (2026-07-15): fixed-seed model-based property coverage now checks mutable and const-typedef `struct`/`union` array compound literals through arithmetic, pointer difference, equality, ordering, conditional, comma, and indexed field-read expressions. The independent model compares hidden storage identity, index/value, concrete aggregate pointee type, const metadata, and exact bounds/cross-root/const-discard/write/type diagnostics. Existing Cust behavior matched; all eleven fuzz-safety tests remain sub-second locally, and warning-free GCC/Clang binaries return 164 like Cust.

Latest update (2026-07-15): fixed-seed model-based property coverage now checks mutable `int`/`char` array compound literals, const array-typedef compound literals, and read-only string literals through arithmetic, pointer difference, equality, ordering, conditional, and comma expressions. The independent model compares hidden storage identity, index/value, pointee type, const/read-only metadata, and exact bounds/cross-root/const-discard/write/type diagnostics. Existing Cust behavior matched; all ten fuzz-safety tests remain sub-second locally and in Docker, and warning-free GCC/Clang binaries return 65 like Cust.

Latest update (2026-07-15): fixed-seed model-based property coverage now checks `int` and `char` array fields in named and anonymous struct/union containers across direct, arrow, nested, indexed-address, and aggregate-compound-literal routes. The independent model compares owner/path/index identity, scalar pointee type, const metadata, and exact cross-owner, bounds, const-discard, read-only-write, and type-conversion diagnostics through arithmetic/difference/conditional/comma wrappers. Existing Cust behavior matched; all nine fuzz-safety tests remain sub-second locally, and warning-free GCC/Clang binaries return 56 like Cust.

Latest update (2026-07-15): fixed-seed model-based property coverage now checks embedded struct/union array fields in named and anonymous struct/union containers across direct, arrow, nested, indexed-address, and aggregate-compound-literal routes. The independent model compares owner/path/index identity, pointee type, const metadata, exact field/literal bounds and cross-owner diagnostics, and panic freedom through arithmetic/difference/conditional/comma wrappers. Existing Cust behavior matched; all eight fuzz-safety tests remain sub-second locally, and warning-free GCC/Clang binaries return 246 like Cust.

Latest update (2026-07-15): fixed-seed model-based property coverage now checks named struct/union pointer expression storage, indexes, pointee types, const metadata, exact bounds/type/const/cross-array diagnostics, and panic freedom across direct, arrow, and aggregate-compound-literal pointer-field reads/assignments/compound assignments plus arithmetic/conditional/comma wrappers. Generated RED cases exposed missing metadata for pointer-field assignment results and `->` reads of const-pointee fields; all metadata paths now agree, and warning-free GCC/Clang binaries return 12 like Cust.

Previous update (2026-07-15): fixed-seed model-based property coverage now checks scalar-array pointer expression values, exact const-discard/cross-array diagnostics, and panic freedom across pointer arithmetic, differences, conditionals, and comma wrappers. The generated RED cases exposed speculative operand evaluation that treated wrapped scalar zero as a null pointer and masked cross-array errors; pointer arithmetic now classifies result shapes before evaluation, including pointer-field assignment result variants. Warning-free GCC/Clang binaries return 50 like Cust.

Previous update (2026-07-15): pointee constness for pointer addition/subtraction now follows only the operand that supplies the pointer-valued result. Scalar pointer differences no longer leak const metadata into mutable pointer bases, genuinely const bases remain const, cross-array diagnostics remain exact, and warning-free GCC/Clang binaries return 9 like Cust.

Previous update (2026-07-15): pointer-pointer subtraction is now classified as a scalar result when nested inside larger additive, multiplicative, conditional, comma, and truthiness expressions. Same-array differences compose normally, cross-array subtraction retains `cannot subtract pointers to different arrays`, pointer-plus/minus-scalar results remain pointer-valued, and warning-free GCC/Clang binaries return 42 like Cust.

Previous update (2026-07-15): address-of indexed aggregate elements now works through non-variable pointer expressions. Pointer-returning calls, conditional/comma pointer expressions, direct pointer-valued aggregate fields, and `->` pointer fields all support `&expr[i]`; named struct/union targets alias original storage, pointer-field indexing preserves const pointer-slot versus const-pointee semantics, exact const-discard and bounds diagnostics pass, and warning-free GCC/Clang binaries return 110/20 like Cust.

Latest update (2026-07-14): address-of aggregate-pointer indexed elements now work for array-backed named struct and union pointers. `&p[i]` preserves concrete pointee/const metadata, aliases mutations to original storage, flows through declarations and function arguments, supports pointer difference, and retains aggregate-array bounds diagnostics. Warning-free GCC/Clang binaries return 205 like Cust.

Latest update (2026-07-14): chained const-qualified array typedef compound literals are now covered across `int`, `char`, named-struct, and enum element types. An additional alias layer preserves read-only pointer metadata, full-array and indexed-element `sizeof`, pointer/parameter flow, pointer arithmetic, exact const-discard/write diagnostics, and lexical shadowing by mutable inner array aliases. Metadata-only pointee inference now classifies scalar and aggregate array compound literals, fixing incorrect indexed-element `sizeof`; warning-free GCC/Clang binaries return 127 like Cust.

Previous update (2026-07-14): chained qualified fixed-array typedef aliases are now covered in parameter-adjustment contexts across scalar, named-struct, and enum element types. An additional alias layer and comma-separated mixed declarator lists preserve element identity and `const`, unnamed prototypes remain compatible with named definitions, adjusted pointer slots remain assignable, exact write/const-discard diagnostics pass, lexical shadowing remains correct, and warning-free GCC/Clang binaries return 127 like Cust. No production change was required.

Previous update (2026-07-14): qualified fixed-array typedef parameter adjustment is now covered across named struct, union, and enum aliases. Unnamed prototypes, named definitions, calls, legal adjusted-pointer slot reassignment, exact const-write/const-discard diagnostics, lexical shadowing, pointer views, `sizeof`, and `_Alignof` all pass; warning-free GCC/Clang binaries return 127, matching Cust. The prior general parser fix required no further production change.

Previous update (2026-07-14): fixed-array typedef aliases derived from qualified anonymous aggregate atomic values are now covered across const/volatile struct and union elements, globals, static/automatic locals, aggregate fields, unnamed prototypes and named definitions, array decay, pointer views/arithmetic, lexical shadowing, `sizeof`, and `_Alignof`. A parser correctness fix maps alias-carried `const` to the pointee of an adjusted array parameter rather than incorrectly making its pointer slot const. Exact negative coverage retains const-write, mutable-pointer decay/argument, and multidimensional-alias diagnostics. Warning-free GCC/Clang binaries return 255, matching Cust; the general scalar const-array typedef regression returns 48 on all three paths.

Previous update (2026-07-14): qualified value aliases derived from anonymous aggregate atomic value aliases are covered across chained aliases, objects/arrays, fields, parameters/prototypes, pointer views, lexical shadowing, `sizeof`, and `_Alignof`, with exact const and nested-atomic diagnostics and warning-free GCC/Clang exit 255.

## P0 — correctness and developer trust

- [x] Scalar additive-expression classification around pointer differences; expressions such as `base + (right - left)`, `(right - left) * scale`, conditional/comma results containing pointer differences, and pointer-difference truthiness remain scalar, while same-array/different-array diagnostics and pointer-valued add/subtract forms remain intact with interpreter and warning-free compiler-oracle coverage.
- [x] Const-pointee metadata propagation through pointer-difference scalar offsets; mutable pointer expressions such as `values + (const_right - const_left)` and `cursor - (const_right - const_left)` derive pointee constness only from the pointer-valued base, genuinely const pointer bases remain const, and same-array/different-array diagnostics stay exact. Focused valid/invalid interpreter tests and warning-free GCC/Clang fixtures return 9 like Cust.
- [x] Deterministic model-based pointer-expression property coverage; fixed-seed bounded scalar-array expression trees combine pointer-plus/minus-scalar, reverse addition, pointer differences, conditional/comma wrappers, and mutable/const bases; an independent test-side model checks result values plus exact const-discard/cross-array diagnostics under `catch_unwind`. Generated RED cases also fixed speculative pointer operand evaluation and retained a fast four-test `fuzz_safety` target.

- [x] General qualified fixed-array typedef parameter adjustment across named aggregate and enum aliases; const-qualified named struct, union, and enum array typedef parameters preserve pointee qualification through unnamed prototypes/named definitions and calls, adjusted pointer slots remain assignable inside callees, mutable-pointer decay/argument and writes are rejected exactly, lexical shadowing remains correct, and warning-free GCC/Clang fixtures use ABI-independent relationships and return 127 like Cust.
- [x] Fixed-array typedef aliases derived from qualified anonymous aggregate atomic value aliases; `typedef ConstAtomicAnonValue ConstAtomicAnonArray[2]` plus volatile and anonymous-union counterparts preserve element identity and qualification through globals/locals, aggregate fields, named/unnamed array parameters and compatible prototypes/definitions, array decay, pointer views, lexical shadowing, `sizeof`, and `_Alignof`; const arrays reject element writes and mutable-pointer decay, multidimensional aliases retain exact diagnostics, warning-free GCC/Clang binaries return 255, and the adjusted-parameter parser fix also covers scalar const-array typedefs.
- [x] Qualified value aliases derived from anonymous aggregate atomic value aliases; `typedef const AtomicAnonValue ConstAtomicAnonValue` and matching `volatile`/anonymous-union forms preserve the original anonymous identity and top-level qualification through chained aliases, objects, fixed arrays, pointer views, fields, parameters/prototypes, lexical shadowing, `sizeof`, and `_Alignof`; const objects/arrays and pointer views retain write/const-discard diagnostics, invalid `_Atomic(QualifiedAlias)` reports the alias token, and warning-free GCC/Clang binaries return 255.
- [x] Const-qualified pointer views derived from anonymous aggregate atomic value aliases; `typedef const AtomicAnonValue *ConstAtomicAnonView` and matching anonymous-union forms preserve the original anonymous identity plus atomic/const pointee metadata across globals/locals, fields, parameters/prototypes, conversions, `sizeof`, and `_Alignof`; mutable-to-const flows work, const-to-mutable declarations/assignments/arguments and writes through const views retain exact diagnostics, const pointer slots remain distinct, deeper pointer/pointer-array aliases retain exact diagnostics, and warning-free GCC/Clang oracle binaries return 255.
- [x] Typedef aliases of pointers and one-dimensional arrays derived from anonymous aggregate atomic value aliases; `AtomicAnonPtr` / `AtomicAnonArray[N]` and matching anonymous-union forms work in globals/locals, aggregate fields, named/unnamed parameters, compatible prototypes/definitions, pointer arithmetic, array decay, `sizeof`, and `_Alignof`; preserve the original anonymous identity and atomic pointee qualification; distinguish const pointer slots; retain pointer-to-pointer, pointer-array, and multidimensional-array typedef diagnostics; and have warning-free GCC/Clang oracle coverage.
- [x] Derived pointer and one-dimensional array declarators from anonymous aggregate atomic value aliases; `AtomicAnonValue *`, `AtomicAnonValue values[N]`, matching anonymous-union aliases, aggregate fields, named/unnamed parameters, compatible prototypes/definitions, pointer arithmetic, array-parameter decay, `sizeof`, and `_Alignof` preserve the alias's anonymous identity while exact tests retain Cust's one-level pointer and fixed-array safety boundaries, with warning-free GCC/Clang oracle coverage.
- [x] Typedef aliases of anonymous aggregate atomic value types, including comma-separated aliases, alias identity reuse, global/local objects, aggregate fields, named/unnamed signature compatibility, object/type-query use, nested-atomic rejection, lexical shadowing, and warning-free native-oracle coverage.
- [x] Typedef aliases of anonymous aggregate atomic pointer types, including comma-separated aliases, alias identity reuse, named/unnamed signature compatibility, object/type-query use, const-pointee and const-slot boundaries, double-atomic rejection, lexical shadowing, and warning-free native-oracle coverage.
- [x] Anonymous aggregate pointer arguments inside `_Atomic(type-name)` across declarations, aggregate fields, parameters, `sizeof`, and `_Alignof`, with unique pointee identities, const-pointee metadata, exact unsupported suffix diagnostics, and warning-free native-oracle coverage for compatible contexts.
- [x] Anonymous aggregate definitions inside `_Atomic(type-name)` across objects, aggregate fields, parameters, `sizeof`, and `_Alignof`, with unique anonymous type identities, exact qualified/nested diagnostics, and warning-free compiler-oracle coverage for native-compatible contexts.
- [x] Parser errors with source spans: line, column, token context
- [x] Exact missing-RHS diagnostics for assignment operators: malformed scalar, dereference, array, struct-field, and expression-level assignments such as `value = ;`, `value += )`, and `value <<= }` report `expected expression after assignment operator '<op>', found ...` at the delimiter/terminator instead of a generic primary-expression diagnostic.
- [x] Add token context snippets to lexer errors
- [x] Block scoping rules for variables inside `{ ... }`
- [x] Better parser recovery/error messages (broad track largely expanded; missing-`=` diagnostics, missing-name/type diagnostics, context-aware unterminated-block diagnostics, array length edge diagnostics including delimiter-only `[` `)`, `;`, and `}` forms plus invalid-start `?` / nested `[` forms, array index missing-expression diagnostics, function-call missing-initial-argument diagnostics including comma-leading and misplaced-`]` argument lists, function-parameter leading/doubled-comma and misplaced-`]` diagnostics, non-constant array-length identifier diagnostics, comma-expression array-length diagnostics, comma-operator missing-RHS diagnostics, conditional-operator missing-operand diagnostics, assignment-operator missing-RHS diagnostics, binary-operator missing-RHS diagnostics, unary-operator missing-operand diagnostics, `sizeof` missing-operand diagnostics, pointer-parameter malformed-list coverage, explicit unsupported pointer-return/pointer-array/pointer-to-pointer diagnostics, unsupported array-return declarator suffix diagnostics, delimiter-aware parameter/call trailing-comma diagnostics, duplicate `switch` case/default label diagnostics, pointer-returning-call scalar-use diagnostics, union value scalar-use diagnostics, parenthesized pointer declarator diagnostics including anonymous aggregate parenthesized pointer declarators, aggregate-field parenthesized pointer/function-pointer declarators, parenthesized pointer typedef declarators, flexible array aggregate-field diagnostics, multidimensional array declaration/parameter/field diagnostics, function-pointer declaration/typedef/local/parameter unsupported diagnostics, local function-definition unsupported diagnostics, function typedef alias unsupported diagnostics, variadic function parameter diagnostics, unsupported `goto`/label diagnostics, unsupported top-level/stray control-flow diagnostics for file-scope `if`/`else`/`while`/`do`/`for`/`switch`/`case`/`default`, function-body unmatched `else`, and statement-only control-flow keywords in `for` initializer/increment clauses, unsupported preprocessor-directive diagnostics, unsupported C11 `_Generic` selection diagnostics, comma-in-integer-constant-expression diagnostics for enum initializers and switch case labels, unsupported aggregate/enum forward-declaration diagnostics, unsupported anonymous aggregate parameter and return-type diagnostics, unsupported K&R/old-style function parameter-list diagnostics, unsupported aggregate bit-field diagnostics, unsupported anonymous aggregate pointer-to-pointer/pointer-array declarator diagnostics, unsupported star-VLA array-length diagnostics, unsupported `void *` diagnostics across return/parameter/declaration/cast/type-query forms, parenthesized pointer cast/type-query diagnostics, unsupported floating-point and complex type diagnostics, break/continue outside loop-or-switch source-location diagnostics, and invalid scalar type-specifier diagnostics are now included)
- [x] Brace-token `_Atomic(type-name)` and initial function-parameter diagnostics: malformed declaration/type-name and parameter-list routes such as `_Atomic({)`, `sizeof(_Atomic({))`, and `int add({ int b)` now report contextual `_Atomic`/function-parameter messages instead of falling through to generic `expected type, found LBrace` parsing.
- [x] Colon-token expression-boundary diagnostics: malformed starts such as `callee(:)`, `callee(1, :)`, `values[:]`, `int values[2] = {:}`, `return :;`, `if (:)`, `switch (:)`, and `return (:);` now report route-local function-call, array-index, initializer, return/control/switch, and grouped-expression diagnostics instead of falling through to generic `expected expression, found Colon` parsing.
- [x] Colon-token parameter and integer-constant boundary diagnostics: malformed function parameters such as `int add(; int b)`, `int add(: int b)`, `int add(. int b)`, `int add(-> int b)`, and after-comma variants now report contextual function-parameter diagnostics instead of generic type parsing; colon starts in fixed array lengths, array designator indexes, enum constant values, and switch `case` labels now report route-local `before ':'` integer-constant diagnostics instead of generic `found Colon` errors.
- [x] Switch `case` label invalid-start diagnostics: malformed labels such as `case [:` and `case ?:` now report contextual `expected integer constant after switch case before '['/'?'` messages instead of falling through to generic integer-constant parsing.
- [x] Function-call missing/misplaced argument diagnostics: delimiter-leading, invalid operand-start, and keyword-start argument lists such as `first(,)`, `first(]`, `take([)`, `take(?)`, `take(return)`, and after-comma variants such as `add(1, int)` now report contextual `expected function call argument...` messages instead of falling through to generic expression parsing.
- [x] Array/string index missing/misplaced operand diagnostics: delimiter-leading, invalid operand-start, and keyword-start index forms such as `values[]`, `values[;`, `values[}`, `values[[)`, `"hi"[?]`, `values[int]`, and `"hi"[return]` now report contextual `expected array index expression...` messages instead of falling through to generic expression parsing.
- [x] Declaration initializer missing/misplaced operand diagnostics: delimiter-leading, invalid operand-start, and keyword-start initializer forms such as `int value = ;`, `int *slot = , fallback = 0`, `int value = [?];`, `int *slot = ?;`, and `int value = typedef;` now report contextual `expected initializer expression after '=' in ... declaration...` messages instead of falling through to generic expression parsing.
- [x] Array designator delimiter diagnostics: malformed bounded and unbounded designator indexes such as `.values[]`, `.values[;]`, `.values[,]`, `.values[}`, `(int[]){[,] = 1}`, and `(int[]){[} = 1}` now report contextual `expected array designator index before ...` messages instead of falling through to generic integer-constant parsing.
- [x] Control-flow condition invalid-start diagnostics: malformed `if`/`while`/`do while`/`switch`/`for` controlling expressions such as `if ([)`, `while (?)`, `do { } while ([);`, `switch ([)`, `for (int i = 0; [; i = i + 1)`, and malformed `for` increments such as `for (int i = 0; i < 3; [)` now report contextual `expected expression after <keyword/clause>...` messages instead of falling through to generic expression parsing or uncontextual for-clause errors.
- [x] `for` initializer invalid-start diagnostics: malformed initializer starts such as `for ([; ; )`, `for (?; ; )`, and `for (,; ; )` now report contextual `expected expression after for initializer...` messages instead of falling through to the generic `unexpected token in for initializer` fallback.
- [x] Return-expression invalid-start diagnostics: malformed value-return starts such as `return [;`, `return ?;`, `return .field;`, and `return ->field;` now report contextual `expected expression after return...` messages instead of falling through to generic expression parsing.
- [x] Operator RHS invalid-start diagnostics: malformed comma, assignment, binary, and conditional operands such as `return 1, [;`, `value = [;`, `value += ?;`, `return 1 + [;`, `return 1 + .field;`, `value = ->field;`, `return 1, .field;`, `return 1 ? [ : 2;`, `return 1 ? 2 : [;`, `return 1 + return;`, `value = return;`, `return 1, return;`, and conditional branches starting with `return` now report contextual `expected expression after ... operator...` messages instead of falling through to generic expression parsing.
- [x] Braced initializer keyword-start diagnostics: malformed initializer elements such as `int values[2] = {return};` and `struct Point point = {return};` now report contextual `expected initializer element in ... before 'return'` diagnostics instead of generic expression-parser failures.
- [x] Aggregate initializer invalid-start diagnostics: malformed aggregate initializer entries such as `(struct Point){[}` now report `expected initializer element in struct 'Point' initializer, found LBracket` at the invalid array-designator start rather than falling through to generic expression parsing; bracket designators remain restricted to array initializer contexts.
- [x] Array-length invalid-start diagnostics: malformed fixed-size array lengths such as `int values[?];` and nested type-name forms such as `sizeof(int[[)` now report contextual `expected array length before '?'/'['` messages instead of falling through to generic integer-constant parsing.
- [x] Array-length keyword/statement-token invalid-start diagnostics: malformed fixed-size array lengths such as `int values[int];`, function parameter lengths such as `char text[char]`, aggregate fields such as `int values[struct];`, typedef aliases such as `typedef int Values[return];`, and type-name lengths such as `sizeof(int[while])` now report contextual `expected array length before '<token>'` messages instead of generic `expected array length, found ...` integer-constant diagnostics while preserving valid `sizeof(...)` / `_Alignof(...)` integer constant expressions.
- [x] `_Alignas` and `_Static_assert` invalid-start diagnostics: malformed declaration-specifier/assertion operands such as `_Alignas([)`, `_Alignas(?)`, `_Static_assert([, "msg")`, and `_Static_assert(?, "msg")` now report contextual source-located route errors instead of falling through to generic expression parsing.
- [x] Function-parameter invalid-start diagnostics: malformed parameter starts such as `int add([ int b)`, `int add(? int b)`, `int add(return int b)`, `int add(int a,[ int b)`, `int add(int a,? int b)`, and `int add(int a,return int b)` now report contextual `expected function parameter...` messages instead of falling through to generic type parsing.
- [x] Array-designator invalid-start diagnostics: malformed bounded and unbounded designator indexes such as `.values[[] = 1`, `.values[?] = 1`, `(int[]){[[] = 1}`, and `(int[]){[?] = 1}` now report contextual `expected array designator index before '['/'?'` messages instead of falling through to generic integer-constant parsing.
- [x] Array-designator keyword/statement-token invalid-start diagnostics: malformed bounded and unbounded designator indexes such as `int values[2] = {[int] = 1};`, `(int[]){[return] = 1}`, and `struct Point points[2] = {[struct] = {1, 2}};` now report contextual `expected array designator index before '<token>'` messages instead of generic `expected array designator index, found ...` integer-constant diagnostics while preserving valid folded constant-expression designators.
- [x] Unary-operator invalid-start diagnostics: malformed operands after prefix unary operators such as `return ![;`, `return *?;`, and `return &return;` now report contextual `expected expression after unary operator '<op>'...` messages instead of falling through to generic expression parsing.
- [x] Enum constant and switch `case` label keyword invalid-start diagnostics: malformed integer-constant routes such as `enum Bad { FIRST = int };`, `enum Bad { FIRST = return };`, `case int:`, and `case return:` now report contextual `expected integer constant after ... before '<keyword>'` messages instead of falling through to generic `expected integer constant..., found ...` diagnostics.
- [x] `sizeof` invalid-start diagnostics: malformed operands such as `sizeof return`, `sizeof int`, `sizeof(return)`, `sizeof(.)`, and `sizeof(->field)` now report contextual `expected sizeof operand...` messages instead of falling through to generic expression parsing, while parenthesized type operands such as `sizeof(int)` remain valid.
- [x] `_Alignof` invalid-start diagnostics: malformed type operands such as `_Alignof([)`, `_Alignof(?)`, `_Alignof(return)`, `_Alignof(.)`, and `_Alignof(->field)` now report contextual `expected _Alignof type before '<token>'` messages instead of falling through to generic `_Alignof` type parsing, while valid `_Alignof(type-name)` operands and integer-constant `_Alignof(...)` folding remain supported.
- [x] `_Alignas` and `_Static_assert` keyword/selector invalid-start diagnostics: malformed declaration-specifier/assertion operands such as `_Alignas(return) int value;`, `_Alignas(.)`, `_Alignas(->field)`, `_Static_assert(return, "condition required");`, `_Static_assert(., "condition required")`, and `_Static_assert(->field, "condition required")` now report contextual route-local messages instead of falling through to generic expression parsing, while valid `_Alignas(type-name)`, `_Alignas(expression)`, and `_Static_assert` forms remain supported.
- [x] `_Atomic(type-name)` invalid-start diagnostics: malformed atomic specifier arguments such as `_Atomic([)`, `_Atomic(?)`, `_Atomic(return)`, `_Atomic(.)`, and `_Atomic(->field)` now report contextual `_Atomic` type diagnostics instead of falling through to generic type parsing, while valid bare `_Atomic` qualifiers and `_Atomic(type-name)` specifiers remain supported across globals, locals, parameters, `sizeof`, casts, and aggregate fields.
- [x] Grouped-expression and cast-operand invalid-start diagnostics: malformed grouped operands such as `return ([);`, `return (?);`, and `return (typedef);`, plus malformed cast operands such as `return (int)[;`, `return (char)?;`, `return (int)return;`, `return (int).;`, and `return (int)->field;`, now report contextual `expected grouped expression...` / `expected expression after cast...` messages instead of falling through to generic primary-expression diagnostics.
- [x] Integer-constant cast operand diagnostics: malformed scalar casts inside parser-folded integer constant expressions such as `enum Bad { VALUE = (int) };`, `int values[(int)];`, `case (int):`, and `(int)return` now report contextual `expected integer constant after cast...` messages instead of falling through to generic integer-constant parser errors.
- [x] Integer-constant unary operand diagnostics: malformed unary operators inside parser-folded integer constant expressions such as `enum Bad { VALUE = + };`, `int values[-];`, `case !:`, and `~return` now report contextual `expected integer constant after unary operator '<op>'...` messages instead of falling through to generic integer-constant parser errors.
- [x] Braced scalar initializer invalid-start diagnostics: malformed scalar braced initializer operands such as `int value = {[};`, scalar aggregate-field braced initializers like `.x = {.bad}`, and scalar compound literals like `(int){->field}` now report contextual `expected initializer element in braced scalar initializer...` diagnostics instead of falling through to generic primary-expression parsing.
- [x] Qualifier-only declaration type diagnostics: malformed declaration contexts that consume `const` / `volatile` / bare `_Atomic` but do not provide a supported type, such as `const global;`, `volatile local;`, `_Atomic value` parameters, `struct S { const field; };`, `typedef const Alias;`, and top-level `const make(void)`-shaped starts, now report contextual `expected <declaration/parameter/field/typedef> type after type qualifier '<qualifier>'...` messages instead of falling through to route-specific generic type errors.
- [x] Function-call and initializer selector-token invalid-start diagnostics: malformed selector starts in call argument lists and braced initializer lists, such as `f(.field)`, `f(1,->field)`, `int values[2] = {.field};`, `int values[2] = {->field};`, and `struct Point point = {->field = 1};`, now report contextual `expected function call argument...` / `expected initializer element...` diagnostics instead of falling through to generic primary-expression errors while preserving valid struct `.field` designators.
- [x] Grouped-expression and unary-operator selector-token invalid-start diagnostics: malformed operands such as `(.field)`, `(->field)`, `!.field`, and `*->field` now report contextual `expected grouped expression...` / `expected expression after unary operator...` diagnostics instead of falling through to generic primary-expression errors while preserving valid postfix selector expressions after real operands.
- [x] Array/string index selector-token invalid-start diagnostics: malformed index expressions such as `values[.field]` and `"hi"[->field]` now report contextual `expected array index expression...` diagnostics instead of falling through to generic primary-expression errors while preserving valid selector postfix expressions after real indexed operands.
- [x] Integer-constant selector-token invalid-start diagnostics: malformed array lengths, designator indexes, enum constant values, and switch case labels such as `int values[.field];`, `{[->field] = 1}`, `enum Bad { A = .field };`, and `case ->field:` now report contextual `expected ... before '.'/'->'` diagnostics instead of falling through to generic integer-constant `found Dot/Arrow` errors, while parenthesized integer constants and ordinary selector expressions keep their existing behavior.
- [x] Brace-token parser-trust invalid-start diagnostics: malformed expression/type/integer-constant operand starts such as `_Alignas({1})`, `_Static_assert({1}, "msg")`, `sizeof({1})`, `_Alignof({1})`, `({1})`, `int values[{1}]`, `enum Bad { A = {1} };`, `case {1}:`, `if ({1})`, `for (...; ...; {1})`, `return 1,{2};`, `value = {2};`, `return 1 + {2};`, `return 1 ? {2} : 3`, and `return 1 ? 2 : {3}` now report contextual route-local diagnostics instead of falling through to generic expression/type/integer-constant parsing, while statement blocks and valid braced initializers remain supported.
- [x] Top-level invalid-start declaration diagnostics: malformed file-scope starts such as `{ int value; }`, `,`, `?`, `.field`, and `->field` now report source-located `expected top-level declaration, found ...` diagnostics instead of falling through to the generic `unexpected token at top level: ...` fallback, while unmatched closing delimiters and stray file-scope control-flow diagnostics remain unchanged.
- [x] Pointer-to-array cast/type-query suffix diagnostics: unsupported direct forms such as `(int[2]*)0`, `(struct Point[2]*)0`, `sizeof(int[2]*)`, and `_Alignof(struct Point[2]*)` now report source-located `pointer-to-array casts/sizeof/_Alignof types are not supported` at `*`, matching array-typedef behavior instead of falling through to generic closing-parenthesis diagnostics.
- [x] Void array/function cast/type-query suffix diagnostics: unsupported forms such as `(void(void))0`, `sizeof(void(void))`, `(void[2])0`, and `_Alignof(void[2])` now inspect the suffix after `void` and report source-located function-type or route-specific void-array diagnostics instead of generic cast delimiters or misleading bare-`void` errors, while preserving the dedicated void-pointer boundary.
- [x] Atomic array-typedef argument diagnostics: invalid forms such as `_Atomic(Scores) value`, `sizeof(_Atomic(Scores))`, `_Alignof(_Atomic(Scores))`, and `((_Atomic(Scores)){...})` now report `array _Atomic types are not supported` at the array alias token instead of accepting the alias as an ordinary array.
- [x] Nested/qualified atomic argument diagnostics: invalid `_Atomic(_Atomic(int))`, `_Atomic(const int)`, `_Atomic(int const)`, `_Atomic(int volatile)`, and `_Atomic(int * const)` forms report source-located nested/qualified diagnostics across declarations, `sizeof`, `_Alignof`, and compound-literal routing, while pointee-qualified atomic pointer arguments remain supported.
- [x] Qualified aggregate atomic-alias function-signature conformance: aliases such as `typedef const struct Point ConstPoint` are rejected at the alias token inside `_Atomic(ConstPoint)` object, named-parameter, and unnamed-prototype declarations; aliases of `_Atomic(struct Point)` cannot be wrapped in a second atomic specifier; unqualified pointer aliases to const aggregate pointees, pointer parameters to atomic aggregate aliases, and inner unqualified aggregate alias shadowing remain supported with compiler-oracle coverage.
- [x] Qualified aggregate atomic-alias field/return conformance: `_Atomic(ConstPoint)` and `_Atomic(AtomicPoint)` reject qualified or already-atomic aggregate aliases at the alias token in aggregate fields and function return declarations/definitions, while `_Atomic(Point)`, `_Atomic(View)`, and pointers to atomic aggregate aliases remain supported. Warning-free compiler-oracle coverage uses aggregate-field size relationships; atomic-qualified return coverage remains interpreter-only because GCC rejects ignored return qualifiers under `-Werror`.
- [x] Direct and typedef-backed atomic enum conformance: `_Atomic(enum State)`, `_Atomic(State)`, and atomic enum aliases work in objects, aggregate fields, function parameters, and type queries; qualified enum aliases and aliases of atomic enum types are rejected at the alias token when wrapped in another `_Atomic(...)`; lexical alias shadowing and warning-free ABI-independent compiler-oracle relationships are covered.
- [x] Inline enum definitions inside atomic type specifiers: `_Atomic(enum Tag { ... })` works in global/local objects, aggregate fields, function parameters, `sizeof`, and `_Alignof`; generated enumerators are installed before initializers or function-body use, parameter/block constants do not leak past their lexical scopes, qualified/nested variants keep exact source locations, and compiler-oracle coverage avoids parameter-list visibility warnings and ABI-sensitive enum/atomic layout assumptions.
- [x] Initial test fixtures for valid and invalid programs
- [x] Improve local Docker test automation for repeatable cron runs

## P1 — C subset expansion

- [x] Chained and comma-separated qualified fixed-array typedef parameter conformance; scalar, named-struct, and enum array aliases carried through an additional typedef layer and mixed declarator lists preserve element qualification through unnamed prototypes/named definitions, adjusted pointer slots remain assignable, mutable decay/arguments and writes are rejected exactly, inner aliases may shadow outer aliases, and warning-free GCC/Clang fixtures return 127 with ABI-independent checks.
- [x] Chained const-qualified array typedef compound-literal conformance; scalar (`int`/`char`), named-struct, and enum array aliases carried through an additional typedef layer preserve read-only pointer metadata, full-array and indexed-element `sizeof`, const pointer/parameter flow, pointer arithmetic, exact mutable-pointer/write diagnostics, and lexical shadowing by mutable inner array aliases; warning-free GCC/Clang fixtures return 127 like Cust.
- [x] Address-of aggregate-pointer indexed elements; `&p[i]` for array-backed `struct T *` and `union T *` values produces the same interpreter-owned target as `p + i`, works in pointer declarations/arguments and mutation aliasing, preserves concrete pointee/const metadata and existing bounds diagnostics, and has warning-free compiler-oracle coverage that avoids tautological self-comparison warnings.
- [x] Address-of indexed aggregate elements through non-variable pointer expressions; pointer-returning calls, conditional/comma pointer expressions, direct pointer-valued aggregate fields, and `->` pointer fields support `&expr[i]`, preserve named struct/union pointee and const metadata, alias original aggregate-array storage, retain bounds diagnostics, and have warning-free GCC/Clang native coverage.
- [x] Extend deterministic model-based property coverage to named aggregate pointers and pointer-valued aggregate fields; bounded fixed-seed struct/union-array roots plus direct, arrow, and aggregate-compound-literal field read/assignment/compound-assignment routes now flow through arithmetic/conditional/comma wrappers, compare selected storage/index/type/const behavior and exact bounds/type/const/cross-array diagnostics to a test-side model, and keep all seven fuzz-safety tests below one second locally.
- [x] Extend deterministic pointer modeling to embedded aggregate-array field storage roots. Fixed-seed generation covers named and anonymous struct/union containers with embedded struct/union arrays; direct, arrow, nested-field, indexed-address, and aggregate-compound-literal routes flow through arithmetic/difference/conditional/comma wrappers; the independent model checks owner/path/index/type/const identity and exact cross-owner, field/literal bounds, const-discard, and type diagnostics under `catch_unwind`; all eight fuzz-safety tests stay below one second locally, and warning-free GCC/Clang fixtures return 246 like Cust.
- [x] Extend deterministic pointer modeling to scalar array-field storage roots. Fixed-seed generation now covers `int` and `char` array fields in named/anonymous struct and union containers; direct, arrow, nested, indexed-address, and aggregate-compound-literal decay flows through arithmetic/difference/conditional/comma wrappers; the independent model checks owner/path/index/pointee/const identity plus exact cross-owner/bounds/const-discard/read-only/type diagnostics. The warning-free compiler-oracle fixture returns 56 under Cust, GCC, and Clang, and all nine fixed-seed fuzz-safety tests remain sub-second locally.

- [x] Extend deterministic pointer modeling to hidden scalar-array literal storage roots. Fixed-seed generation covers mutable `int`/`char` array compound literals, const array-typedef compound literals, and read-only string literals through arithmetic/difference/equality/order/conditional/comma wrappers; the independent model checks hidden storage identity, index/value, pointee type, const/read-only metadata, and exact bounds/cross-root/const-discard/write/type diagnostics. Warning-free GCC/Clang fixtures return 65 like Cust, and all ten fuzz-safety tests remain sub-second locally and in Docker.

- [x] Extend deterministic pointer modeling to hidden aggregate-array compound-literal storage roots. Fixed-seed generation covers mutable and const-typedef `struct`/`union` array compound literals through arithmetic/difference/equality/order/conditional/comma wrappers and indexed field reads; the independent model checks hidden storage identity, index/value, concrete aggregate type, const metadata, and exact bounds/cross-root/const-discard/write/type diagnostics. Warning-free GCC/Clang fixtures return 164 like Cust, and all eleven fuzz-safety tests remain sub-second locally.

- [ ] Extend deterministic pointer modeling across pointer-returning function boundaries. Acceptance: generate scalar and named `struct`/`union` pointers returned from functions selecting mutable/const array roots; cover calls nested in arithmetic, indexing/address-of, difference, equality, ordering, conditional, and comma expressions; compare storage identity, index, concrete pointee type, and const metadata; assert exact cross-root, type, const-discard, bounds, and interpreter-only out-of-scope diagnostics; keep native coverage to defined global/static-root operations and retain sub-second local/Docker execution.

- [x] Aggregate-array field path designators in supported struct/union initializers: nested element field paths such as `.points[2].x = 5`, whole aggregate-array element designators such as `.points[1] = {2, 3}`, union aggregate-array field paths such as `.numbers[1].tag = 'C'`, repeated partial element designators, and source-located out-of-bounds diagnostics are now covered by interpreter and compiler-oracle fixtures.
- [x] Function definitions and function calls, including ordinary C parenthesized function declarator names for supported top-level definitions/prototypes and block-scope prototypes (`int (add)(int, int);`, `int (main)(void)`) while preserving function-pointer declarator rejection
- [x] Parenthesized ordinary declarator names for supported object/parameter/field forms; acceptance: `int (value)`, `int *(cursor)`, `int (items)[3]`, `struct Point (point)`, aggregate pointer declarations, declaration-list tails, and aggregate field names parse as the same declarations as their unparenthesized forms while `(*name)` function-pointer/pointer-to-array diagnostics remain targeted, with interpreter and C compiler-oracle coverage.
- [x] Parenthesized typedef declarator names for supported alias forms; acceptance: `typedef int (Count);`, `typedef int *(IntPtr);`, `typedef int (Scores)[3];`, `typedef struct Point (PointAlias), *(PointPtr), (PointArray)[2];` parse as the same aliases as their unparenthesized forms while `typedef int (*Callback)(int);` keeps the existing unsupported function-pointer typedef diagnostic, with interpreter and C compiler-oracle coverage.
- [x] Function prototypes/declarations at top level (`int helper(int);`, `int add(int, int);`, `void mutate(int *);`, `char first(char *);`) with semicolon-terminated parser support, C-style unnamed prototype parameter declarations, signature compatibility checks against definitions/earlier declarations, invalid conflicting-prototype diagnostics, and C compiler-oracle coverage
- [x] `char` function return types and prototypes (`char first(char *text);`) with scalar return-shape diagnostics, `sizeof` call-size support, interpreter fixtures, and C compiler-oracle coverage
- [x] Local function parameters
- [x] Recursive calls with a bounded call-depth limit regression suite
- [x] Boolean/logical operators: `&&`, `||`, `!`
- [x] Unary plus
- [x] `for` loops
- [x] `break` and `continue`
- [x] Empty statements and expression statements
- [x] Assignment expressions for scalar, array-index, and dereferenced pointer lvalues, including right-associative chained assignment and C compiler-oracle coverage
- [x] Conditional operator `?:` with C-style truthiness, right-associative nesting, branch short-circuiting, assignment-expression operands, pointer-truthiness conditions, and compiler-oracle coverage
- [x] `do { ... } while (condition);` loops with guaranteed first body execution, C-style truthiness, `break`/`continue` handling, loop-step budgeting, exact missing-semicolon diagnostics, and compiler-oracle coverage
- [x] Increment/decrement operators (`++`/`--`) or compound assignment (`+=`, `-=`) once selected with precise parser/interpreter acceptance fixtures and C compiler-oracle coverage
- [x] Increment/decrement operators (`++`/`--`) with prefix/postfix semantics for scalar, indexed array/pointer, and dereferenced pointer lvalues plus `for` clause use, non-lvalue diagnostics, and C compiler-oracle coverage
- [x] Multiplicative compound assignments: `*=`, `/=`, and `%=` as assignment-precedence expressions/statements for scalar variables, indexed scalar array/pointer lvalues, and dereferenced pointer lvalues, reusing existing division-by-zero diagnostics and pointer-arithmetic rejection paths, with interpreter and C compiler-oracle coverage
- [x] Bitwise and shift operators: unary `~`, binary `&`, `^`, `|`, `<<`, and `>>` with C precedence, interpreter diagnostics for pointer operands, and C compiler-oracle coverage
- [x] Bitwise/shift compound assignments: `&=`, `|=`, `^=`, `<<=`, and `>>=` as right-associative assignment-precedence expressions/statements for scalar, indexed array/pointer, and dereferenced pointer lvalues, with invalid pointer diagnostics and C compiler-oracle coverage
- [x] Comma operator `left, right` with lowest-precedence parsing, side-effecting left-expression evaluation, right-expression result propagation, pointer/truthiness support, call-argument separator preservation, exact malformed-RHS diagnostic, and C compiler-oracle coverage
- [x] C comma expressions inside supported subscript/index expressions; acceptance: `values[i++, 2]`, `text[i++, 1]`, `points[i++, 2].field`, and `cursor[i++, 1].field` parse as direct comma-expression indices with side effects preserved, while existing missing-bracket diagnostics and array-designator integer-constant parsing remain stable. Interpreter and compiler-oracle fixtures cover the supported subset.
- [x] Scalar cast expressions for supported scalar types and scalar typedef aliases plus one-level pointer cast expressions for supported pointer types and pointer typedef aliases; acceptance: `(int)expr`, `(char)expr`, and `(Count)expr` parse at unary precedence and evaluate the operand as a scalar without adding native-width truncation semantics beyond Cust's existing integer storage model; `(int *)0`, `(const int *)0`, `(IntPtr)(values + 1)`, and `(ConstIntPtr)cursor` parse as pointer expressions over interpreter-owned pointer values, preserve source const-pointee safety, infer pointee type metadata for `sizeof(*(T *)0)`, and include valid/invalid/compiler-oracle fixtures. Unsupported aggregate casts remain targeted diagnostics.
- [x] C-style void cast expressions; acceptance: `(void)expr` parses at unary precedence, evaluates scalar, pointer, aggregate, and void-call operands in discard context so side effects are preserved without scalar conversion, rejects value use with `void expression used as scalar`, and includes valid/invalid/compiler-oracle fixtures
- [x] Switch statements with integer/character `case` labels, `default`, C-style fallthrough, `break` consumption, `continue` propagation to enclosing loops, exact missing-colon diagnostics, enum constants and parser-folded integer constant expressions in `case` labels, duplicate case/default diagnostics, and C compiler-oracle coverage
- [x] Single-statement control-flow bodies and `else if` chains for `if`/`else`/`while`/`do`/`for`, including nearest-`if` dangling-`else` binding, loop control propagation, and C compiler-oracle coverage
- [x] C-style block comments `/* ... */` treated as lexer whitespace across lines/inline token boundaries, with source-line/caret diagnostics for unterminated block comments and C compiler-oracle coverage
- [x] `void` helper functions and empty `return;` statements, including side-effect-only void calls, int/void return-shape diagnostics, scalar-use diagnostics for void calls, and C compiler-oracle coverage
- [x] Top-level global variables for scalar `int`/`char`, arrays, and supported pointer globals, initialized before `main()` in a persistent outer scope, mutable from helper functions, with duplicate-global diagnostics and C compiler-oracle coverage
- [x] `sizeof` over supported types and expressions, with Cust-defined sizes (`int=8`, `char=1`, pointer `=8`), declared scalar/array/pointer element-type tracking, non-evaluating operand semantics, exact `sizeof(void)` diagnostics, interpreter fixtures, and stable C compiler-oracle coverage for char/string/char-array sizes
- [x] Top-level const-qualified function return types: `const int f(void)`, `const char f(void)`, `const Count f(void)`, `const struct Point make(void)`, `const Point make(void)`, `const union Number make(void)`, and `const Number make(void)` parse as ordinary by-value return signatures while `const T *` pointer returns keep pointee-const semantics; covered by interpreter fixtures and design notes because native `-Werror` compiler-oracle builds reject ignored top-level return qualifiers.
- [x] Uninitialized scalar and pointer declarations: `int x;` / `char c;` default to `0`, supported pointer declarations such as `int *p;` default to null, existing array declarations remain zero-initialized, exact missing-`=` diagnostics are preserved for malformed declarations like `int x 1;`, and stable global zero-initialization has C compiler-oracle coverage

- [x] Enum constant declarations, inline enum object declarations, direct named-enum type spellings, inline enum return-type definitions, direct enum aggregate fields, and direct enum compound literals: `enum Tag { A = 1, B, C = -1 };` at top level and inside blocks, inline enum definition object declarations such as `enum { READY = 1 } state = READY;`, `const enum { LOCKED = 7 } global = LOCKED;`, `enum Mode { IDLE = 11 } mode = IDLE;`, inline enum object declarations in `for` initializers and local `static`/`auto`/`register` declaration contexts, top-level inline enum return-type specifiers such as `enum Status { STATUS_READY = 5 } choose(void);` with file-scope enumerator visibility before runtime, direct enum aggregate fields such as `enum State state;`, `const enum State fixed;`, `enum State states[3];`, and `enum State *cursor;` inside supported structs with pointer-field indexing/address-of through `job->cursor[1]` / `&job->cursor[1]`, optional tags, implicit incrementing values, trailing commas, scoped/shadowable integer constants, enum initializers referencing earlier constants with parser-folded integer constant expressions, enum constants usable as same-statement initializers and later scalar expressions, enum constants usable as `switch case` labels with duplicate-label detection, read-only assignment diagnostics for enum constants and const enum fields, malformed-value diagnostics, plus `enum Tag` / `const enum Tag` declarations, globals, parameters, return types, prototypes, scalar and one-dimensional array compound literals such as `(enum Tag){A}` and `(enum Tag[3]){A}`, and `sizeof(enum Tag)` mapped onto Cust's integer storage model with C compiler-oracle coverage
- [x] Inline enum definitions in function parameter type specifiers are visible to the corresponding function body without leaking to file scope; acceptance: `int f(enum Mode { READY = 3 } value) { return READY + value; }` executes by installing the parameter-list enum constants at the start of the function body, while prototypes continue to discard parameter-list inline enum constants instead of registering file-scope enumerators. Interpreter fixture coverage is included; native compiler-oracle coverage is intentionally skipped because `cc -std=c11 -Wall -Wextra -Werror` warns about enum tags declared inside parameter lists.
- [x] Inline enum definitions in cast type specifiers install their enumerators before the enclosing runtime statement; acceptance: `(enum InlineExpr { EXPR_VALUE = 4 })0` works in expression statements, declaration-list initializers, and return expressions, later expressions in the same block can use the generated enum constants, and interpreter plus C compiler-oracle fixtures cover the supported subset.
- [x] Inline enum definitions in control-flow expression contexts, switch `case` label integer constant expressions, ordinary `_Alignof(enum Tag { ... })` type-name expression contexts, assignment-lvalue RHS contexts, function-call argument subexpressions, and pointer/aggregate expression statements install their enumerators before runtime use; acceptance: `(enum IfInline { IF_VALUE = 5 })flag` in `if`, `(enum WhileInline { WHILE_LIMIT = 3 })i < WHILE_LIMIT` in `while`, inline enum definitions in `for` init/condition/increment clauses, `switch ((enum SwitchInline { SWITCH_CASE = 22 })SWITCH_CASE) { case SWITCH_CASE: ... }`, `case sizeof(enum CaseSize { CASE_SIZE = 7 }):`, `_Alignof(enum LocalAlign { LOCAL_ALIGN = 6 })` in scalar assignment statements, declaration-list initializers, return expressions, lvalue-specific statements such as `values[0] = _Alignof(enum ArrayAlign { ARRAY_ALIGN = 3 }) + ARRAY_ALIGN`, `box.field += _Alignof(enum FieldAlign { FIELD_ALIGN = 5 }) + FIELD_ALIGN`, and `*slot = _Alignof(enum DerefAlign { DEREF_ALIGN = 11 }) + DEREF_ALIGN`, call arguments such as `take(sizeof(enum ArgSize { ARG_SIZE = 4 }) ? ARG_SIZE : 0, _Alignof(enum ArgAlign { ARG_ALIGN = 6 }) ? ARG_ALIGN : 0, (enum ArgCast { ARG_CAST = 8 })0 + ARG_CAST)`, plus expression statements such as `(cursor = values + (sizeof(enum PtrExpr { PTR_EXPR_OFFSET = 2 }) ? PTR_EXPR_OFFSET : 0));` and `(point = (struct Pair){ .x = _Alignof(enum AggExpr { AGG_EXPR_VALUE = 4 }) ? AGG_EXPR_VALUE : 0 });` parse and execute without undefined-enumerator runtime failures, with interpreter and C compiler-oracle fixtures covering the supported subset.
- [x] Direct aggregate pointer-field indexing/address-of parity for direct struct variables; acceptance: `job.cursor[0]` and `&job.cursor[1]` on pointer-valued aggregate fields such as `enum State *cursor;` route through safe pointer-field indexing instead of scalar array-field storage, matching existing `job->cursor[1]` behavior with interpreter and C compiler-oracle coverage.
- [x] Preprocessor-free struct first milestone: top-level `struct Name { int x; char y; };` declarations, top-level/local zero-initialized `struct Name value;` variables, scalar member reads/writes with `.`, targeted unknown-field diagnostics, deterministic Cust `sizeof` for structs/fields, design notes in `docs/plans/struct-model.md`, and C compiler-oracle coverage
- [x] Struct copy assignment and field lvalue expressions: same-type `b = a;` value-copy semantics with mismatched-type diagnostics, field assignment expressions (`return p.x = 3;`), field compound assignments (`p.x += 1` and related supported operators), prefix/postfix field increment/decrement, interpreter fixtures, invalid diagnostics, and C compiler-oracle coverage
- [x] Struct by-value function parameters: parser accepts `struct Point p` parameters in definitions/prototypes after prior struct declarations, calls copy same-type struct arguments into callee scope, callee field writes do not mutate caller structs, mismatched/non-struct arguments report targeted diagnostics, and interpreter plus C compiler-oracle fixtures cover the supported subset
- [x] Struct return types: parser accepts `struct Point make(...)` definitions/prototypes after a prior struct declaration, returns clone same-type struct values by value so local return variables remain valid after callee exit, struct-returning calls can be assigned to same-type struct variables, mismatched/empty struct returns report targeted diagnostics, `sizeof(struct_return_call())` uses deterministic Cust struct sizes, and interpreter plus C compiler-oracle fixtures cover the supported subset
- [x] Struct pointers and `->`: parser accepts one-level `struct Point *p` declarations and parameters/prototypes after prior struct declarations, `&point` creates interpreter-owned struct pointers, `p->x` and `(*p).x` read/write scalar fields, field assignment/compound/increment lvalues work through pointers, null/out-of-scope diagnostics are targeted, and interpreter plus C compiler-oracle fixtures cover the supported subset
- [x] Top-level and block-scoped `typedef` aliases: `typedef int Count;`, `typedef char Byte;`, `typedef struct Point Point;`, anonymous aggregate typedef definitions such as `typedef struct { int x; int y; } Point;` and `typedef union { int value; char tag; } Number;`, top-level and block-scoped aggregate typedef definitions such as `typedef struct Point { int x; int y; } Point;` and `typedef union Number { int value; char tag; } Number;`, named enum aliases such as `typedef enum Status Status;` after a prior named enum declaration, anonymous enum typedef definitions such as `typedef enum { READY = 1 } Status;`, one-level pointer aliases such as `typedef int *IntPtr;`, `typedef char *CharPtr;`, and `typedef struct Point *PointPtr;`, one-dimensional array aliases such as `typedef int Scores[3];`, `typedef char Word[4];`, and `typedef Point Points[2];` after the underlying type is declared, plus comma-separated typedef declarator lists such as `typedef int Count, *CountPtr, Counts[3];`, `typedef const int ConstCount, *ConstCountView, ConstCounts[2];`, `typedef struct Point Point, *PointPtr, Points[2];`, and `typedef struct { int value; int extra; } Anon, *AnonPtr, Anons[2];`, resolve at parse time without runtime storage changes; aliases work in globals, locals, arrays, one-level pointer declarations, function prototypes/definitions/parameters/returns where applicable, scalar struct fields, pointer-alias declarations/parameters, enum-alias declarations/parameters/returns, array-alias declarations/parameters with C-style decay, char-array string initializers, and `sizeof(alias)` / `sizeof(pointer_alias)` / `sizeof(array_alias)`, with block-local alias and enum-tag shadowing plus aggregate-tag scope expiry and true block-local aggregate tag shadowing via unique internal type identities, exact malformed typedef diagnostics including pointer-to-pointer alias rejection, pointer-array alias rejection, unknown enum tag rejection, and block enum tag expiry, and C compiler-oracle coverage
- [x] Anonymous aggregate object declarations for supported structs/unions; acceptance: `struct { int x; int y; } point = {1, 2};`, `union { int value; char tag; } number = {5};`, global anonymous aggregate variables, anonymous aggregate `for` initializer variables including qualifier-leading forms such as `for (const struct { int limit; } cfg = {...}, *view = &cfg; ...)` and `for (volatile union { int value; char tag; } number = {...}; ...)`, anonymous aggregate arrays, scalar array fields inside anonymous aggregates, brace/designated initializers, field reads, qualified anonymous aggregate objects such as `const struct { int x; int y; } origin = {2, 3};`, and same-declaration pointer declarator lists such as `struct { int x; int y; } point = {4, 5}, copy = point, *slot = &point;` work through Cust's existing unique internal type identities without installing user-visible tags or typedef aliases, with interpreter and C compiler-oracle coverage.
- [x] Block-scope standalone named aggregate tag definitions for supported structs/unions; acceptance: local `struct Pair { int x; int y; };` and nested-block `union Number { int value; char tag; };` install tag metadata in the current block scope, later same-block declarations such as `struct Pair p = {3, 4};` work through existing aggregate storage/initializer/access paths, inline enum definitions inside local aggregate field lists install usable block-local enumerators, and tags expire when the block ends. Interpreter, invalid out-of-scope, and C compiler-oracle fixtures cover the supported subset.
- [x] Named aggregate definitions with declarators for supported structs/unions; acceptance: `struct Pair { int x; int y; } pair = {1, 2}, *slot = &pair;`, `union Number { int value; char tag; } number = {7};`, global declarations, local declarations, static locals, pointer declarators, comma-separated declarator lists, and inline enum fields after the definition body route through existing aggregate declaration semantics while standalone `struct Pair { ... };` remains supported, with interpreter and C compiler-oracle coverage.
- [x] Inline named aggregate definitions in top-level function return type specifiers, function parameter type specifiers, and expression type contexts; acceptance: `struct Pair { int x; int y; } make_pair(int base);`, `union Number { int value; char tag; } pick_number(int flag) { ... }`, parenthesized function declarator routing after the inline return definition, and const-qualified aggregate return-type parsing register return-type tags before function declarations/bodies and reuse existing by-value aggregate return semantics; parameter-list definitions such as `int sum(struct Pair { int x; int y; } p) { struct Pair copy = p; ... }` keep the tag visible while parsing the function body but do not leak it to later file-scope declarations; expression type contexts such as `((struct ExprPoint { int x; int y; }){3, 4}).x`, later same-block `struct ExprPoint` declarations, matching `union` compound literals, and `sizeof(struct SizeBox { ... }) == sizeof(struct SizeBox)` work through the existing parser/type metadata path. Interpreter and C compiler-oracle fixtures cover the return-type and expression-type subsets; top-level const return coverage and parameter-list aggregate definitions are interpreter-only because native `-Werror` treats const aggregate returns as ignored qualifiers and warns on parameter-list tag visibility.
- [x] Const-qualified non-pointer and one-level pointer typedef aliases preserve read-only metadata: `typedef const int ConstInt;`, `typedef const char ConstChar;`, `typedef const struct Point ConstPoint;`, `typedef const int Scores[3];`, `typedef const int *ConstIntView;`, `typedef const struct Point *ConstPointView;`, `typedef int * const ConstIntSlot;`, and `typedef struct Point * const PointSlot;` create parser aliases whose later alias-spelled declarations/parameters reuse existing const scalar, aggregate, array, pointee, and pointer-slot enforcement after initialization, with invalid assignment/const-discard diagnostics and C compiler-oracle coverage.
- [x] First-pass `const` qualifiers for leading `const int` / `const char` scalar declarations, zero-initialized arrays, and scalar function parameters; const variables remain readable in normal expressions but direct assignment, compound assignment, increment/decrement, parameter mutation, and writes through scalar pointers are rejected, while const arrays use read-only array storage and reject indexed/pointer writes with interpreter plus C compiler-oracle coverage
- [x] `sizeof(const type)` parsing for supported scalar, typedef-alias, and pointer type forms; acceptance: `sizeof(const int) == sizeof(int)`, `sizeof(const char) == sizeof(char)`, aliases such as `Count`/`Pair` preserve their underlying size, pointer spellings such as `sizeof(const int *)` and pointer typedef aliases report deterministic pointer size, `sizeof(const void)` preserves the targeted void diagnostic, and native compiler-oracle coverage avoids ABI-sensitive exact sizes
- [x] Const-qualified pointer semantics beyond size/type contexts; acceptance: `const int *p` / `const char *p` / `const struct Point *p` create read-only pointer views that reject writes through `*p`, `p[i]`, `p->field`, `(*p).field`, compound assignment, and increment/decrement while allowing pointer reassignment; `int * const p` / `char * const p` / `struct Point * const p` create non-reassignable pointer slots that may still mutate mutable targets; `const int * const p` combines both; pointer conversions preserve pointee constness by allowing mutable-to-const pointer flow while rejecting const-to-mutable declarations/assignments/arguments; parser/interpreter metadata is documented in `docs/plans/const-pointer-model.md`; valid/invalid interpreter fixtures and C compiler-oracle coverage verify the supported subset
- [x] Const-qualified struct variables and by-value parameters for the scalar-field struct subset; acceptance: `const struct Point p;` / typedef-spelled `const Point p;` create read-only zero-initialized struct bindings, `int f(const struct Point p)` receives a by-value read-only parameter copy, direct field/copy assignment to const struct bindings reports `cannot assign to const variable '<name>'`, writes through const struct pointers or pointers to const struct targets report `cannot assign through pointer to const`, struct-pointer const-discard conversions are rejected, and valid/invalid/compiler-oracle fixtures cover the supported subset
- [x] Const-qualified scalar and aggregate struct fields; acceptance: `struct Config { const int magic; const char marker; int value; };` records field-level read-only metadata, reads work through direct variables and struct pointers, mutable sibling fields remain writable, field assignment/compound/increment and pointer writes to const scalar fields report `cannot assign to const struct field '<field>'`, whole-struct copy assignment into struct types with const fields reports `cannot assign to struct '<Type>' with const fields`, and const-qualified named/anonymous aggregate fields such as `const struct Point point;` / `const struct { int x; } point;` reject nested writes like `box.point.x = 3` with `cannot assign to const struct field 'point'`. Valid/invalid/compiler-oracle fixtures cover the supported subset
- [x] Const-qualified aggregate-array fields reject nested scalar writes and aggregate element copy-assignment through direct variable-backed, struct-pointer-backed, and aggregate compound-literal paths; acceptance: `struct Box { const struct Point points[2]; }; box.points[1].x = 9;`, `((struct Box){{...}}).points[1].x = 9`, and `slot->points[0] = replacement` all report `cannot assign to const struct field 'points'` without evaluating compound-literal initializers for diagnostic classification, while preserving the existing pointer-to-const safety model for other const pointer expressions.

- [x] Static storage-class parsing for supported top-level declarations; acceptance: lexer recognizes `static`, parser accepts it before supported global scalar/array/pointer/struct declarations plus function prototypes/definitions, and interpreter treats it as single-file linkage metadata with the same runtime behavior as ordinary globals/functions, with interpreter plus compiler-oracle fixtures covering the supported subset
- [x] Persistent local `static` storage for supported local declarations; acceptance: block/function-scope `static` scalar, pointer, array, and struct locals initialize once, persist across calls, keep lexical/block visibility, can be addressed safely after the declaring function returns through Cust's interpreter-owned pointer model, preserve const/read-only enforcement where applicable, and interpreter plus compiler-oracle fixtures cover the supported subset
- [x] Empty `void` parameter lists in function definitions and prototypes; acceptance: `int main(void)`, `int helper(void);`, and `void side_effect(void)` parse as zero-argument signatures, compatible declarations/definitions compare as empty parameter lists, malformed named/extra `void` parameters report a targeted diagnostic, and interpreter plus compiler-oracle fixtures cover the supported subset
- [x] Scalar brace initializers for one-dimensional arrays; acceptance: `int values[3] = {1, 2};`, `char bytes[4] = {1, 0, 2};`, `static int seen[3] = {4, 5};`, and `const int table[2] = {8, 9};` initialize declared elements left-to-right, zero-fill omitted trailing elements, preserve read-only const arrays and persistent static storage, accept trailing commas, reject excess initializers with `too many initializers for array '<name>'`, and include interpreter plus C compiler-oracle coverage
- [x] Scalar brace initializers for supported structs; acceptance: `struct Point p = {1, 2};`, `const struct Config c = {7, 8};`, and `static struct Point saved = {3, 4};` initialize scalar fields in declaration order, evaluate initializer expressions, zero-fill omitted trailing fields, preserve const field/read-only metadata after initialization, reject excess entries with `too many initializers for struct '<Type>'`, and include interpreter plus C compiler-oracle coverage
- [x] Braced scalar initializer expressions for scalar contexts; acceptance: `int x = {expr};`, scalar entries in array initializers such as `int values[3] = {{1}, {2}, [2] = {3}};`, scalar struct/union fields such as `struct Pair p = {{1}, {2}};`, and scalar path designators such as `.field = {expr}` parse as the contained expression with side effects preserved; trailing commas are accepted. Compiler-oracle coverage is intentionally skipped because `cc -std=c11 -Wall -Wextra -Werror` rejects these otherwise-C-compatible forms with `-Werror=braces`.

- [x] Nested struct fields for prior named struct types; acceptance: `struct Rect { struct Point origin; int width; };` stores nested field maps, `rect.origin.x` reads/writes scalar nested fields, nested fields can be copied and passed by value, and deterministic recursive `sizeof(rect.origin)` uses Cust's no-padding size model. Regression coverage includes `tests/fixtures/valid/nested_struct_fields.c`, invalid `nested_struct_unknown_field.c`, and compiler-oracle fixture `tests/fixtures/compat/valid/nested_struct_fields.c`.
- [x] Nested struct brace initializers; acceptance: `struct Rect r = {{1, 2}, 3};` recursively initializes prior named nested struct fields, zero-fills omitted nested fields, supports global/local/static/const struct declaration contexts, rejects excess nested entries with `too many initializers for struct '<Nested>'`, and includes interpreter plus C compiler-oracle fixtures.
- [x] One-dimensional scalar array fields inside structs; acceptance: `struct Packet { int values[3]; char tag[2]; };` stores array field storage with deterministic Cust element sizes, `packet.values[i]` reads/writes scalar elements, element assignment/compound/increment lvalues work, brace initializers recursively initialize array fields with omitted-element zero-fill and excess-entry diagnostics, same-type struct copy and by-value parameters deep-copy array fields, and interpreter plus C compiler-oracle fixtures cover the supported subset.

- [x] Pointer fields inside structs as the next scoped aggregate/pointer milestone; acceptance: `struct Node { int *external; struct Node *next; };` supports one-level scalar/struct pointer fields with interpreter-owned pointer metadata, self-referential struct pointer fields, pointer-field initializers/reassignment, direct struct pointer-field array-backed pointer arithmetic (`cursor.p - 1`), compound pointer assignments (`cursor.p += 2`), prefix/postfix pointer increments/decrements (`++cursor.p`, `cursor.p--`), struct-pointer arrow access to pointer fields with matching pointer arithmetic/reassignment (`slot->p += 3`, `slot->p--`, `slot->p = slot->p - 2`), concrete pointee type checks for direct and arrow pointer-field assignment, `node.next->value` and `*node.external` use, const-pointee conversion checks for `const T *field`, targeted pointer-to-pointer/pointer-array field rejection, updated struct/pointer model docs, and valid/invalid/compiler-oracle fixtures without native ABI layout assumptions.
- [x] Arrays of supported struct types; acceptance: `struct Point points[2];` stores zero-initialized struct elements with deterministic Cust layout, supports element field access `points[i].x`, field assignment/compound/increment lvalues, brace initializers for arrays of structs, copy/by-value semantics with deep-cloned struct elements and embedded arrays while pointer fields copy pointer values, targeted bounds/initializer diagnostics, `sizeof(points)` / `sizeof(points[i])` in Cust's deterministic model, and compiler-oracle coverage that avoids native struct `sizeof` assumptions.
- [x] Address-of struct-array elements and struct fields; acceptance: `struct Point *p = &points[0];`, `&point.x`, nested field paths such as `&packet.anchor.y`, and `&points[i].x` produce interpreter-owned pointers that alias the original storage, preserve null/out-of-scope/const diagnostics, and include valid/invalid/compiler-oracle fixtures without enabling pointer-to-pointer or unsafe host addresses.
- [x] Designated initializers for supported one-dimensional scalar arrays, supported structs, and one-dimensional arrays of supported structs/unions; acceptance: array entries such as `{[2] = 5, [0] = 1}` initialize specific scalar elements with omitted zero-fill and mixed positional continuation, struct entries such as `{.y = 2, .x = 1}` initialize fields out of order, nested aggregate brace lists support their own array/struct designators, path designators such as `.inner.x = 1` and `.values[1] = 2` initialize nested struct fields and scalar array-field elements in source order while merging sibling nested fields, aggregate-array designators such as `struct Point points[3] = {[2] = {.x = 5}, [0] = {1, 2}};` and `union Number numbers[3] = {[1] = {.tag = 7}};` initialize selected struct/union array elements while omitted elements stay zero/default initialized, invalid array/path/aggregate-array designator bounds and unknown struct fields report targeted diagnostics, design notes live in `docs/plans/designated-initializers.md`, and interpreter plus C compiler-oracle fixtures cover the supported subset.
- [x] Scoped `union` model as a substantial aggregate follow-up; acceptance: `docs/plans/union-model.md` documents deterministic no-ABI-padding storage semantics, named scalar-field union declarations are supported, union variables/arrays/nested struct fields/by-value parameters/direct union-returning prototypes and definitions can be zero-initialized, initialized with one first-field brace initializer, or initialized from same-type aggregate-returning call expressions, scalar member reads/writes over root variables, array elements, nested union field paths, and union pointer targets share one logical scalar value, one-level union pointers and pointer fields inside unions (including self-referential union pointers and scalar pointer fields) use Cust's interpreter-owned pointer metadata, `sizeof(union_variable)` reports the maximum field size, excess initializers and pointer-to-pointer union fields report targeted diagnostics, and valid/invalid/compiler-oracle fixtures cover the supported subset.
- [x] Aggregate-valued conditional and comma expressions; acceptance: `struct Point p = flag ? a : b;`, `union Number n = (side_effect(), right);`, aggregate copy assignment from `?:`, and nested aggregate initializer entries from comma expressions evaluate only the selected/right aggregate expression, preserve by-value copies, and include a mismatched selected-branch diagnostic plus C compiler-oracle coverage.
- [x] Aggregate-valued assignment expressions for supported structs and unions; acceptance: same-type variable assignments such as `struct Point copy = (target = source);` and aggregate pointer dereference assignments such as `union Number picked = (*slot = replacement);` mutate the destination and return a by-value copy of the assigned aggregate, preserving const-field/const-pointee enforcement, same-type mismatch diagnostics, and compiler-oracle coverage.
- [x] Direct aggregate type spellings in `sizeof(...)`; acceptance: `sizeof(struct Name)`, `sizeof(union Name)`, `sizeof(const union Name)`, `sizeof(struct Name *)`, and `sizeof(union Name *)` parse as type operands with Cust's deterministic struct sum / union max-field / pointer sizes, while compiler-oracle coverage checks stable C-compatible size relationships without depending on native struct padding.
- [x] Type-aware `sizeof` for dereferenced pointer-valued expressions; acceptance: `sizeof(*(char_ptr + 1))`, `sizeof(*pointer_returning_call(...))`, `sizeof(*(&points[0].x))`, `sizeof(*(struct_ptr + 1))`, and `sizeof(*(cond ? left : right))` infer the pointed-to scalar/aggregate type without evaluating side-effectful operands, preserving Cust's deterministic `char`/aggregate sizes and compiler-oracle coverage.
- [x] One-dimensional array type-name operands in `sizeof(...)`; acceptance: `sizeof(int[3])`, `sizeof(char[4])`, `sizeof(const int[2])`, `sizeof(struct Pair[2])`, and aggregate typedef spellings such as `sizeof(Number[3])` report deterministic element-size-times-length results without runtime evaluation, keep pointer-array and multidimensional array type forms outside the supported subset with targeted diagnostics, and include interpreter plus C compiler-oracle coverage.
- [x] C99-style aggregate, scalar, scalar-array, aggregate-array, and addressable scalar/aggregate compound literals; acceptance: `(struct Point){1, 2}`, `(Point){.x = 1, .y = 2}`, and `(union Number){.tag = 9}` parse as rvalue aggregate expressions, reuse existing aggregate initializer semantics, work in declaration initializers, assignment expressions, by-value arguments, aggregate returns, conditional/comma aggregate contexts, and scalar field reads such as `((Number){12}).tag`; `(int){expr}`, `(char){expr}`, and scalar typedef compound literals parse as scalar expressions with optional trailing commas, preserve initializer side effects, remain non-evaluating under `sizeof`, support scalar compound-literal lvalue operations including direct assignment, compound assignment, and prefix/postfix increment/decrement, and support aggregate compound-literal scalar-field lvalue operations such as `((struct Point){1, 2}).x = 7`, compound assignment, and prefix/postfix increment/decrement while rejecting const-field writes; `&(int){7}` and `&(struct Point){.x = 1}` create hidden mutable current-scope storage and return interpreter-owned pointers that support dereference writes, pointer parameters, aggregate `->` access, and existing type/const conversion checks; `(int[]){1, 2}`, `(char[]){'a', 0}`, `(char[]){"cat"}`, fixed-size `(int[3]){...}`, and one-dimensional scalar/char array typedef compound literals such as `(Scores){1, 2, 3}` / `(Word){"cat"}` create mutable interpreter-owned array storage, evaluate as pointer expressions to element zero in ordinary pointer contexts, support positional/designated/string initializers with inferred unsized lengths or checked fixed lengths, report full array-object size in non-evaluating `sizeof`, and have valid/invalid/compiler-oracle coverage; `(struct Point[]){{1, 2}, {.x = 3}}`, `(union Number[]){{7}, [2] = {.value = 9}}`, fixed-size `(struct Point[3]){...}`, and one-dimensional aggregate array typedef compound literals such as `(Points){{1, 2}, {.x = 3}}` create mutable interpreter-owned aggregate array storage in the current scope, evaluate as pointers to element zero in ordinary pointer contexts, support positional/designated aggregate initializers with inferred unsized lengths or checked fixed lengths, and have valid/invalid/compiler-oracle coverage; addressable scalar/aggregate compound literals allocate hidden current-scope storage and preserve interpreter-owned pointer safety.
- [x] C99-style const-qualified array typedef compound literals preserve read-only pointer metadata; acceptance: `typedef const int Scores[2]; (Scores){1, 2}` and `typedef const struct Point Points[2]; (Points){{...}}` evaluate as const array/aggregate-array compound literals, flow into `const T *` pointers and parameters, reject mutable `T *` conversion with `cannot discard const qualifier from pointer target`, reject writes through const pointer views, and include interpreter plus C compiler-oracle coverage.
- [x] C integer literal base spellings for decimal, octal, and hexadecimal constants; acceptance: `052`, `0x2a`, and `0X10` lex as scalar integer constants usable anywhere numeric literals are already accepted, invalid octal digits such as `08` report a targeted lexer diagnostic with source/caret context, out-of-range base-prefixed literals preserve the existing out-of-range diagnostic, and interpreter plus C compiler-oracle fixtures cover supported behavior.
- [x] C integer literal suffix spellings for the supported deterministic integer model; acceptance: decimal, octal, and hexadecimal constants accept `u`/`U`, `l`/`L`, `ll`/`LL`, and unsigned-long combinations such as `UL`, `lu`, `uL`, and `LLU`, preserving the parsed numeric value as Cust's existing `i64` scalar storage, with interpreter and C compiler-oracle fixture coverage.
- [x] C signed/unsigned integer, character, long, short, long-long, `_Bool`, local `auto`/`register` storage-class, top-level `extern` function/object storage-class, C11 `_Thread_local` storage-class, one-dimensional array typedef aliases, `volatile` type qualifiers, C11 `_Atomic` type qualifier/specifier syntax, C99 `restrict` pointer qualifiers, top-level function specifier spellings, const-qualified typedef aliases, postfix/interleaved const qualifier orderings, permuted supported scalar type specifier orders such as `int unsigned`, `char signed`, and `int long long unsigned`, and C11 `_Alignas` declaration specifiers parse as syntax parity over Cust's deterministic type/function/storage model. `_Thread_local` is accepted as parser-level no-op metadata for supported top-level object declarations and `static _Thread_local` globals/locals in Cust's deterministic single-thread interpreter model. `_Atomic` is accepted as parser-level no-op metadata in bare qualifier form and `_Atomic(type-name)` specifier form without adding native atomic operations or storage-layout changes. `_Alignas(type-name)` / `_Alignas(expression)` is accepted as parser-level no-op metadata on supported object declarations, static locals, `for` initializer declarations, and aggregate fields without changing Cust's deterministic layout. `restrict` is accepted as a runtime no-op on supported post-star pointer declarations, parameters, and aggregate fields such as `int * restrict p`, `int * const restrict p`, and `int * volatile restrict p`, while preserving existing `const` metadata and C compiler-oracle coverage; leading non-pointer uses such as `restrict int value`, `int f(restrict int value)`, and `struct Bad { restrict int value; };` now report targeted pointer-declarator-only diagnostics. `inline` and `_Noreturn` are accepted as parser-level no-op metadata on function prototypes/definitions, with `static inline` helper definitions and an unused `_Noreturn` prototype covered by compiler-oracle fixtures. Postfix const spellings such as `int const`, `unsigned const int`, `struct Point const`, and `typedef int const Alias` preserve the same read-only metadata as leading `const` forms.
- [x] Storage/alignment-qualified anonymous aggregate declaration-context parity: qualifier-leading anonymous aggregate object declarations behind local `static`, local `_Thread_local`, `auto`/`register`, and `_Alignas` routing reuse `parse_aggregate_var_decl()` instead of falling into scalar declaration parsing. Acceptance includes `_Alignas(8) volatile union { ... } scratch`, `static _Alignas(8) const struct { ... } tuned, *view = &tuned`, top-level `_Thread_local const struct { ... } global_shape`, and local `static _Thread_local const struct { ... } local_shape`, preserving anonymous type identity, static-local wrapping, same-declaration pointer declarators, const pointer-view metadata, and C compiler-oracle coverage.
- [x] Block-scope `extern` object declarations as parser-only syntax over Cust's single-file global storage model; acceptance: `extern int total;`, `extern char marker;`, `extern int values[3];`, `extern struct Point origin;`, `extern union Number number;`, and `extern int *cursor;` inside blocks do not create shadowing local storage, so reads and writes continue to resolve to existing global bindings, while initialized block-scope extern declarations report `extern local declarations cannot have initializers`; covered by interpreter and C compiler-oracle fixtures.
- [x] Block-scope function prototype declarations as parser-only syntax parity over Cust's existing top-level function table; acceptance: local declarations such as `int add(int, int);`, `extern char pick(char, char);`, `extern struct Point make_point(int, int);`, `union Number make_number(int);`, and array-parameter prototypes parse inside function blocks as no-op statements while calls still resolve to top-level Cust functions, with interpreter and C compiler-oracle coverage.
- [x] C11 `_Static_assert(condition, "message")` syntax parity at top level and block scope; acceptance: top-level assertions run before `main()` through the existing global-statement path, block assertions execute in statement order, true assertions continue normally, false assertions report `static assertion failed: <message>`, assertion conditions use existing Cust scalar/truthiness semantics with assignment-precedence parsing so the separating comma is preserved, inline enum type definitions inside assertion conditions emit their pending enum constants before runtime assertion evaluation and later statements, and interpreter plus C compiler-oracle fixtures cover the supported subset.
- [x] C11 `_Alignof(type-name)` syntax parity for supported deterministic type metadata; acceptance: `_Alignof(char)`, `_Alignof(_Bool)`, `_Alignof(int)`, qualified scalar aliases, one-level pointer type names, one-dimensional scalar/aggregate array type names, and direct/typedef aggregate type names parse as non-evaluating type-query expressions; Cust reports deterministic alignment (`char`/`_Bool` = 1, `int`/integer aliases = 8, pointer = 8, arrays use element alignment, structs/unions use max field alignment), rejects `_Alignof(void)` with a targeted diagnostic, and includes interpreter plus ABI-independent C compiler-oracle coverage.
- [x] Anonymous aggregate type-name operands in `sizeof(...)` and `_Alignof(...)`; acceptance: `sizeof(struct { int x; char tag; })`, `sizeof(union { int value; char tag; })`, `sizeof(const struct { ... })`, `sizeof(struct { ... } *)`, `sizeof(struct { char c; }[3])`, and `_Alignof(struct/union { ... })` parse through shared anonymous aggregate field-list parsing without installing a source-level tag/alias, use Cust's deterministic no-padding aggregate size and max-field alignment model, and include interpreter plus ABI-independent C compiler-oracle relationship coverage.
- [x] Anonymous aggregate compound literals for expression-local C99 type names; acceptance: `((struct { int x; int y; }){.x = 2, .y = 5}).x`, `((union { int value; char tag; }){7}).value`, and anonymous aggregate-array compound literals such as `((struct { int x; int y; }[]){{1, 2}, {.y = 9}})[1].y` parse by creating a unique internal anonymous aggregate type identity, reuse existing aggregate/aggregate-array initializer and pointer/indexing semantics, do not install source-level tags or typedef aliases, and include interpreter plus C compiler-oracle coverage.
- [x] Anonymous aggregate pointer casts for expression-local C99 type names; acceptance: `(struct { int x; } *)0`, `(const union { char tag; } *)0`, and `sizeof(*(struct { char tag; } *)0)` parse by creating a unique internal anonymous aggregate type identity and lowering to Cust's existing safe one-level pointer-cast metadata path, preserve leading const as pointee-const metadata, do not install source-level tags or typedef aliases, and include interpreter plus C compiler-oracle coverage using null comparisons and non-evaluating `sizeof` rather than incompatible distinct anonymous type assignments.
- [x] Anonymous aggregate fields inside supported aggregate definitions; acceptance: `struct Box { struct { int x; int y; } point; union { int value; char tag; } number; struct { int value; } items[2]; };` parses field-local anonymous aggregate definitions into unique internal type identities without installing source-level tags or typedef aliases, reuses existing nested aggregate field/array initializer and field-access semantics, preserves distinct anonymous type incompatibility for separately spelled pointer types, and includes interpreter plus C compiler-oracle coverage.
- [x] Nested named aggregate field definitions inside supported aggregate definitions; acceptance: `struct Scene { struct Point { int x; int y; } origin, cursor; union Number { int value; char tag; } primary, secondary; struct Segment { struct Point start; struct Point end; } segments[2]; };` parses field-local named `struct`/`union` definitions before their field declarator lists, installs reusable tag metadata for later fields in the same aggregate definition, and reuses existing nested aggregate field, array-field, initializer, access, mutation, and C compiler-oracle paths.
- [x] Inline enum field definitions inside supported aggregate definitions; acceptance: `struct Flags { enum State { STATE_READY = 3, STATE_DONE = 7 } state; enum { MODE_FAST = 11 } mode; };` and aggregate typedef forms such as `typedef struct { enum { TYPE_VALUE = 17 } code; } TypeHolder;` parse enum specifiers in field type positions as scalar integer fields, install their enumerators into the enclosing runtime scope before later global initializers/functions use them, append multiple inline enum field definitions without overwriting pending constants, and include interpreter plus C compiler-oracle coverage.
- [x] Standard C simple escape sequences in character and string literals; acceptance: `\\a`, `\\b`, `\\f`, `\\r`, `\\v`, and `\\?` now join the already-supported `\\n`, `\\t`, `\\0`, `\\\\`, `\\'`, and `\\\"` escapes, lex to their C byte values in scalar character literals and NUL-terminated string literals, preserve unsupported-escape diagnostics for other escapes, and include interpreter plus C compiler-oracle fixtures.
- [x] C numeric escape sequences in character and string literals; acceptance: octal escapes such as `\\101` consume up to three octal digits, hexadecimal escapes such as `\\x2a` consume one or more hexadecimal digits, values are stored in Cust's deterministic integer byte model for character literals and NUL-terminated string literals, missing hexadecimal digits report a targeted lexer diagnostic with source/caret context, and interpreter plus C compiler-oracle fixtures cover supported behavior.
- [x] Fixed-size `char` array initialization from string literals; acceptance: `char word[4] = "cat";` copies the NUL terminator when it fits, `char exact[3] = "dog";` accepts C's exact-size non-NUL-terminated initialization, shorter strings zero-fill trailing elements, const/static char arrays preserve existing read-only/persistent storage behavior, too-long initializer strings report `initializer string for char array '<name>' is too long`, and interpreter plus C compiler-oracle fixtures cover supported behavior while avoiding native `-Werror=unterminated-string-initialization` forms in the oracle.
- [x] Inferred-length scalar array declarations with initializers; acceptance: `int values[] = {1, 2, [4] = 5, 6};`, `char word[] = "cat";`, and `const int table[] = {[1] = 3, [3] = 4};` infer the declared object length from positional entries, designator indices, or string literal bytes including the NUL terminator, lower to the existing fixed-length scalar array runtime path for zero-fill, `sizeof`, pointer/array-parameter decay, and const enforcement, and reject initializer-less `int values[];` with a targeted missing-initializer diagnostic. Interpreter and compiler-oracle fixtures cover the supported subset.
- [x] Inferred-length aggregate array declarations with initializers; acceptance: `struct Point points[] = {{1, 2}, {.y = 4}, [3] = {5, 6}};`, `const struct Point fixed[] = {{7, 8}, {.x = 9}};`, and `union Number numbers[] = {{3}, [2] = {.value = 5}};` infer lengths from positional/designated aggregate entries, lower to existing fixed-length aggregate-array storage for zero-fill, `sizeof`, pointer decay/arithmetic, const enforcement, and mutation aliasing, and reject initializer-less `struct Point points[];` with `expected '=' after inferred aggregate array declaration`. Interpreter and compiler-oracle fixtures cover the supported subset.
- [x] Comma-separated scalar, pointer, array, and aggregate declaration lists as a scoped ordinary-C declaration syntax milestone; acceptance: scalar lists such as `int a = 1, b, c = a + 2;`, pointer lists such as `int *p = values, *q = values + 1;`, scalar array lists such as `int a[2] = {1, 2}, b[2];`, aggregate variable lists such as `struct Point p = {1, 2}, q = p;`, aggregate array lists such as `struct Point points[2] = {{1, 2}}, empty[1];`, const pointer lists such as `const int *view = values + 1, *start = values;`, static locals, globals, locals, and `for (int i = 0, j = 3; ...)` parse as same-scope declaration lists. Omitted initializers zero/default initialize each declarator, const/pointee metadata applies to every declarator, declaration-list initializers preserve comma separators by parsing assignment-precedence expressions, and interpreter plus C compiler-oracle fixtures cover the supported subset.
- [x] Direct aggregate-array element copy assignment for supported struct/union arrays and aggregate pointers; acceptance: `points[0] = replacement`, `struct Point returned = (points[0] = replacement)`, and `cursor[0] = (struct Point){...}` deep-copy same-type aggregate RHS values into the selected element, return a by-value copy in aggregate expression contexts, preserve copy isolation and const/read-only target diagnostics, and include interpreter plus C compiler-oracle fixtures.
- [x] Embedded aggregate-array element copy assignment through supported struct fields and struct pointers; acceptance: `line.points[1] = replacement`, `struct Point returned = (line.points[1] = replacement)`, and `slot->points[0] = (struct Point){...}` deep-copy same-type aggregate RHS values into the embedded aggregate-array element, return a by-value copy in aggregate expression contexts, preserve mismatch and const/read-only diagnostics, and include interpreter plus C compiler-oracle fixtures.

- [x] Fixed-size `char` array initialization from string literals inside supported aggregate fields; acceptance: struct/union `char[N]` fields can use positional string-literal initializers, field designators such as `.text = "hi"`, nested path designators such as `.label.text = "A\\x2a"`, and struct-array element initializers, preserving zero-fill, exact too-long diagnostics, and C compiler-oracle coverage.
- [x] Scalar array-field decay and `&field[index]` parity for direct struct variables and struct-array elements from the 2026-05-09 autonomous run, including parser lowering, pointer eval helpers, and native exit-code pitfall.
- [x] Arrow (`->`) scalar array-field decay/index/address-of parity for struct pointers; acceptance: `slot->values` and `slot->label` decay to existing scalar pointer/array parameter paths, `slot->values[i]` reads/writes embedded array elements, `&slot->values[i]` creates aliasing array-element pointers, const struct-pointer views reject mutable pointer decay, and interpreter plus C compiler-oracle fixtures cover the supported subset.
- [x] Nested scalar array-field decay and `&field[index]` parity through nested struct fields; acceptance: `one.inner.values`, `boxes[i].inner.values`, and `ptr->inner.values` decay to existing scalar pointer/array parameter paths, matching `&...values[j]` forms create aliasing array-element pointers, const root structs and const struct arrays preserve pointer-to-const conversion diagnostics, and interpreter plus C compiler-oracle fixtures cover the subset.

- [x] Address-of dereference pointer identity expressions for the supported one-level pointer subset; acceptance: `&*p`, `&*(values + 1)`, and `&*null_ptr` preserve the underlying interpreter-owned pointer metadata without dereferencing the pointee, work in pointer declarations/parameters, and include interpreter plus C compiler-oracle coverage for warning-free non-null native cases.
- [x] Aggregate-array fields inside supported structs; acceptance: `struct Line { struct Point points[2]; };` stores zero/default initialized embedded struct-array elements, recursively brace-initializes positional/designated element fields, supports scalar field reads and lvalue assignment/compound assignment through `line.points[i].field`, preserves deep-copy/by-value isolation for containing structs, and includes interpreter plus compiler-oracle fixtures without native struct layout assumptions.
- [x] Embedded aggregate-array field pointer decay and element address-of; acceptance: `line.points` decays to `struct Point *` in pointer initialization/parameter contexts, `&line.points[i]` returns an aliasing aggregate pointer, pointer arithmetic such as `line.points + 2` remains bounded over the embedded field elements, callee-side `points[i].field` reads/writes mutate the containing struct storage, const containing structs reject mutable pointer flows, and interpreter plus C compiler-oracle fixtures cover the supported subset.
- [x] Struct-pointer embedded aggregate-array field pointer decay/address-of parity; acceptance: `slot->points` decays to `struct Point *` in pointer initialization/parameter contexts, `&slot->points[i]` returns an aliasing aggregate pointer, `slot->points + n` remains bounded over embedded field elements, `slot->points[i].field` reads/writes through the containing struct pointer, const struct-pointer views reject mutable aggregate-pointer flows, and interpreter plus C compiler-oracle fixtures cover the supported subset.
- [x] Char-array string-literal initializers in scalar-array compound literals; acceptance: `(char[]){"cat"}` and fixed-size forms allocate mutable current-scope array compound-literal storage using the existing string-literal byte/NUL rules, infer unsized length from the literal including the NUL terminator, reject too-long fixed-size strings with `initializer string for char array compound literal is too long`, and include interpreter plus C compiler-oracle coverage.
- [x] Nested embedded aggregate-array field pointer decay/address-of fixture coverage; acceptance: `box->inner.points`, `&box->inner.points[i]`, and `box->inner.points + n` exercise nested aggregate field paths through a struct pointer, preserve mutation aliasing and const-discard diagnostics, and are covered by interpreter plus C compiler-oracle fixtures.
- [x] `sizeof` array compound literals and embedded union-array field pointer parity; acceptance: `sizeof((char[]){...})`, `sizeof((int[3]){...})`, and `sizeof((struct Point[2]){...})` report full array-object sizes without evaluating initializer side effects, while direct and struct-pointer embedded union-array fields (`bag.numbers`, `bag->numbers`, `&bag.numbers[i]`, `&bag->numbers[i]`, and pointer arithmetic) are locked in with interpreter and compiler-oracle fixtures plus const-discard diagnostics.
- [x] Pointer-valued field reads on aggregate compound literals; acceptance: supported pointer fields such as `((struct Cursor){values + 1}).p` evaluate as pointer expressions in pointer declarations, function calls, dereference, pointer arithmetic, and equality contexts, preserve pointee type and const metadata for conversion checks, and include interpreter plus C compiler-oracle coverage.
- [x] Array-valued field decay on aggregate compound literals; acceptance: scalar array fields and embedded aggregate-array fields such as `((struct Packet){{1, 2, 3}}).values`, `((struct Packet){{1, 2, 3}}).values[1]`, `&((struct Packet){{1, 2, 3}}).values[1]`, `((struct Line){{{1, 2}, {3, 4}}}).points`, `((struct Line){{{1, 2}, {3, 4}}}).points[1].y`, and `&((struct Line){{{1, 2}, {3, 4}}}).points[0]` evaluate through safe pointer/index/address-of paths for pointer declarations, parameters, pointer arithmetic, indexing/`->`/`.` access, and const-discard conversion checks, with interpreter plus C compiler-oracle coverage.
- [x] Field access on aggregate-valued expressions beyond raw aggregate literals; acceptance: grouped aggregate assignment expressions, aggregate pointer dereference assignments such as `(((struct Line){{...}}).points[0] = replacement).x`, aggregate-valued conditional/comma expressions, aggregate-returning calls such as `make_point(5).y`, and matching union-valued expression results such as `(left = right).value`, `(cond ? left : replacement).value`, `(side_effect(), right).value`, `make_number(5).value`, and `((*(&left) = make_number(6))).value` can be followed by `.` for scalar/nested field reads, reusing existing `eval_struct_expr` aggregate by-value semantics; direct lvalue write coverage for scalar and aggregate array fields on aggregate compound literals is locked in with interpreter and C compiler-oracle fixtures, and naked union-returning calls used as scalar expressions report union-specific diagnostics.
- [x] `sizeof` field queries on aggregate-valued expressions; acceptance: `sizeof((left = right).x)`, `sizeof((cond ? left : right).tag)`, `sizeof((side_effect(), right).x)`, `sizeof(make_point(7).tag)`, and matching union expression forms infer the selected field size without evaluating assignments, comma-left operands, or aggregate-returning calls, with interpreter and ABI-independent C compiler-oracle fixtures. Follow-up coverage also includes full array-object sizing for scalar and embedded aggregate-array fields selected from aggregate-valued expressions such as `sizeof(make_line(10).points)`, `sizeof((line = make_line(20)).numbers)`, `sizeof((1 ? line : make_line(30)).values)`, and `sizeof(make_box(40).line.points)` without evaluating the operand.

- [x] C reverse-subscript expression parity for the supported one-level pointer/array subset; acceptance: `i[p]` now works like `p[i]` for scalar array variables, pointer variables/parameters, string literals and `char *` pointers, scalar-array compound literals, and aggregate array pointers followed by field access such as `1[points].y`; writes such as `2[values] = 9` reuse existing dereference lvalue handling, with interpreter and C compiler-oracle coverage.
- [x] Address-of string-literal element expressions; acceptance: direct `&"text"[i]` now creates the same read-only interpreter-owned string array-element pointer as grouped/reverse forms, works in pointer declarations and reads with relative indexing, rejects writes through the produced pointer with `cannot modify read-only array through pointer`, and includes interpreter plus C compiler-oracle coverage.
- [x] Adjacent string literal concatenation; acceptance: tokens such as `"he" "llo"` concatenate into one NUL-terminated read-only byte array for ordinary string pointer expressions, direct indexing, `sizeof`, char-array initializers, char-array compound literals, and pointer/array-parameter calls, with interpreter plus C compiler-oracle coverage.

- [x] Embedded aggregate-array field pointer equality parity; acceptance: direct and nested embedded aggregate-array field pointers such as `line.points`, `&line.points[0]`, `line.points + 2`, `&line.points[2]`, and `box.line.points` compare equal/unequal by owner/path/index metadata instead of falling through to generic pointer inequality, with interpreter and C compiler-oracle fixtures.
- [x] Aggregate compound-literal pointer-field lvalues; acceptance: pointer-valued fields selected from aggregate compound literals support assignment, pointer compound assignment, and prefix/postfix increment/decrement in pointer contexts (for example `(((struct Cursor){values}).p = values + 2)[-1]`, `((struct Cursor){values + 1}).p += 2`, `--((struct Cursor){values + 3}).p`, and `((struct Cursor){values + 1}).p++`), preserving pointer-slot const diagnostics and pointee type/const conversion checks with interpreter and C compiler-oracle fixtures.
- [x] Address-of scalar fields selected from aggregate compound literals; acceptance: `&((struct Point){4, 8}).x`, nested paths such as `&((struct Box){{2, 3}, 4}).inner.y`, and union scalar fields such as `&((union Number){7}).value` create safe interpreter-owned pointers to hidden current-scope compound-literal storage, preserve field pointee type/const metadata, support mutation through the resulting pointer, and include interpreter plus C compiler-oracle fixtures.
- [x] Address-of aggregate fields selected from aggregate compound literals; acceptance: `&((struct Box){{5, 7}, 9}).inner` creates a safe interpreter-owned struct pointer to the hidden current-scope compound-literal storage, supports `->` field reads/writes and helper-function mutation, preserves aggregate pointer metadata, and includes interpreter plus C compiler-oracle fixtures.
- [x] Address-of scalar fields reached through struct pointers; acceptance: `&point_ptr->x`, `&box_ptr->inner.y`, and scalar fields reached after addressing a nested aggregate field such as `struct Point *inner = &box_ptr->inner; &inner->x` produce safe interpreter-owned scalar field pointers that alias the original struct storage, preserve pointer type/const metadata, and include interpreter plus C compiler-oracle fixtures.
- [x] Address-of scalar fields through pointers into embedded aggregate-array fields; acceptance: `struct Point *p = line.points + 1; int *x = &p->x;`, nested containing paths such as `box.line.points + 2`, and helper-call/dereference mutation through the resulting scalar pointers alias the original containing struct storage, preserve pointer type/const checks, and include interpreter plus C compiler-oracle coverage.
- [x] Address-of aggregate-valued fields through pointers into embedded aggregate-array fields; acceptance: `struct Segment *p = drawing.segments + 1; struct Point *start = &p->start;` returns a safe struct pointer to the embedded aggregate field, supports `->` reads/writes and helper mutation through the resulting pointer, aliases the original containing struct storage, and includes interpreter plus C compiler-oracle coverage.
- [x] Pointer equality for scalar fields reached through pointers into embedded aggregate-array fields; acceptance: independently computed pointers such as `int *left = &(line.points + 1)->x` (via supported intermediate pointer variables) compare equal to pointers targeting the same embedded element field, compare unequal to sibling fields or different embedded elements, preserve dereference mutation aliasing, and include interpreter plus C compiler-oracle coverage.
- [x] Direct address-of for scalar field paths after embedded aggregate-array indexing expressions; acceptance: `&line.points[1].x` and nested containing paths such as `&box.line.points[2].y` produce safe scalar pointers that alias the original embedded aggregate-array element fields, reuse existing `StructFieldElement` pointer metadata through parser lowering, and include interpreter plus C compiler-oracle coverage.

- [x] Direct address-of for aggregate field paths after struct-array and embedded aggregate-array indexing expressions; acceptance: `&segments[1].start`, `&drawing.segments[1].start`, and nested containing paths such as `&box.drawing.segments[0].end` produce safe struct pointers that alias the original aggregate fields, reuse existing `PointerValue::StructField` metadata with element indexes preserved, support helper-call mutation through `->`, and include interpreter plus C compiler-oracle coverage.
- [x] Broader parser-folded integer constant expressions for enum initializers, `switch case` labels, supported one-dimensional array declarator lengths, and supported array/aggregate designator indexes; acceptance: parenthesized constants, unary `+`/`-`/`~`/`!`, scalar type-name casts such as `(int)`, `(char)`, and scalar typedef aliases, multiplicative `*`/`/`/`%`, shifts `<<`/`>>`, relational `<`/`<=`/`>`/`>=`, equality `==`/`!=`, bitwise `&`/`^`/`|`, logical `&&`/`||`, conditional `?:`, type-query unary operators `sizeof(type-name)` / `_Alignof(type-name)`, parenthesized and unparenthesized expression-form `sizeof` operands for supported scalar/string/compound-literal expression shapes, and nested `sizeof sizeof(type-name)` work with visible enum constants without evaluating runtime expressions, preserve duplicate switch-case diagnostics after folding, and include interpreter plus C compiler-oracle coverage. Array lengths now accept the same parser-folded integer constant expression subset for ordinary scalar arrays, typedef array declarators, aggregate fields, aggregate arrays, array parameters, and fixed-size array compound literal type names, while preserving positive-length diagnostics. Designator indexes now accept the same integer constant expression subset for fixed scalar arrays, inferred and fixed array compound literals, scalar array fields, aggregate-array designators, and struct/union array initializers while preserving non-negative/too-large/out-of-bounds diagnostics and comma-expression rejection.

- [x] Comma-separated aggregate field declaration lists inside supported struct/union definitions; acceptance: `struct Point { int x, y; char tag, code; };`, `struct Segment { struct Point start, end; int weights[2], offsets[2]; };`, `struct Cursor { int *head, *tail; const int *view, *limit; };`, and `union Number { int value, other; };` parse each declarator with its own pointer/array suffix while preserving shared declaration specifiers, const/pointee metadata, duplicate-field diagnostics, aggregate initializers, field access, pointer fields, and C compiler-oracle coverage.
- [x] Typedef-backed aggregate field declaration lists preserve alias metadata; acceptance: `typedef int *IntPtr; typedef const int *ConstIntView; typedef int * const ConstIntSlot; struct Cursor { IntPtr head, tail; ConstIntView view, limit; ConstIntSlot fixed; };` preserves pointee const and pointer-slot const through comma-separated struct fields, rejects reassignment to `fixed` with `cannot assign to const struct field 'fixed'`, and includes interpreter plus C compiler-oracle coverage.

## P2 — data types

- [x] `char` literals, declarations, and function parameters
- [x] arrays
- [x] string literals as read-only byte arrays
- [x] pointer model design document before implementation
- [x] first scalar pointer milestone: pointer declarations, scalar address-of/dereference, dereference assignment, reassignment, null diagnostics, and out-of-scope scalar diagnostics
- [x] pointer parameters, fixed-size and unsized scalar/aggregate array parameter spellings as C-style pointer parameters, C99 array-parameter bracket qualifiers (`static`, `const`, `restrict`, `volatile`, and `_Atomic`) as parser metadata, pointer-slot const enforcement for `int values[const N]`, array/string decay to pointer arguments, and pointer indexing for array-base pointers
- [x] `&array[index]` array-element pointers and dereference assignment/read support
- [x] Scoped array-backed pointer arithmetic: `p + n`, `n + p`, `p - n`, same-storage pointer difference, `p += n`, `p -= n`, and pointer-variable `++`/`--` with scalar/null/out-of-bounds diagnostics plus interpreter and C compiler-oracle fixtures
- [x] Same-array pointer ordering comparisons for supported array-backed pointers; acceptance: `p < q`, `p <= q`, `p > q`, and `p >= q` work for pointers into the same scalar array, string-literal storage, aggregate array, embedded aggregate-array field storage (including nested field paths), and scalar array fields selected from direct structs, struct-array elements, or struct pointers (including `packet.values < &packet.values[3]`, `packets[i].values`, and `slot->values`), comparisons between different arrays/fields report `cannot compare pointers to different arrays`, scalar/null pointer ordering remains unsupported, and interpreter plus C compiler-oracle fixtures cover the subset
- [x] Scoped aggregate-array element pointer arithmetic, indexed field access, indexed by-value expressions, array-to-pointer decay, aggregate pointer dereference values/copy assignment, and unsized array parameter spellings: `struct Point *p = &points[0]` and `union Number *n = &numbers[0]` support bounded `p + n`, `p - n`, `p += n`, `p -= n`, pointer-variable `++`/`--`, same-array pointer difference, C-style indexed field lvalues such as `p[i].x`, `p[i].x += 1`, and `n[i].value++`, by-value aggregate expressions such as `struct Point copy = p[i];`, `copy = p[i];`, `sum(p[i])`, `struct Point copy = *p;`, `sum(*p);`, `return *p;`, and `*p = replacement;` over interpreter-owned struct/union pointers, direct decay of aggregate array variables to existing `struct T *` / `union T *` parameters such as `use(points)`, and C-style parameter spellings such as `int values[]`, `char text[]`, `struct Point points[]`, and `union Number numbers[]` parsed as pointer parameters, with one-past/out-of-bounds/const-pointee diagnostics and compiler-oracle coverage
- [x] Pointer pointee type compatibility diagnostics for scalar and aggregate pointers; acceptance: `char *` cannot flow into `int *`, `struct Size *` cannot be assigned to `struct Point *`, and `union Number *` cannot be passed to `struct Point *`; null remains compatible with all pointer types, const-preservation diagnostics still run, and invalid interpreter fixtures report targeted `cannot convert pointer to ... to pointer to ...` messages.
- [x] Pointer-returning functions and prototypes for the existing one-level pointer subset; acceptance: `int *choose(...)`, `char *tail(...)`, `const int *view(...)`, `struct Point *pick(...)`, `union Number *pick(...)`, and pointer-typedef return spellings parse as function signatures, return interpreter-owned pointer metadata by value, preserve const and pointee-type conversion checks at the return boundary and at direct call conversion sites such as pointer declarations, keep pointer-to-pointer returns rejected, and include valid/invalid/compiler-oracle fixtures.

## P3 — C-subset conformance tooling

- [x] Compare Cust output against `gcc`/`clang`/`cc` for supported programs as an external test oracle only
- [x] Add an initial corpus under `tests/fixtures/`
- [x] Add more conformance fixtures with explicit expected results and optional compiler-oracle comparisons where supported (pointer scalar/parameter/array-decay/array-element/truthiness/equality coverage, aggregate pointer declaration-list coverage for `struct Point *p = points, *q = points + 1;` / `union Number *n = numbers, *m = numbers + 2;`, same-declaration anonymous aggregate array plus pointer declarator coverage for `struct { int x; int y; } points[3] = {...}, *slot = points + 1;` and matching anonymous union arrays/pointers, mixed declaration-context coverage combining comma-separated typedef aliases, inline enum object declarations, pointer-first anonymous aggregate declaration lists, const anonymous aggregate arrays and pointer views, aggregate compound literal returns, direct enum pointer/type-query coverage for `enum State *`, `const enum State view[static 2]`, enum casts, `sizeof(enum State[N])`, `_Alignof(enum State[N])`, and `sizeof(enum State *)`, inline enum `sizeof(enum Tag { ... })` type-definition expression coverage for block expressions, declaration-list initializers, and return expressions, mixed aggregate-field/type-query coverage combining nested named aggregate field declaration lists, anonymous aggregate fields, typedef-backed pointer fields, struct-pointer field access, scalar array-field `sizeof` relationships, aggregate-array `_Alignof` relationships, named aggregate typedef declaration-list coverage for `typedef struct Point { ... } Point, *PointPtr, PointArray[3];` plus matching union aliases with array-typedef decay/pointer mutation, enum typedef declaration-list coverage for `typedef enum State { ... } State, *StatePtr, StateArray[4];` with enum-array pointer arithmetic plus ABI-independent type-query checks, and C99/C++-style `//` line-comment lexer coverage that proves trailing comments stop at the newline and `//` inside string literals remains literal text; targeted invalid const anonymous aggregate array pointer-view writes, plus invalid negative pointer-index and unsupported pointer/integer diagnostics have been added; continue expanding mixed supported-subset fixtures as new features land)
- [x] Add fuzz/property tests for lexer/parser safety
- [x] Targeted invalid pointer-difference diagnostics through field-backed array paths; acceptance: embedded aggregate-array fields from different fields, scalar array fields from different aggregate objects, and anonymous aggregate-array fields from different aggregate objects all report `cannot subtract pointers to different arrays`, proving pointer owner/path metadata distinguishes less-traveled field-backed array storage roots.
- [x] Targeted invalid pointer arithmetic diagnostics through distinct string literal storage roots; acceptance: subtracting pointers into separate string literal arrays reports `cannot subtract pointers to different arrays`, and relational ordering between pointers into separate string literal arrays reports `cannot compare pointers to different arrays`, proving read-only string literal storage carries distinct array identity metadata through pointer arithmetic checks.
- [x] Targeted invalid pointer arithmetic diagnostics through separate array compound literal storage roots; acceptance: subtracting pointer-valued one-dimensional array compound literals such as `((int[]){1, 2, 3}) - ((int[]){4, 5, 6})` reports `cannot subtract pointers to different arrays`, and relational ordering between separate array compound literals reports `cannot compare pointers to different arrays`, proving hidden compound-literal array storage carries distinct array identity metadata through pointer arithmetic checks.
- [x] Targeted invalid pointer arithmetic diagnostics through separate aggregate-array compound literal storage roots; acceptance: subtracting pointer-valued aggregate-array compound literals such as `((struct Point[]){{1, 2}}) - ((struct Point[]){{3, 4}})` reports `cannot subtract pointers to different arrays`, and relational ordering between separate aggregate-array compound literals reports `cannot compare pointers to different arrays`, proving hidden aggregate-array compound literal storage carries distinct array identity metadata through pointer arithmetic checks.
- [x] Targeted invalid pointer ordering diagnostics through distinct anonymous aggregate-array field storage roots; acceptance: relational ordering between anonymous aggregate-array fields in different containing struct objects, such as `right.items > left.items`, reports `cannot compare pointers to different arrays`, proving embedded field owner/path metadata distinguishes separately owned anonymous aggregate-array field storage roots for ordering as well as subtraction.
- [x] Targeted invalid pointer arithmetic diagnostics through array fields selected from separate aggregate compound literals; acceptance: subtracting scalar array fields such as `((struct Packet){{1, 2}}).values - ((struct Packet){{3, 4}}).values` and embedded aggregate-array fields such as `((struct Line){{...}}).points - ((struct Line){{...}}).points` reports `cannot subtract pointers to different arrays`, and relational ordering between those separately evaluated aggregate compound-literal field roots reports `cannot compare pointers to different arrays`, proving hidden aggregate compound-literal field storage carries distinct array identity metadata.
- [x] Inline enum type definitions inside aggregate initializer expressions and designator indexes have conformance coverage; acceptance: nested aggregate initializer fields use `sizeof(enum InitX { ... })`, enum casts, `_Alignof(enum TailValue { ... })`, and inline-enum-derived array designator indexes, generated enumerators remain visible to later same-block expressions, and interpreter plus warning-free native compiler-oracle fixtures return the same value.
- [x] Inline enum type definitions inside conditional and short-circuit expression operands have conformance coverage; acceptance: generated enumerators from selected and unselected `?:` branches plus unevaluated `&&`/`||` operands remain visible to later same-block expressions, and interpreter plus warning-free native compiler-oracle fixtures return the same value.
- [x] Parser-folded integer constant expressions in supported array type-name operands have conformance coverage; acceptance: `sizeof(int[BASE + EXTRA])`, `sizeof(char[sizeof(char) + 2])`, `sizeof(const Count[BASE ? 2 : 4])`, `sizeof(Pair[BASE])`, `_Alignof(int[BASE + EXTRA])`, and `_Alignof(Pair[EXTRA + 1])` all use the same fixed-size integer-constant-expression length subset as array declarations, with interpreter and warning-free native compiler-oracle fixtures checking ABI-independent size/alignment relationships.
- [x] Inline enum and aggregate type definitions inside supported array declarator lengths and array type-name length expressions have conformance coverage; acceptance: `int values[sizeof(enum Len { LEN = 5 })];` exposes `LEN` to later same-block statements, `typedef int Scores[sizeof(struct Cell { char tag; })];` installs `struct Cell` for later same-block declarations, `sizeof(int[sizeof(enum ArrayLen { N = 3 })])` keeps generated enumerators visible, and `sizeof(struct Box { ... }[2]) == 2 * sizeof(struct Box)` verifies inline aggregate array type operands with ABI-independent native compiler-oracle coverage.
- [x] Inline aggregate type definitions inside control-flow controlling expressions have conformance coverage; acceptance: `if (sizeof(struct IfBox { ... }) == sizeof(struct IfBox))`, `while (... && sizeof(struct WhileCell { ... }) == sizeof(struct WhileCell))`, and `switch (sizeof(struct SwitchBox { ... }) == sizeof(struct SwitchBox))` install inline struct tags into the enclosing block so statement bodies can declare same-tag objects. Follow-up coverage also locks in inline named `union` definitions in the same `if`/`while`/`switch` controlling-expression positions with same-body union declarations. Interpreter plus warning-free native compiler-oracle fixtures cover the supported subset.
- [x] Inline aggregate type definitions inside `for` clauses have conformance coverage; acceptance: inline named `struct` definitions in `for` initializer and condition expressions plus an inline named `union` definition in a `for` increment expression install tags early enough that loop bodies can declare same-tag objects, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside function-call argument subexpressions have conformance coverage; acceptance: call arguments containing `sizeof(struct ArgBox { ... })`, `sizeof(union ArgChoice { ... })`, and an inline-`struct` aggregate compound literal install tags early enough that later same-block declarations can use those tags, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside C11 `_Static_assert` conditions have conformance coverage; acceptance: assertion conditions containing `sizeof(struct AssertBox { ... }) == sizeof(struct AssertBox)` and `sizeof(union AssertChoice { ... }) == sizeof(union AssertChoice)` install tags early enough that later same-block declarations can use those tags, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside conditional and short-circuit expression operands have conformance coverage; acceptance: `flag ? sizeof(struct ThenBox { ... }) == sizeof(struct ThenBox) : sizeof(struct ElseBox { ... }) == sizeof(struct ElseBox)` installs tags from both selected and unselected branches, and `0 && sizeof(struct AndBox { ... })` / `1 || sizeof(union OrChoice { ... })` install tags from short-circuited operands early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside declaration-list initializer and assignment RHS expressions have conformance coverage; acceptance: declaration-list initializers such as `int ok = sizeof(struct DeclBox { ... }) == sizeof(struct DeclBox), value = 4;`, scalar assignment RHS expressions using `sizeof(struct AssignBox { ... })`, and compound-assignment RHS expressions using `sizeof(union AssignChoice { ... })` install tags early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside discarded expression statements have conformance coverage; acceptance: `(void)(sizeof(struct VoidBox { ... }) == sizeof(struct VoidBox))`, `(void)((struct LiteralBox { ... }){7}).value`, and `(void)(sizeof(union VoidChoice { ... }) == sizeof(union VoidChoice))` install tags early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside return expressions have conformance coverage; acceptance: return-expression type queries such as `sizeof(struct ReturnBox { ... }) == sizeof(struct ReturnBox)`, inline aggregate compound literals such as `((struct ReturnBox){...}).field`, and matching inline `union` definitions parse and execute with tags installed in the enclosing function block, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside aggregate initializer expressions and designator indexes have conformance coverage; acceptance: positional initializer entries using `sizeof(struct InitBox { ... }) == sizeof(struct InitBox)`, aggregate compound-literal initializer entries, array designator indexes using inline `struct` type queries, and field designator values using inline `union` type queries install tags early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside pointer-valued initializer and assignment expressions have conformance coverage; acceptance: `int *field = &((struct PointerBox { int value; }){5}).value`, pointer reassignment from another inline `struct` compound-literal field address, and matching `union` field-address coverage install tags early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside scalar array compound-literal initializer expressions and designator indexes have conformance coverage; acceptance: `(int[]){ sizeof(struct LitArrayBox { ... }) == sizeof(struct LitArrayBox), [sizeof(struct LitArrayIndex { ... }) == sizeof(struct LitArrayIndex)] = sizeof(union LitArrayChoice { ... }) == sizeof(union LitArrayChoice), ((struct LitArrayPoint { ... }){5}).x }` installs inline `struct`/`union` tags early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside aggregate-array compound-literal initializer expressions and designator indexes have conformance coverage; acceptance: `(struct Anchor[]){ { sizeof(struct AggLitBox { ... }) == sizeof(struct AggLitBox), 2 }, [sizeof(struct AggLitIndex { ... }) == sizeof(struct AggLitIndex)] = { sizeof(union AggLitChoice { ... }) == sizeof(union AggLitChoice), ((struct AggLitPoint { ... }){5}).x } }` installs inline `struct`/`union` tags early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside pointer arithmetic and pointer-index expressions have conformance coverage; acceptance: pointer offsets and indexes containing `sizeof(struct PtrOffset { ... })`, `sizeof(union PtrIndex { ... })`, aggregate compound-literal field addresses, and nested array index expressions used by pointer dereference writes install inline `struct`/`union` tags early enough for later same-block declarations, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside pointer comparison and pointer-difference expressions have conformance coverage; acceptance: pointer equality operands, pointer relational operands, and pointer-difference offset expressions containing `sizeof(struct PtrEq { ... })`, `sizeof(union PtrRel { ... })`, and `sizeof(struct PtrDiff { ... })` parse and evaluate through existing interpreter-owned pointer metadata, with interpreter and warning-free native compiler-oracle fixtures.
- [x] Inline aggregate type definitions inside non-evaluating `sizeof(*pointer_expr)` operands have conformance coverage; acceptance: scalar pointer expressions, aggregate pointer expressions, and address-of aggregate compound-literal field expressions containing inline `struct`/`union` definitions infer the correct pointee type metadata without evaluating operands, with interpreter and warning-free native compiler-oracle fixtures using ABI-independent `sizeof(*ptr_expr) == sizeof(element-type)` relationships.

- [x] `sizeof` over comma-expression operands preserves C's non-evaluating semantics while using the right operand's type metadata. Acceptance: `sizeof((marker = marker + 1, (char){7}))`, `sizeof((marker = marker + 10, values + 1))`, `sizeof((marker = marker + 100, text[1]))`, and `sizeof((marker = marker + 1000, values[0]))` leave `marker` unchanged and match `sizeof(char)`, pointer-expression size, `sizeof(char)`, and `sizeof(int)` respectively, with interpreter and ABI-independent native compiler-oracle coverage.
- [x] `sizeof` over aggregate-valued conditional-expression operands preserves C's non-evaluating semantics while using the common branch aggregate type metadata. Acceptance: `sizeof(flag ? left : right) == sizeof(struct Pair)`, `sizeof((marker = marker + 1, flag ? left : right)) == sizeof(struct Pair)`, `sizeof(1 ? (left = right) : right) == sizeof(struct Pair)`, and matching `union` coverage leave side-effect markers and aggregate assignments unevaluated while preserving mismatched aggregate-branch diagnostics, with interpreter and ABI-independent native compiler-oracle coverage.
- [x] `sizeof` over aggregate assignment-expression operands preserves C's non-evaluating semantics while using the assigned aggregate variable's type metadata. Acceptance: `sizeof((left = right)) == sizeof(struct Pair)`, `sizeof((marker = marker + 1, left = right)) == sizeof(struct Pair)`, and `sizeof((number = replacement)) == sizeof(union Number)` leave target aggregate variables and side-effect markers unchanged while matching native `sizeof(expr) == sizeof(type)` relationships, with interpreter and warning-free C compiler-oracle coverage.
- [x] `sizeof` over aggregate element assignment-expression operands preserves C's non-evaluating semantics while using the selected aggregate element type metadata. Acceptance: `sizeof((points[0] = replacement))`, `sizeof((*slot = replacement))`, `sizeof((line.points[1] = replacement))`, `sizeof((line_ptr->points[0] = replacement))`, and matching union aggregate-array element assignment forms leave aggregate elements and side-effect markers unchanged while matching native `sizeof(expr) == sizeof(type)` relationships, with interpreter and warning-free C compiler-oracle coverage.
- [x] `sizeof` over scalar fields selected from embedded aggregate-array elements preserves C's non-evaluating semantics while using the selected field type metadata. Acceptance: `sizeof(line.points[0].tag) == sizeof(char)`, `sizeof(line.points[1].x) == sizeof(int)`, and comma-wrapped operands such as `sizeof((marker = marker + 1, line.points[0].tag))` leave side-effect markers unchanged while matching native `sizeof(expr) == sizeof(type)` relationships, with interpreter and warning-free C compiler-oracle coverage.
- [x] Field access on aggregate-array element assignment results parses and evaluates as by-value aggregate result selection. Acceptance: `(points[0] = replacement).x`, `(line.points[1] = replacement).tag`, and `(line_ptr->points[0] = replacement).x` mutate the assignment target, return a copied aggregate value for field selection, and `sizeof((marker = marker + 1, (points[1] = replacement).tag))` remains non-evaluating with the selected field size, with interpreter and warning-free C compiler-oracle coverage.
- [x] Field access on aggregate-field assignment results parses and evaluates as by-value aggregate result selection. Acceptance: `(line.start = replacement).x` and `(slot->end = other).tag` mutate the selected aggregate field, return a copied aggregate value for field selection, and `sizeof((marker = marker + 1, (line.end = other).tag))` remains non-evaluating with the selected field size, with interpreter and warning-free C compiler-oracle coverage.

- [x] Field-access parser diagnostics after direct `.` and pointer `->` selectors have exact-output regression coverage. Acceptance covered by malformed `p.;`, `p.];`, `q->;`, and `q->];` reporting `expected struct field name after '.'/'->', found ...` with source locations; focused coverage passed immediately because the shared field-name parser helper already produced the desired contextual diagnostics.
- [x] Unsupported pointer-array cast type names have exact source-located diagnostics. Acceptance covered by scalar `(int *[2])0`, named and anonymous aggregate pointer-array casts, and pointer-typedef arrays such as `(IntPtr[2])0`, all reporting `pointer array casts are not supported` at `[` instead of a generic cast-closing-parenthesis error while preserving existing one-level-pointer and pointer-to-pointer cast boundaries.
- [x] Unsupported pointer-array `sizeof`/`_Alignof` type names have exact source-located diagnostics. Acceptance covered direct scalar, named aggregate, anonymous aggregate, and pointer-typedef forms such as `sizeof(int *[2])` and `_Alignof(PointPtr[2])`, all reporting `pointer array sizeof/_Alignof types are not supported` at `[` instead of generic closing-parenthesis errors while preserving supported ordinary array type queries, one-level pointer sizes, and pointer-to-pointer boundaries.
- [x] Abstract function type names in casts and type queries have exact source-located diagnostics. Acceptance covered `(int(void))0`, `sizeof(int(void))`, and `_Alignof(int(void))` reporting `function casts/sizeof/_Alignof types are not supported` at the declarator suffix instead of generic closing-parenthesis errors, while parenthesized pointer type diagnostics remain unchanged.
- [x] Function-array type names in casts and type queries have exact source-located diagnostics. Acceptance covered `(int[2](void))0`, `sizeof(int[2](void))`, `_Alignof(int[2](void))`, a named aggregate array, and a typedef-array type name reporting the established `function casts/sizeof/_Alignof types are not supported` error at the function suffix instead of generic closing-parenthesis errors, while valid array compound literals remain unchanged.
- [x] C11 `_Static_assert` missing-argument diagnostics are targeted before generic expression parsing. Acceptance covered by `_Static_assert(, "condition required");` reporting `expected _Static_assert condition, found Comma` at the delimiter instead of generic `expected expression`, plus `_Static_assert(1, );` locking in the existing source-located string-literal-message diagnostic.
- [x] C11 `_Alignas` missing-argument diagnostics are targeted before generic expression parsing. Acceptance covered by `_Alignas() int value;`, block-local `_Alignas(,) int value;`, and block-local `_Alignas(; int value;` reporting `expected _Alignas argument, found ...` at the delimiter/terminator instead of generic `expected expression`, while valid `_Alignas` declaration specifiers continue to parse as no-op metadata.
- [x] Declaration initializer missing-expression diagnostics are targeted before generic expression parsing. Acceptance covered by `int value = ;`, `int value = 1, other = ;`, `int *slot = ,`, `struct Point point = ;`, fixed array declarations such as `int values[2] = ;`, array declaration-list tails such as `int first[1] = {1}, second[2] = ;`, fixed aggregate-array declarations such as `struct Point points[1] = ;`, and aggregate-array declaration-list tails reporting `expected initializer expression after '=' in ... declaration, found ...` at the delimiter/terminator, while omitted initializers (`int value;`, `int *slot;`) and braced/string aggregate/array initializers remain valid.
- [x] Braced initializer missing-element diagnostics are targeted before generic expression parsing. Acceptance covered by scalar array initializer forms such as `int values[2] = {, 1}`, array designator values such as `[0] = ,`, struct/union initializer forms such as `struct Point point = {, 1}` and `.x = ,`, and aggregate-array initializers such as `struct Point points[2] = {, {1, 2}}` reporting `expected initializer element in ... initializer, found ...` at the offending delimiter while preserving empty `{}` and trailing-comma initializer forms.
- [x] Braced scalar initializer missing-expression diagnostics are targeted before generic expression parsing. Acceptance covered by scalar declarations such as `int value = {,};` and `int value = {};`, plus aggregate scalar-field entries such as `struct Point point = { .x = {,} };`, reporting `expected initializer element in braced scalar initializer for ...` at the offending delimiter while preserving valid braced scalar initializers and trailing-comma scalar initializer forms.
- [x] Excess braced initializer diagnostics include source locations. Acceptance covered by scalar braced initializers, fixed scalar arrays, scalar struct/union initializers, nested struct-field initializers, and struct-array initializers reporting `too many initializers for ... at line ..., column ...` at the first excess initializer token instead of returning unlocated parser errors.
- [x] Excess compound-literal initializer diagnostics include source locations. Acceptance covered by fixed-size scalar array compound literals and aggregate-array compound literals reporting `too many initializers for ... compound literal at line ..., column ...` at the first excess initializer token instead of returning unlocated diagnostics.
- [x] Malformed struct initializer path-designator diagnostics are targeted before generic integer-constant parsing. Acceptance covered by `.inner. = 1`, `.values[] = 1`, `.values[;] = 1`, `.values[} = 1`, and matching scalar array compound-literal designator forms reporting contextual source-located diagnostics, including `expected array designator index before ']'`, `before ';'`, and `before '}'` for delimiter-only array designators.
- [x] Too-long char-array string literal initializer diagnostics include source locations. Acceptance covered by fixed char array declarations, char array fields inside aggregate initializers, and fixed-size char array compound literals reporting `initializer string for char array ... is too long at line ..., column ...` at the offending string literal token instead of returning unlocated parser errors.
- [x] Inferred scalar/aggregate array missing-initializer diagnostics are targeted before generic compound-literal parsing. Acceptance covered by `int values[] = ;`, declaration-list tails such as `int first[] = {1}, second[] = ;`, `struct Point points[] = ;`, and aggregate declaration-list tails reporting `expected initializer expression after '=' in inferred array declaration` / `inferred aggregate array declaration` at the delimiter.
- [x] Non-`char` string literal initializer type-mismatch diagnostics include source locations. Acceptance covered by fixed scalar arrays (`int values[3] = "nope"`), inferred scalar arrays (`int values[] = "nope"`), and scalar array compound literals (`(int[]){"nope"}`) reporting `string literal initializer requires char array ... at line ..., column ...` at the offending string literal token instead of returning unlocated parser errors.
- [x] Missing cast-expression operands are targeted before generic primary-expression parsing. Acceptance covered by scalar casts (`(int);`), pointer casts (`(int *) ;`), and void casts (`(void);`) reporting `expected expression after cast, found ...` at the offending delimiter/terminator while valid cast fixtures remain unchanged.
- [x] Missing scalar compound-literal initializer expressions have exact parser-trust coverage. Acceptance covered by `(int){}`, `(char){,}`, and `(int){;}` reporting `expected initializer element in braced scalar initializer for scalar compound literal, found ...` at the offending delimiter; focused coverage passed immediately because scalar compound literals already reuse the shared braced-scalar initializer guard.
- [x] Unsupported `void` object and typedef forms have targeted source-located diagnostics. Acceptance covered by file-scope `void value;`, block-local `void value;`, aggregate fields `void value;`, and `typedef void Void;` reporting `void object declarations are not supported` / `void typedef aliases are not supported` at the `void` keyword instead of function/type parser fallthroughs, while supported `void` functions, empty `void` parameter lists, `(void)expr` casts, and `void *` diagnostics remain unchanged.
- [x] Missing return-expression diagnostics are targeted before generic primary-expression parsing. Acceptance covered by malformed `return ,;`, `return );`, and `return }` reporting `expected expression after return, found ...` at the offending delimiter while preserving valid empty `return;` handling and existing return-type/return-shape diagnostics.
- [x] Qualifier-only cast type diagnostics are targeted before generic type parsing. Acceptance covered by `(const);`, `(volatile);`, and `(_Atomic);` reporting `expected cast type after type qualifier '<qualifier>', found RParen` at the missing type location instead of generic `expected type, found RParen`, while preserving valid qualified casts and existing missing-cast-operand diagnostics.
- [x] Qualifier-only `sizeof`/`_Alignof` type diagnostics are targeted before generic type parsing. Acceptance covered by `sizeof(volatile)`, `sizeof(_Atomic)`, `_Alignof(volatile)`, and `_Alignof(_Atomic)` reporting `expected sizeof/_Alignof type after type qualifier '<qualifier>', found RParen` at the missing type location instead of generic `expected type, found RParen`, while preserving valid qualified type-query parsing.
- [x] Missing `_Atomic(type-name)` specifier arguments are targeted before generic type parsing. Acceptance covered by `_Atomic()` and `_Atomic(,)` in global declarations, block locals, parameters, aggregate fields, `sizeof(_Atomic())`, and `(_Atomic())0` reporting `expected _Atomic type, found ...` at the delimiter instead of generic `expected type, found ...`, while valid bare `_Atomic` qualifiers and `_Atomic(type-name)` specifiers remain unchanged.
- [x] Missing control-flow controlling expressions are targeted before generic expression parsing. Acceptance covered by malformed `if ()`, `while (;`, `do { } while ();`, `switch ()`, `switch (;`, and `switch (}` reporting `expected expression after <keyword>, found ...` at the offending delimiter.
- [x] Misplaced `sizeof` expression operands are targeted before generic expression parsing. Acceptance covered by malformed `sizeof([)`, `sizeof(?)`, `sizeof(:)`, `sizeof(.)`, `sizeof(->field)`, and `sizeof({1})` reporting `expected sizeof operand before '<token>'` at the offending token instead of generic `expected sizeof operand, found ...` / primary-expression diagnostics, while existing delimiter-only `sizeof()` / `sizeof(;)` and valid type/expression operands remain unchanged.
- [x] Missing grouped-expression operands are targeted before generic primary-expression parsing. Acceptance covered by malformed `()`, `(, 1)`, `(;`, and `(]` reporting `expected grouped expression, found ...` at the offending delimiter/terminator while preserving valid grouped expressions and existing `expected ')' after grouped expression` diagnostics.
- [x] Invalid-start braced-initializer designator values are targeted before generic expression parsing. Acceptance covered by malformed designator values such as `struct Point point = {.x = [?], .y = 1};` and `int values[2] = {[0] = ?};` reporting contextual `expected initializer element in ... initializer, found LBracket/Question` diagnostics at the offending token while preserving valid array designator starts like `[0] = value`.
- [x] Invalid-start enum constant values are targeted before generic integer-constant parsing. Acceptance covered by `enum BadStart { FIRST = [?] };` and `enum BadStart { FIRST = ? };` reporting `expected integer constant after enum constant '=' before '['/'?'` at the offending token instead of `expected integer constant after enum constant '=', found ...`, while valid enum initializer and missing-value diagnostics remain unchanged.
- [x] Invalid-start expression statement diagnostics are targeted before generic statement fallback. Acceptance covered by block-local malformed statement starts such as `[;`, `?;`, and `,;` reporting `expected expression statement, found LBracket/Question/Comma` at the offending token instead of `unexpected token in statement: ...`, while preserving unmatched closing-delimiter diagnostics and normal empty/block statement parsing.

- [x] Invalid-start return/control expression keyword diagnostics are targeted before generic expression parsing. Acceptance covered by `return int;`, `return struct;`, `if (return)`, `while (int)`, `do { } while (struct);`, `for (int i = 0; return; ...)`, and `for (...; ...; struct)` reporting `expected expression after <context> before '<keyword>'` at the offending keyword instead of generic `expected expression, found ...` or `unexpected token in for increment`, while preserving delimiter, bracket/question, and valid expression behavior.
- [x] Parenthesized integer-constant expression missing/invalid operand diagnostics are targeted before generic integer-constant parsing. Acceptance covered by `enum Bad { VALUE = () };`, fixed array lengths such as `int values[(,)];` / `int values[(?)];`, switch labels such as `case (:`, and keyword starts such as `(return)` reporting `expected integer constant in parenthesized integer constant expression before '<token>'` at the offending token instead of generic `..., found ...`, while preserving valid parenthesized integer constants and comma-operator rejection after a parsed left operand.

- [x] Selector-token invalid-start diagnostics for control/conditional expression boundaries are targeted before generic expression parsing. Acceptance covered by malformed `for (.field; ; )`, `if (.field)`, `while (->field)`, `for (...; .field; ...)`, `for (...; ...; ->field)`, `return 1 ? .field : 2`, and `return 1 ? 2 : ->field` reporting contextual `expected expression after <context>, found Dot/Arrow` diagnostics instead of generic `expected expression, found ...` or `unexpected token in for increment` fallbacks.

## P4 — product quality

- [x] CLI flag: `--tokens`
- [x] CLI flag: `--ast`
- [x] CLI flag: `--max-steps`
- [x] CLI flag: `--version`
- [x] Better README examples
- [x] Release notes and tags (`CHANGELOG.md` and annotated `v0.1` tag)
- [x] License file (`AGPL-3.0-or-later`) with README/package metadata and a regression test that keeps the strong copyleft declaration present.
